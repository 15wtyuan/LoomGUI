[package]
name = "loomgui_ffi_c"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = ["parse"]
parse = ["loomgui_core/parse"]

[dependencies]
loomgui_core = { path = "../loomgui_core", default-features = false }

[build-dependencies]
csbindgen = "1"
