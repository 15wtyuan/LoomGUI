# v1d.2 键盘 + 焦点 + Tab + `:focus` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1d.2 — keydown/up 事件通道 + 焦点状态（Scene.focused_node + Node.tabindex）+ click-to-focus + Tab/Shift+Tab 导航 + `:focus` 伪类 + FocusIn/Out 事件，检测全在 core，C# 只路由。

**Architecture:** 复用既有 EventRecord 流（EVT 12-15，key_code 复用 `touch_id`、modifiers 复用 `pad[0]`，零新输出 ABI）。焦点单一全局 `Scene.focused_node`；可聚焦 opt-in 靠 HTML `tabindex` 属性；`:focus` 伪类靠 `Node.focused` + dynamic.rs compound 门控（rematch 每帧跑）。Stage tick 管线：pending_focus_request 消费 → solve → process（含 click-to-focus）→ process_keys（keydown/up + Tab 导航）→ rematch → render。

**Tech Stack:** Rust（loomgui_core + loomgui_ffi_c，csbindgen FFI）/ C#（Unity 6.5，LoomEventHandler/LoomInputCollector）/ pkg.bin v5。

**Spec:** `docs/superpowers/specs/2026-06-24-v1d.2-keyboard-focus-design.md`

## Global Constraints

- **ABI 不变**：EventRecord 20B（node_id@0 / event_type@4 / click_count@5 / pad@6[2] / touch_id@8 / x@12 / y@16）、PointerEvent 16B。新 KeyEvent 8B 仅输入侧（key_code@0 / modifiers@4 / is_down@5 / pad@6[2]）。
- **EVT 常量**：EVT_KEY_DOWN=12 / EVT_KEY_UP=13 / EVT_FOCUS_IN=14 / EVT_FOCUS_OUT=15（复用 event_type 空位）。
- **key 事件 EventRecord 字段复用**：node_id=焦点节点；click_count=0；pad[0]=modifiers（FocusIn/Out=0）；pad[1]=0；touch_id=key_code（i32 重解释 u32；FocusIn/Out=0）；x/y=0.0。
- **pkg.bin v5**：PKG_FORMAT_VERSION 4→5，MIN=MAX=5（旧 v4 拒）。NodeBlock flags byte 后追加 tabindex:i32（None→i32::MIN 哨兵）。
- **Scene::build 7-tuple**：`(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)`（末位 tabindex）。
- **tabindex 语义**：None=不可聚焦；Some(-1)=仅编程聚焦；Some(0)=DOM 序可聚焦；Some(N>0)=显式序可聚焦。HTML 属性解析 `v.parse::<i32>().ok()`（非数字→None）。
- **focus 通道**：`focus_node(scene, new, out)` 是 input.rs 模块级 `pub(crate)` 自由函数——pending_focus_request 消费（Stage tick 最前）+ click-to-focus（process Down arm）+ Tab 导航（process_keys）三处共用。
- **request_focus 语义**：强制聚焦任意非 disabled 节点（含 tabindex=None/-1），照 fgui RequestFocus 不查 focusable；disabled 拒；记 `pending_focus_request`，tick 最前消费（不直接写 last_events，避免被 tick 覆盖）。
- **Tab 消费**：Tab/Shift+Tab 被导航消费，不发 keydown EventRecord。其他键有焦点才发 keydown/up，无焦点丢弃。
- **version 字符串**：`"v1d.1"` → `"v1d.2"`。
- **测试**：core `cargo test -p loomgui_core`；ffi `cargo test -p loomgui_ffi_c`。C# 静态-only（家里机编译跑，本机无 Unity）。
- ** Ponytail full**：最短 diff，YAGNI，`ponytail:` 注释标简化。

---

## File Structure

| 文件 | 职责 | 改动 |
|---|---|---|
| `loomgui_core/src/scene/node.rs` | Node 树 + Scene::build | +Node.tabindex/focused、+Scene.focused_node、build 7-tuple、gather_rec tabindex 解析 |
| `loomgui_core/src/asset/mod.rs` | pkg.bin 读写 | v4→v5、NodeBlock +tabindex、entries 7-tuple、extract_dynamic_rules +pseudo_focus |
| `loomgui_core/src/parse/selector.rs` | 选择器解析 | +`:focus` → pseudo_focus |
| `loomgui_core/src/style/dynamic.rs` | 伪类重匹配 | +Compound.pseudo_focus、+compound_matches_with_state focus 门控 |
| `loomgui_core/src/input.rs` | 指针/键盘状态机 | +KeyEvent、+EVT 常量、+focus_node、+build_tab_chain/next_focus、+process_keys、Down arm click-to-focus |
| `loomgui_core/src/stage.rs` | tick 管线 | +pending_keys/pending_focus_request、+set_key_input/request_focus/blur、tick 接 focus_node/process_keys |
| `loomgui_ffi_c/src/lib.rs` | FFI 导出 | +set_key_input/request_focus/focused_node、version v1d.2、abi 测 |
| `loomgui_unity/.../LoomEventHandler.cs` | C# 事件路由 | EventType +4、DispatchPending +4 case、EventContext KeyCode/Modifiers |
| `loomgui_unity/.../LoomInputCollector.cs` | C# 输入采集 | +键盘采集 set_key_input |
| `loomgui_unity/.../LoomEventHandlerTests.cs` | C# EditMode 测 | +KeyDown/FocusIn BubbleRoute 测 |
| `docs/v1d.2-home-verification.md` | 家里机验收文档 | 新建 |

---

### Task 1: Node.tabindex/focused + Scene.focused_node + Scene::build 7-tuple + gather_rec tabindex 解析

**Files:**
- Modify: `loomgui_core/src/scene/node.rs`（Node 结构 + Default + Scene 结构 + Scene::build 签名/构造 + gather_rec + build_scene 签名）
- Test: `loomgui_core/src/scene/node.rs`（既有 tests + parse_tests mod）

**Interfaces:**
- Produces: `Node.tabindex: Option<i32>`、`Node.focused: bool`、`Scene.focused_node: Option<NodeId>`、`Scene::build(entries: &[(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)])`。后续 T2/T4/T5 依赖。

- [ ] **Step 1: 写失败测——Node 默认 tabindex=None/focused=false**

在 `node.rs` 的 `tests` mod 末尾（`scene_default_has_empty_dynamic_rules` 后）加：

```rust
    #[test]
    fn node_has_tabindex_focused_defaults() {
        let n = Node::default();
        assert_eq!(n.tabindex, None, "tabindex 默认 None（不可聚焦）");
        assert!(!n.focused, "focused 默认 false");
    }
```

- [ ] **Step 2: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib scene::node::tests::node_has_tabindex_focused_defaults`
Expected: 编译失败（`Node` 无 `tabindex`/`focused` 字段）。

- [ ] **Step 3: 改 Node 结构 + Default**

`node.rs` Node 结构（`draggable` 字段后）加两字段：

```rust
    /// v1d.1：opt-in 可拖拽（HTML `draggable="true"` 属性）。drag 状态机据此发起 drag。
    pub draggable: bool,
    /// v1d.2：HTML tabindex 属性值。None=不可聚焦；Some(-1)=仅编程聚焦；
    /// Some(0)=DOM 序可聚焦；Some(N>0)=显式序可聚焦。
    pub tabindex: Option<i32>,
    /// v1d.2：当前是否聚焦（运行时，:focus 伪类源）。仅 focused_node 链上节点 true。
    pub focused: bool,
}
```

`Node::default()`（`draggable: false,` 后）补：

```rust
            draggable: false,
            tabindex: None,
            focused: false,
        }
    }
}
```

- [ ] **Step 4: 写失败测——Scene.focused_node 默认 None + build 7-tuple**

`tests` mod 加：

```rust
    #[test]
    fn scene_default_focused_node_none() {
        let s = Scene {
            roots: vec![],
            nodes: vec![],
            dynamic_rules: Default::default(),
            focused_node: None,
        };
        assert_eq!(s.focused_node, None, "Scene 默认 focused_node=None");
    }

    #[test]
    fn scene_build_7tuple_sets_tabindex() {
        let entries: Vec<(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)> = vec![
            (None, NodeKind::Container, ResolvedStyle::default(), Vec::new(), None, false, None),
            (Some(0), NodeKind::Button, ResolvedStyle::default(), Vec::new(), None, false, Some(0)),
            (Some(0), NodeKind::Button, ResolvedStyle::default(), Vec::new(), None, false, Some(3)),
        ];
        let scene = Scene::build(&entries);
        assert_eq!(scene.nodes[0].tabindex, None, "root tabindex=None");
        assert_eq!(scene.nodes[1].tabindex, Some(0), "btn1 tabindex=Some(0)");
        assert_eq!(scene.nodes[2].tabindex, Some(3), "btn2 tabindex=Some(3)");
        assert!(!scene.nodes[0].focused, "focused 默认 false");
        assert_eq!(scene.focused_node, None, "build 后 focused_node=None");
    }
```

- [ ] **Step 5: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib scene::node::tests::scene_build_7tuple_sets_tabindex`
Expected: 编译失败（Scene 无 `focused_node`；build 是 6-tuple）。

- [ ] **Step 6: 改 Scene 结构 + Scene::build 签名/构造**

`Scene` 结构（`dynamic_rules` 字段后）加：

```rust
    /// 运行时伪类重匹配规则表（spec §5.5）。默认空；T7 包加载填，T8 inline 路径空。
    pub dynamic_rules: crate::style::dynamic::DynamicRuleTable,
    /// v1d.2：当前焦点节点（单一全局，照 fgui Stage.focus）。None=无焦点。
    pub focused_node: Option<NodeId>,
}
```

`Scene::build` 签名 + 构造改 7-tuple：

```rust
    pub fn build(entries: &[(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)]) -> Scene {
        let mut scene = Scene {
            roots: Vec::new(),
            nodes: Vec::new(),
            dynamic_rules: crate::style::dynamic::DynamicRuleTable::default(),
            focused_node: None,
        };
        for (i, (parent_idx, kind, style, classes, id_attr, draggable, tabindex)) in entries.iter().enumerate() {
            scene.nodes.push(Node {
                id: NodeId(i),
                parent: parent_idx.map(NodeId),
                kind: kind.clone(),
                style: style.clone(),
                base_style: style.clone(),
                taffy_id: None,
                layout_rect: Rect::default(),
                clip_rect: if style.overflow_hidden {
                    Some(Rect::default())
                } else {
                    None
                },
                children: Vec::new(),
                dirty_mesh: true,
                dirty_text: matches!(kind, NodeKind::Text { .. }),
                classes: classes.clone(),
                id_attr: id_attr.clone(),
                touchable: style.touchable,
                hovered: false,
                active: false,
                disabled: false,
                draggable: *draggable,
                tabindex: *tabindex,
                focused: false,
            });
        }
        // 接 children + roots（entries 先序 → 按 parent 出现序填，与旧 build_rec 一致）
        for i in 0..entries.len() {
            match entries[i].0 {
                Some(p) => scene.nodes[p].children.push(NodeId(i)),
                None => scene.roots.push(NodeId(i)),
            }
        }
        scene
    }
```

> **注意既有测试 helper 也要补**：`tests` mod 内 `one_button_scene()`/`button_with_text_child_scene()`/`nested_scene()` 等**直接构造 `Scene { roots, nodes, dynamic_rules }`** 的字面量（不含 focused_node）会编译失败——加 `focused_node: None`。这些是既有测（input.rs 也直接构造 Scene，但那是 input.rs 文件）。本 task 只改 node.rs，node.rs 内直接构造 Scene 的字面量逐个补 `focused_node: None`。input.rs 的 Scene 字面量在 T4 改。

- [ ] **Step 7: 写失败测——gather_rec tabindex 解析（parse_tests mod）**

`parse_tests` mod 末尾加：

```rust
    #[test]
    fn tabindex_attr_parsed() {
        let html = r#"<div><button tabindex="0">a</button><button tabindex="3">b</button><button tabindex="-1">c</button><button tabindex="abc">d</button><button>e</button></div>"#;
        let css = "";
        let tree = parse_html(html).unwrap();
        let sheet = parse_css(css).unwrap();
        let styles = resolve_styles(&tree, &sheet);
        let scene = build_scene(&tree, &styles);
        let btns: Vec<_> = scene.nodes.iter().filter(|n| matches!(n.kind, NodeKind::Button)).collect();
        assert_eq!(btns.len(), 5);
        assert_eq!(btns[0].tabindex, Some(0), "tabindex=\"0\" → Some(0)");
        assert_eq!(btns[1].tabindex, Some(3), "tabindex=\"3\" → Some(3)");
        assert_eq!(btns[2].tabindex, Some(-1), "tabindex=\"-1\" → Some(-1)");
        assert_eq!(btns[3].tabindex, None, "tabindex=\"abc\"（非数字）→ None");
        assert_eq!(btns[4].tabindex, None, "无 tabindex 属性 → None");
    }
```

- [ ] **Step 8: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib scene::node::parse_tests::tabindex_attr_parsed`
Expected: FAIL（`tabindex` 字段读到了但 gather_rec 未填——恒 None，断言 Some(0) 失败）。注：若 Step 6 已让字段存在，此测编译过但断言失败。

- [ ] **Step 9: 改 build_scene + gather_rec 7-tuple + tabindex 解析**

`build_scene` 函数签名 + entries 类型改 7-tuple：

```rust
#[cfg(feature = "parse")]
pub fn build_scene(tree: &ElementTree, styles: &[ResolvedStyle]) -> Scene {
    let mut entries: Vec<(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)> = Vec::new();
    for root in &tree.roots {
        gather_rec(tree, styles, *root, None, &mut entries);
    }
    Scene::build(&entries)
}

#[cfg(feature = "parse")]
fn gather_rec(
    tree: &ElementTree,
    styles: &[ResolvedStyle],
    el_id: ElementId,
    parent_idx: Option<usize>,
    entries: &mut Vec<(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)>,
) -> usize {
```

在 `gather_rec` 内 `draggable` 解析后加 tabindex 解析，entries.push 改 7-tuple：

```rust
    // v1d.1：draggable="true" → Node.draggable（HTML 原生属性，AI 熟悉）。
    let draggable = el.attrs.get("draggable").map(|v| v == "true").unwrap_or(false);
    // v1d.2：tabindex 属性 → Option<i32>。非数字 → None（照 DOM 容错：无效值忽略）。
    // 语义：None=不可聚焦；Some(-1)=仅编程；Some(0)=DOM 序；Some(N>0)=显式序。
    let tabindex = el.attrs.get("tabindex").and_then(|v| v.parse::<i32>().ok());
    let my_idx = entries.len();
    entries.push((parent_idx, kind.clone(), style.clone(), el.classes.clone(), el.id.clone(), draggable, tabindex));
```

`gather_rec` 内 Container/Button 裸文本产 Text 子的 entries.push（既有 `(Some(my_idx), NodeKind::Text {...}, ts, Vec::new(), None, false)`）也改 7-tuple（末位加 `, None`）：

```rust
            entries.push((Some(my_idx), NodeKind::Text { content: text.clone() }, ts, Vec::new(), None, false, None));
```

- [ ] **Step 10: 跑 node.rs 全测**

Run: `cargo test -p loomgui_core --lib scene::node`
Expected: PASS（含新 3 测 + 既有全过；既有直接构造 Scene 的字面量已补 focused_node）。

> **注**：此时 `cargo test -p loomgui_core` 全局会编译失败——input.rs/stage.rs/asset 内直接构造 Scene 字面量 + Scene::build 6-tuple 调用点未改（T2/T4/T5 改）。本 task 只验 node.rs 自身测过。Step 10 跑 `scene::node` 子模块即可。

- [ ] **Step 11: Commit**

```bash
git add loomgui_core/src/scene/node.rs
git commit -m "feat(v1d.2-T1): Node.tabindex/focused + Scene.focused_node + build 7-tuple + tabindex 解析"
```

---

### Task 2: pkg.bin v5 — NodeBlock +tabindex + entries 7-tuple

**Files:**
- Modify: `loomgui_core/src/asset/mod.rs`（PKG_FORMAT_VERSION、write_package NodeBlock、read_package entries/NodeBlock、既有测 entries 6→7-tuple）

**Interfaces:**
- Consumes: T1 的 `Scene::build` 7-tuple、`Node.tabindex: Option<i32>`。
- Produces: pkg.bin v5（NodeBlock flags byte 后追加 tabindex:i32，None→i32::MIN 哨兵）；`read_package` 解出 tabindex 填 Node。

- [ ] **Step 1: 写失败测——pkg v5 round-trip tabindex**

`asset/mod.rs` 的 `tests` mod 末尾加：

```rust
    #[test]
    fn pkg_v5_preserves_tabindex() {
        use crate::style::resolved::ResolvedStyle;
        let entries: Vec<(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)> = vec![
            (None, NodeKind::Container, ResolvedStyle::default(), Vec::new(), None, false, None),
            (Some(0), NodeKind::Button, ResolvedStyle::default(), Vec::new(), None, false, Some(0)),
            (Some(0), NodeKind::Button, ResolvedStyle::default(), Vec::new(), None, false, Some(3)),
            (Some(0), NodeKind::Button, ResolvedStyle::default(), Vec::new(), None, false, Some(-1)),
        ];
        let scene = Scene::build(&entries);
        let pkg = write_package(&scene, (100.0, 50.0), &AtlasSection::default(), &crate::style::dynamic::DynamicRuleTable::default());
        let (scene2, _, _) = read_package(&pkg).unwrap();
        assert_eq!(scene2.nodes[0].tabindex, None, "root tabindex=None round-trip");
        assert_eq!(scene2.nodes[1].tabindex, Some(0));
        assert_eq!(scene2.nodes[2].tabindex, Some(3));
        assert_eq!(scene2.nodes[3].tabindex, Some(-1));
    }

    #[test]
    fn pkg_v5_rejects_v4() {
        // 手搓 v4 包（version=4）——v5 MIN=5 应拒 TooOld
        let mut pkg = Vec::new();
        pkg.extend_from_slice(&PKG_MAGIC.to_le_bytes());
        pkg.extend_from_slice(&4u32.to_le_bytes()); // version=4
        pkg.extend_from_slice(&0u32.to_le_bytes());
        pkg.extend_from_slice(&0u32.to_le_bytes());
        pkg.extend_from_slice(&0u32.to_le_bytes());
        pkg.extend_from_slice(&100.0f32.to_le_bytes());
        pkg.extend_from_slice(&50.0f32.to_le_bytes());
        match read_package(&pkg) {
            Err(PkgError::TooOld(4)) => (), // 预期
            other => panic!("v4 应被 v5 拒，got {:?}", other),
        }
    }
```

- [ ] **Step 2: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib asset::tests::pkg_v5_preserves_tabindex`
Expected: FAIL（PKG_FORMAT_VERSION 仍 4，write 出的包 tabindex 未写，read 读不到 Some(0)）。

- [ ] **Step 3: 改版本号 + 模块 doc**

`asset/mod.rs` 顶部：

```rust
//! 包格式（spec §5）：.pkg.bin v5。
//! Rust-internal（packager 写、runtime 读，C# 不解析）。
//!
//! 扁平布局：Header(28B) + StringTable + NodeBlock（DFS 先序，含 classes/id/flags/tabindex）+
//! AtlasSection + DynamicRuleSection（v3 新增，bincode 整个 DynamicRuleTable）。
//! style 字段 = bincode(ResolvedStyle)。字符串表放 text content + image src +
//! atlas filename + sprite src + classes + id_attr（统一 intern 去重）。
//! v4：NodeBlock 末 +1 byte flags（bit0=draggable）。v5：flags 后 +tabindex:i32（None→i32::MIN）。

pub const PKG_MAGIC: u32 = 0x474B504C; // 磁盘字节(LE) "LPKG"（不与 frame blob "LOOM" 撞）
pub const PKG_FORMAT_VERSION: u32 = 5; // v5：+NodeBlock tabindex（v4=draggable flags，v3=DynamicRuleSection+classes/id，v2=AtlasSection，v1=纯 scene）
const MIN_VERSION: u32 = 5;
const MAX_VERSION: u32 = 5;
```

- [ ] **Step 4: 改 write_package NodeBlock——flags 后追加 tabindex**

`write_package` 内 NodeBlock 写循环（既有 `let flags: u8 = ...; out.push(flags);` 后）加 tabindex 写。None→`i32::MIN` 哨兵：

```rust
        // v4：flags byte（bit0=draggable）
        let flags: u8 = if scene.nodes[node_i].draggable { 0x01 } else { 0x00 };
        out.push(flags);
        // v5：tabindex i32（None→i32::MIN 哨兵，反序列化还原 None）
        let tab = scene.nodes[node_i].tabindex.unwrap_or(i32::MIN);
        out.extend_from_slice(&tab.to_le_bytes());
    }
```

- [ ] **Step 5: 改 read_package entries 7-tuple + NodeBlock 读 tabindex**

`read_package` 内 `entries` 类型改 7-tuple：

```rust
    // NodeBlock → entries（v5：tabindex 来自 flags 后 i32，i32::MIN→None）
    let mut entries: Vec<(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)> = Vec::with_capacity(node_count);
```

NodeBlock 读循环（flags 读后、`let parent = ...` 前）加 tabindex 读 + entries.push 改 7-tuple：

```rust
        // v4：flags byte（bit0=draggable）
        let flags = r.u8("flags")?;
        let draggable = (flags & 0x01) != 0;
        // v5：tabindex i32（i32::MIN 哨兵 → None）
        let tab_raw = r.i32("tabindex")?;
        let tabindex = if tab_raw == i32::MIN { None } else { Some(tab_raw) };
        let parent = if pidx < 0 { None } else { Some(pidx as usize) };
        let kind = match kind_tag {
            KIND_CONTAINER => NodeKind::Container,
            KIND_BUTTON => NodeKind::Button,
            KIND_IMAGE => NodeKind::Image {
                src: string_at(&strings, src_idx)?,
            },
            KIND_TEXT => NodeKind::Text {
                content: string_at(&strings, text_idx)?,
            },
            other => return Err(PkgError::BadKind(other)),
        };
        entries.push((parent, kind, style, classes, id_attr, draggable, tabindex));
    }
```

- [ ] **Step 6: 改既有测 entries 6→7-tuple + read_rejects_unsupported_version**

`asset/mod.rs` tests mod 内**所有**直接构造 entries 的字面量补末位 tabindex（6-tuple → 7-tuple）。逐个改：`write_read_roundtrip_preserves_scene` / `read_rejects_unsupported_version` / `atlas_section_round_trips` / `pkg_v3_round_trip_preserves_dynamic_rules` / `pkg_v3_empty_dynamic_rules` / `pkg_v3_nodeblock_preserves_classes_and_id_attr` / `stringtable_dedups_repeated_strings` / `pkg_v4_preserves_draggable_flag`。

每条：类型标注 `(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool)` → 末加 `, Option<i32>`；每条元组末位加 `, None`。

例（`write_read_roundtrip_preserves_scene`）：
```rust
        let entries: Vec<(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)> = vec![
            (None, NodeKind::Container, ResolvedStyle::default(), Vec::new(), None, false, None),
            (
                Some(0),
                NodeKind::Text { content: "hi".into() },
                ResolvedStyle::default(),
                Vec::new(),
                None,
                false,
                None,
            ),
            // ... 其余每条末位加 , None
        ];
```

`read_rejects_unsupported_version` 改（MIN=MAX=5，原 v5→TooNew/v3→TooOld 改 v6→TooNew/v4→TooOld）：

```rust
    #[test]
    fn read_rejects_unsupported_version() {
        let entries: Vec<(Option<usize>, NodeKind, ResolvedStyle, Vec<String>, Option<String>, bool, Option<i32>)> =
            vec![(None, NodeKind::Container, ResolvedStyle::default(), Vec::new(), None, false, None)];
        let mut bytes = write_package(&Scene::build(&entries), (100.0, 100.0), &AtlasSection::default(), &crate::style::dynamic::DynamicRuleTable::default());
        bytes[4..8].copy_from_slice(&6u32.to_le_bytes()); // version=6 → too new
        assert!(matches!(read_package(&bytes), Err(PkgError::TooNew(6))));
        bytes[4..8].copy_from_slice(&4u32.to_le_bytes()); // version=4 → too old（v5 起拒 v4）
        assert!(matches!(read_package(&bytes), Err(PkgError::TooOld(4))));
    }
```

`pkg_v3_rejects_v2`（断言 TooOld(2)）/`pkg_v4_rejects_v3`（断言 TooOld(3)）——v5 MIN=5 仍拒 v2/v3，断言不变，无需改。

- [ ] **Step 7: 跑 asset 全测**

Run: `cargo test -p loomgui_core --lib asset`
Expected: PASS（含新 2 测 + 既有全过，entries 全 7-tuple）。

> **注**：`cargo test -p loomgui_core` 全局此时仍失败——input.rs/stage.rs 直接构造 Scene 字面量未补 focused_node（T4/T5）；loomgui_ffi_c abi_tests 手搓 entries 6-tuple（T5）。本 task 验 asset 模块测过。

- [ ] **Step 8: Commit**

```bash
git add loomgui_core/src/asset/mod.rs
git commit -m "feat(v1d.2-T2): pkg.bin v5 — NodeBlock +tabindex + entries 7-tuple"
```

---

### Task 3: selector `:focus` 解析 + Compound.pseudo_focus + dynamic focus 门控 + extract_dynamic_rules

**Files:**
- Modify: `loomgui_core/src/parse/selector.rs`（parse 循环 + Compound 构造）
- Modify: `loomgui_core/src/style/dynamic.rs`（Compound + compound_matches_with_state + btn_scene helper）
- Modify: `loomgui_core/src/asset/mod.rs`（extract_dynamic_rules +pseudo_focus）

**Interfaces:**
- Produces: `Compound.pseudo_focus: bool`、`:focus` 选择器解析、`compound_matches_with_state` focus 门控（`node.focused`）。rematch_pseudo_classes 既有每帧跑自动吃。

> **结构确认（已查源码）**：`Compound` **只在 `style/dynamic.rs` 定义一处**——`parse/selector.rs:7` `pub use crate::style::dynamic::{...Compound...}` 重导出同一类型。selector.rs 的 parse 循环（selector.rs:138-146）构造的就是 dynamic.rs 的 Compound。故加 `pseudo_focus`：① dynamic.rs Compound 结构定义加字段；② selector.rs parse 循环填字段 + match 加 `"focus"`；③ dynamic.rs `compound_matches_with_state` 加门控。三处，非两个结构体。

- [ ] **Step 1: 写失败测——selector 解析 :focus**

`selector.rs` 的 `tests` mod（既有 `:hover` 测旁）加：

```rust
    #[test]
    fn parse_focus_pseudo() {
        let s = parse_selector(".btn:focus").unwrap();
        assert!(s.compound[0].pseudo_focus, ":focus → pseudo_focus=true");
        assert!(!s.compound[0].pseudo_hover);
    }

    #[test]
    fn parse_focus_with_hover() {
        let s = parse_selector(".btn:focus:hover").unwrap();
        assert!(s.compound[0].pseudo_focus, ":focus → pseudo_focus");
        assert!(s.compound[0].pseudo_hover, ":hover → pseudo_hover");
    }
```

- [ ] **Step 2: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib parse::selector::tests::parse_focus_pseudo`
Expected: FAIL（Compound 无 `pseudo_focus` 字段，编译失败）。

- [ ] **Step 3: 改 selector.rs parse 循环 + parse 版 Compound**

parse 循环（selector.rs:120-146 区域）加 focus：

```rust
        let mut pseudo_hover = false;
        let mut pseudo_active = false;
        let mut pseudo_disabled = false;
        let mut pseudo_focus = false;
        let mut rest = text.as_str();
        while let Some(colon) = rest.find(':') {
            let after = &rest[colon + 1..];
            let end = after
                .find(|c: char| c == '.' || c == '#' || c == ':')
                .unwrap_or(after.len());
            let name = &after[..end];
            match name {
                "hover" => pseudo_hover = true,
                "active" => pseudo_active = true,
                "disabled" => pseudo_disabled = true,
                "focus" => pseudo_focus = true,
                _ => {} // 未知伪类静默忽略（v1d.2 认 hover/active/disabled/focus）
            }
            rest = &after[end..];
        }
        compound.push(Compound {
            tag,
            classes,
            id,
            combinator: *comb,
            pseudo_hover,
            pseudo_active,
            pseudo_disabled,
            pseudo_focus,
        });
```

若 parse 与 dynamic 共用同一 Compound（selector.rs 内定义），本 Step 即足够（一处）。若 parse 有独立 Compound，还需在 selector.rs 的 Compound 定义加 `pub pseudo_focus: bool`。读 selector.rs 确认 Compound 定义位置，加字段。

> **已确认**：Compound 是 dynamic.rs 单一定义（selector.rs `pub use` 重导出）。故 parse 循环构造的 Compound 字段（含新 `pseudo_focus`）填在此处即可，无需在 selector.rs 改结构定义——结构定义在 dynamic.rs（Step 6 改）。本 Step 只改 parse 循环的 match + Compound 字面量填字段。

- [ ] **Step 4: 跑 selector 测**

Run: `cargo test -p loomgui_core --lib parse::selector`
Expected: PASS（新 2 测 + 既有）。

- [ ] **Step 5: 写失败测——dynamic :focus 门控**

`dynamic.rs` 的 `tests` mod（既有 hover/active/disabled 测旁）加：

```rust
    #[test]
    fn focus_pseudo_matches_focused_node() {
        let mut s = btn_scene();
        s.dynamic_rules
            .rules
            .push(rule(".btn:focus", "background-color", "#0000ff"));
        s.nodes[1].focused = true;
        let changed = rematch_pseudo_classes(&mut s);
        assert_eq!(
            s.nodes[1].style.background_color,
            Some([0.0, 0.0, 1.0, 1.0]),
            "focused → :focus 匹配 → 蓝"
        );
        assert!(!changed, "background_color 视觉字段 → layout 不 dirty");
    }

    #[test]
    fn focus_pseudo_no_match_unfocused() {
        let mut s = btn_scene();
        s.nodes[1].base_style.background_color = None;
        s.dynamic_rules
            .rules
            .push(rule(".btn:focus", "background-color", "#0000ff"));
        s.nodes[1].focused = false;
        rematch_pseudo_classes(&mut s);
        assert_eq!(s.nodes[1].style.background_color, None, "unfocused → :focus 不匹配 → 回 base");
    }

    #[test]
    fn focus_pseudo_in_descendant_chain() {
        // .parent:focus .child —— parent 聚焦 → child style 变
        let mut root = Node::default();
        root.id = NodeId(0);
        root.classes = vec!["parent".to_string()];
        root.layout_rect = Rect { x: 0.0, y: 0.0, w: 200.0, h: 200.0 };
        let mut child = Node::default();
        child.id = NodeId(1);
        child.parent = Some(NodeId(0));
        child.classes = vec!["child".to_string()];
        child.layout_rect = Rect { x: 0.0, y: 0.0, w: 50.0, h: 50.0 };
        root.children = vec![NodeId(1)];
        let mut s = Scene {
            roots: vec![NodeId(0)],
            nodes: vec![root, child],
            dynamic_rules: DynamicRuleTable::default(),
            focused_node: None,
        };
        s.dynamic_rules.rules.push(rule(".parent:focus .child", "color", "#0000ff"));
        s.nodes[0].focused = true;
        rematch_pseudo_classes(&mut s);
        assert_eq!(s.nodes[1].style.color, [0.0, 0.0, 1.0, 1.0], "parent:focus → child 变蓝");
    }
```

- [ ] **Step 6: 改 dynamic.rs Compound + compound_matches_with_state + btn_scene helper**

若 dynamic.rs 有独立 `Compound`（serde 进 pkg.bin），加字段（Compound 是 dynamic.rs 单一定义——见本 task 顶部「结构确认」）：

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Compound {
    pub tag: Option<String>,
    pub classes: Vec<String>,
    pub id: Option<String>,
    pub combinator: Combinator,
    pub pseudo_hover: bool,
    pub pseudo_active: bool,
    pub pseudo_disabled: bool,
    pub pseudo_focus: bool,
}
```

> Compound derive Serialize/Deserialize（bincode 进 pkg.bin DynamicRuleSection）。加字段后旧 dynamic blob 不兼容——但 pkg.bin v5 整体新（T2 升 version），v5 包内 dynamic blob 必是新 Compound，一致。✅

`compound_matches_with_state`（dynamic.rs）加 focus 门控：

```rust
fn compound_matches_with_state(c: &Compound, node_id: NodeId, scene: &Scene) -> bool {
    let node = &scene.nodes[node_id.0];
    if c.pseudo_hover && !node.hovered {
        return false;
    }
    if c.pseudo_active && !node.active {
        return false;
    }
    if c.pseudo_disabled && !node.disabled {
        return false;
    }
    if c.pseudo_focus && !node.focused {
        return false;
    }
    compound_matches_node(c, node)
}
```

`btn_scene()` helper + dynamic.rs tests mod 内所有直接构造 `Scene { roots, nodes, dynamic_rules }` 的字面量补 `focused_node: None`（T1 加字段后这些字面量缺该字段，编译失败）。

- [ ] **Step 7: 改 extract_dynamic_rules +pseudo_focus**

`asset/mod.rs` `extract_dynamic_rules` 内 `has_pseudo`（既有 3 个 pseudo）加 `pseudo_focus`：

```rust
        let has_pseudo = sel
            .compound
            .iter()
            .any(|c| c.pseudo_hover || c.pseudo_active || c.pseudo_disabled || c.pseudo_focus);
```

- [ ] **Step 8: 跑 selector + dynamic 全测**

Run: `cargo test -p loomgui_core --lib parse::selector && cargo test -p loomgui_core --lib style::dynamic`
Expected: PASS（新 5 测 + 既有）。

- [ ] **Step 9: Commit**

```bash
git add loomgui_core/src/parse/selector.rs loomgui_core/src/style/dynamic.rs loomgui_core/src/asset/mod.rs
git commit -m "feat(v1d.2-T3): :focus 伪类 — selector 解析 + Compound.pseudo_focus + 门控 + extract_dynamic"
```

---

### Task 4: input.rs — KeyEvent + EVT 常量 + focus_node + Tab 链 + keydown/up + click-to-focus + process_keys

**Files:**
- Modify: `loomgui_core/src/input.rs`（KeyEvent 结构、EVT 常量、focus_node、build_tab_chain、next_focus、process_keys、PointerKind::Down arm click-to-focus、既有 Scene 字面量补 focused_node）

**Interfaces:**
- Consumes: T1 的 `Node.tabindex`/`Node.focused`/`Scene.focused_node`。
- Produces: `KeyEvent`（#[repr(C)] POD）、EVT_KEY_DOWN/UP/FOCUS_IN/FOCUS_OUT、`pub(crate) fn focus_node(scene, new, out)`、`pub(crate) fn process_keys(scene, keys, out)`。T5（Stage）依赖。

- [ ] **Step 1: 改 input.rs 既有 Scene 字面量补 focused_node**

input.rs tests mod 内所有直接构造 `Scene { roots, nodes, dynamic_rules }` 的字面量（`one_button_scene`/`button_with_text_child_scene`/`nested_scene`/各 inline 测）补 `focused_node: None`。T1 加字段后这些字面量缺该字段，编译失败。逐个加。

> 这些 helper 很多（`one_button_scene`/`button_with_text_child_scene`/`nested_scene` 及 `hover_between_siblings_old_chain_rollout`/`two_touches_independent_down_up`/`hover_global_merge_two_fingers`/`active_global_merge_two_fingers`/`rollover_per_touch_independent`/`down_leaf_destroyed_fallback_to_ancestor`/`one_draggable_button_scene` 等内联构造）。全 grep `dynamic_rules: Default::default()` 或 `dynamic_rules: DynamicRuleTable` 后的 `}` 前补 `,\n            focused_node: None,`。

- [ ] **Step 2: 写失败测——focus_node 发 FocusOut 旧 + FocusIn 新**

input.rs tests mod 末尾加（v1d.2 区，既有 `// ===== v1d.1 T4` 后）：

```rust
    // ===== v1d.2 T4: 焦点 + 键盘 =====

    /// root + btnA(tabindex=0) + btnB(tabindex=0)，均 @ 各位可区分。
    fn two_focusable_scene() -> Scene {
        let mut root = Node::default();
        root.id = NodeId(0);
        root.layout_rect = Rect { x: 0.0, y: 0.0, w: 200.0, h: 200.0 };
        let mut a = Node::default();
        a.id = NodeId(1); a.parent = Some(NodeId(0)); a.kind = NodeKind::Button;
        a.tabindex = Some(0);
        a.layout_rect = Rect { x: 0.0, y: 0.0, w: 50.0, h: 50.0 };
        let mut b = Node::default();
        b.id = NodeId(2); b.parent = Some(NodeId(0)); b.kind = NodeKind::Button;
        b.tabindex = Some(0);
        b.layout_rect = Rect { x: 100.0, y: 0.0, w: 50.0, h: 50.0 };
        root.children = vec![NodeId(1), NodeId(2)];
        Scene { roots: vec![NodeId(0)], nodes: vec![root, a, b], dynamic_rules: Default::default(), focused_node: None }
    }

    #[test]
    fn focus_node_emits_focusout_then_focusin() {
        let mut s = two_focusable_scene();
        let mut out = Vec::new();
        // 先聚焦 A
        focus_node(&mut s, Some(NodeId(1)), &mut out);
        assert!(s.nodes[1].focused, "A focused=true");
        assert_eq!(s.focused_node, Some(NodeId(1)));
        // 聚焦 B → FocusOut@A + FocusIn@B
        focus_node(&mut s, Some(NodeId(2)), &mut out);
        assert!(!s.nodes[1].focused, "A focused=false（失焦）");
        assert!(s.nodes[2].focused, "B focused=true");
        assert_eq!(s.focused_node, Some(NodeId(2)));
        let types: Vec<u8> = out.iter().map(|e| e.event_type).collect();
        // out 含 [FocusIn@A, FocusOut@A, FocusIn@B]
        assert!(out.iter().any(|e| e.event_type == EVT_FOCUS_IN && e.node_id == 1), "FocusIn@A");
        assert!(out.iter().any(|e| e.event_type == EVT_FOCUS_OUT && e.node_id == 1), "FocusOut@A");
        assert!(out.iter().any(|e| e.event_type == EVT_FOCUS_IN && e.node_id == 2), "FocusIn@B");
    }

    #[test]
    fn focus_node_same_target_no_event() {
        let mut s = two_focusable_scene();
        let mut out = Vec::new();
        focus_node(&mut s, Some(NodeId(1)), &mut out);
        let mut out2 = Vec::new();
        focus_node(&mut s, Some(NodeId(1)), &mut out2);   // 同目标
        assert!(out2.is_empty(), "同目标重复聚焦 → 不发事件");
    }

    #[test]
    fn focus_node_clear_blur() {
        let mut s = two_focusable_scene();
        let mut out = Vec::new();
        focus_node(&mut s, Some(NodeId(1)), &mut out);
        focus_node(&mut s, None, &mut out);   // 清焦点
        assert_eq!(s.focused_node, None);
        assert!(!s.nodes[1].focused);
        assert!(out.iter().any(|e| e.event_type == EVT_FOCUS_OUT && e.node_id == 1), "blur → FocusOut@A");
    }
```

- [ ] **Step 3: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib input::tests::focus_node_emits_focusout_then_focusin`
Expected: FAIL（`focus_node`/`EVT_FOCUS_IN` 未定义，编译失败）。

- [ ] **Step 4: 加 EVT 常量 + focus_node**

input.rs 顶部 EVT 常量区（EVT_LONG_PRESS=9 后）加：

```rust
pub const EVT_KEY_DOWN: u8 = 12;
pub const EVT_KEY_UP: u8 = 13;
pub const EVT_FOCUS_IN: u8 = 14;
pub const EVT_FOCUS_OUT: u8 = 15;
```

KeyEvent 结构（PointerKind 后）加：

```rust
/// v1d.2：键盘输入事件（FFI POD）。C# set_key_input 推一组；core process_keys 产 keydown/up EventRecord。
/// 8B：key_code@0(4) + modifiers@4(1) + is_down@5(1) + pad@6(2)。
#[repr(C)]
#[derive(Debug, Clone, Copy)]
pub struct KeyEvent {
    pub key_code: u32,   // KeyCode 枚举值（Unity KeyCode 转 u32；core 不解释语义，只透传 + Tab 判定）
    pub modifiers: u8,   // bit0=shift / bit1=ctrl / bit2=alt
    pub is_down: bool,   // true=按下→keydown；false=松开→keyup
    pub pad: [u8; 2],
}

/// v1d.2 modifiers 位掩码（KeyEvent.modifiers）。
pub const MOD_SHIFT: u8 = 0x01;
pub const MOD_CTRL: u8 = 0x02;
pub const MOD_ALT: u8 = 0x04;

/// v1d.2：Tab 的 KeyCode 值（Unity KeyCode.Tab = 9）。core 内判定 Tab 导航用。
pub const KEY_TAB: u32 = 9;
```

> **KeyCode.Tab 值确认**：Unity `KeyCode.Tab` 枚举值。读 LoomInputCollector 或 Unity 文档确认 = 9。实施时若 C# 用 `(uint)KeyCode.Tab` 传，core 这边匹配同值。本 plan 定 KEY_TAB=9（Unity KeyCode.Tab）。C# 侧 T6 用 `(uint)KeyCode.Tab` 保证一致。

focus_node 自由函数（input.rs，PointerState impl 块外，模块级）：

```rust
/// v1d.2：设焦点为 new（None=清除焦点）。发 FocusOut@旧焦点 + FocusIn@新焦点。
/// input.rs 模块级 pub(crate) 自由函数——process（click-to-focus）+ process_keys（Tab）+ Stage（pending_focus_request）共用。
/// 写 scene.focused_node + node.focused 标志 + 推 FocusOut/FocusIn 进 out。
fn focus_node(scene: &mut Scene, new: Option<NodeId>, out: &mut Vec<EventRecord>) {
    let old = scene.focused_node;
    if old == new {
        return; // 无变化不发
    }
    if let Some(o) = old {
        if o.0 < scene.nodes.len() {
            scene.nodes[o.0].focused = false;
        }
        out.push(EventRecord {
            node_id: o.0 as u32,
            event_type: EVT_FOCUS_OUT,
            click_count: 0,
            pad: [0, 0],
            touch_id: 0,
            x: 0.0,
            y: 0.0,
        });
    }
    if let Some(n) = new {
        if n.0 < scene.nodes.len() {
            scene.nodes[n.0].focused = true;
        }
        out.push(EventRecord {
            node_id: n.0 as u32,
            event_type: EVT_FOCUS_IN,
            click_count: 0,
            pad: [0, 0],
            touch_id: 0,
            x: 0.0,
            y: 0.0,
        });
    }
    scene.focused_node = new;
}
```

- [ ] **Step 5: 跑 focus_node 测**

Run: `cargo test -p loomgui_core --lib input::tests::focus_node`
Expected: PASS（新 3 测）。

- [ ] **Step 6: 写失败测——Tab 链 + next_focus**

input.rs tests mod 加：

```rust
    /// root + A(tabindex=2) + B(tabindex=1) + C(tabindex=0) + D(tabindex=-1) + E(无属性) + disabled F(tabindex=0)
    fn tab_chain_scene() -> Scene {
        let mut root = Node::default();
        root.id = NodeId(0);
        root.layout_rect = Rect { x: 0.0, y: 0.0, w: 400.0, h: 200.0 };
        let mk = |id: usize, ti: Option<i32>, disabled: bool| {
            let mut n = Node::default();
            n.id = NodeId(id); n.parent = Some(NodeId(0)); n.kind = NodeKind::Button;
            n.tabindex = ti; n.disabled = disabled;
            n.layout_rect = Rect { x: id as f32 * 50.0, y: 0.0, w: 40.0, h: 40.0 };
            n
        };
        let a = mk(1, Some(2), false);
        let b = mk(2, Some(1), false);
        let c = mk(3, Some(0), false);
        let d = mk(4, Some(-1), false);
        let e = mk(5, None, false);
        let f = mk(6, Some(0), true);   // disabled
        root.children = vec![NodeId(1), NodeId(2), NodeId(3), NodeId(4), NodeId(5), NodeId(6)];
        Scene { roots: vec![NodeId(0)], nodes: vec![root, a, b, c, d, e, f], dynamic_rules: Default::default(), focused_node: None }
    }

    #[test]
    fn tab_chain_orders_by_tabindex_then_dfs() {
        let s = tab_chain_scene();
        let chain = build_tab_chain(&s);
        // 正整数组 [B(tabindex=1), A(tabindex=2)] 升序，后接 0 组 [C(tabindex=0)]。
        // D(-1)/E(None)/F(disabled) 不进。
        let ids: Vec<usize> = chain.iter().map(|n| n.0).collect();
        assert_eq!(ids, vec![2, 1, 3], "链序：正整数升序(B=1,A=2)后接 0 组(C=0)");
    }

    #[test]
    fn tab_forward_cycles_through_chain() {
        let mut s = tab_chain_scene();
        let mut out = Vec::new();
        // 焦点 None → Tab → B(2)（链首）
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: 0, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(2)), "首次 Tab → 链首 B");
        // Tab → A(1)
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: 0, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(1)), "Tab → A");
        // Tab → C(3) → Tab → wrap 回 B(2)
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: 0, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(3)), "Tab → C");
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: 0, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(2)), "链尾 Tab → wrap 回链首 B");
    }

    #[test]
    fn shift_tab_backward_cycles() {
        let mut s = tab_chain_scene();
        let mut out = Vec::new();
        // 焦点 None → Shift+Tab → 链尾 C(3)
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: MOD_SHIFT, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(3)), "Shift+Tab 从 None → 链尾 C");
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: MOD_SHIFT, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(1)), "Shift+Tab → A");
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: MOD_SHIFT, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(2)), "Shift+Tab → B");
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: MOD_SHIFT, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, Some(NodeId(3)), "链首 Shift+Tab → wrap 回链尾 C");
    }

    #[test]
    fn tab_empty_chain_no_op() {
        // 无可聚焦节点 → Tab 无操作（不发 keydown，不改焦点）
        let mut s = one_button_scene();   // btn 无 tabindex
        let mut out = Vec::new();
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: 0, is_down: true, pad: [0, 0] }], &mut out);
        assert_eq!(s.focused_node, None, "无可聚焦 → Tab 不改焦点");
        assert!(out.is_empty(), "空链 Tab → 无事件");
    }
```

- [ ] **Step 7: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib input::tests::tab_chain_orders_by_tabindex_then_dfs`
Expected: FAIL（`build_tab_chain`/`process_keys` 未定义，编译失败）。

- [ ] **Step 8: 加 build_tab_chain + dfs_collect + next_focus**

input.rs 模块级（focus_node 旁）加：

```rust
/// v1d.2：DFS 先序收集 tabindex>=0 且非 disabled 节点，分桶 positive(>0)/zero(==0)。
fn dfs_collect(scene: &Scene, id: NodeId, positive: &mut Vec<(i32, NodeId)>, zero: &mut Vec<NodeId>) {
    if id.0 >= scene.nodes.len() {
        return;
    }
    let n = &scene.nodes[id.0];
    if !n.disabled {
        match n.tabindex {
            Some(t) if t > 0 => positive.push((t, id)),
            Some(0) => zero.push(id),
            _ => {} // Some(-1)/None 不进链
        }
    }
    // 先序：先本节点入桶，再递归 children（DOM 序）
    let children: Vec<NodeId> = n.children.clone();
    for c in children {
        dfs_collect(scene, c, positive, zero);
    }
}

/// v1d.2：构造 Tab 链——正整数按 tabindex 升序（stable，同值保 DFS 序），后接 0 组（DFS 序）。
/// 照 DOM：正整数显式序先于 0 组。tabindex=-1/None/disabled 不进。
pub(crate) fn build_tab_chain(scene: &Scene) -> Vec<NodeId> {
    let mut positive: Vec<(i32, NodeId)> = Vec::new();
    let mut zero: Vec<NodeId> = Vec::new();
    for root in &scene.roots {
        dfs_collect(scene, *root, &mut positive, &mut zero);
    }
    positive.sort_by_key(|(t, _)| *t); // stable：同 tabindex 保 DFS 序
    positive.into_iter().map(|(_, n)| n).chain(zero.into_iter()).collect()
}

/// v1d.2：从 current 焦点算 Tab/Shift+Tab 下一个焦点。空链 → None。
/// current 在链中 → 取前/后；不在（或 None）→ 链首(forward)/链尾(backward)；边界 wrap。
fn next_focus(chain: &[NodeId], current: Option<NodeId>, backward: bool) -> Option<NodeId> {
    if chain.is_empty() {
        return None;
    }
    let idx = current.and_then(|c| chain.iter().position(|n| *n == c));
    let next = match idx {
        Some(i) => {
            let len = chain.len();
            // forward: (i+1)%len；backward: (i-1+len)%len
            let ni = if backward {
                (i + len - 1) % len
            } else {
                (i + 1) % len
            };
            chain[ni]
        }
        None => {
            // current 不在链 → forward 取链首，backward 取链尾
            if backward { *chain.last().unwrap() } else { chain[0] }
        }
    };
    Some(next)
}
```

- [ ] **Step 9: 加 process_keys + keydown/up**

input.rs 模块级加（focus_node/build_tab_chain 后）：

```rust
/// v1d.2：处理键盘事件——keydown/up（有焦点才发）+ Tab/Shift+Tab 导航（focus_node）。
/// Stage tick 在 pointer process 后调。Tab 被导航消费（不发 keydown，照 DOM Tab 默认动作=移焦）。
pub(crate) fn process_keys(scene: &mut Scene, keys: &[KeyEvent], out: &mut Vec<EventRecord>) {
    for ke in keys {
        let focused = scene.focused_node;
        if ke.is_down && ke.key_code == KEY_TAB {
            // Tab 导航
            let chain = build_tab_chain(scene);
            if chain.is_empty() {
                continue; // 无可聚焦节点 → Tab 无操作（不发 keydown）
            }
            let backward = (ke.modifiers & MOD_SHIFT) != 0;
            let next = next_focus(&chain, focused, backward);
            focus_node(scene, next, out); // 发 FocusOut(旧)+FocusIn(新)
            continue; // Tab 被消费，不发 keydown
        }
        // 普通 keydown/up：有焦点才发（无焦点丢弃，spec §1）
        if let Some(n) = focused {
            let event_type = if ke.is_down { EVT_KEY_DOWN } else { EVT_KEY_UP };
            out.push(EventRecord {
                node_id: n.0 as u32,
                event_type,
                click_count: 0,
                pad: [ke.modifiers, 0],   // pad[0]=modifiers
                touch_id: ke.key_code as i32, // touch_id 复用装 key_code（u32 bit pattern → i32）
                x: 0.0,
                y: 0.0,
            });
        }
    }
}
```

- [ ] **Step 10: 跑 Tab 链测**

Run: `cargo test -p loomgui_core --lib input::tests::tab_`
Expected: PASS（新 4 测）。

- [ ] **Step 11: 写失败测——keydown/up + Tab 不发 keydown + click-to-focus**

input.rs tests mod 加：

```rust
    #[test]
    fn keydown_emitted_to_focused_node() {
        let mut s = two_focusable_scene();
        let mut out = Vec::new();
        focus_node(&mut s, Some(NodeId(1)), &mut out);   // 聚焦 A
        out.clear();
        // Enter keydown（KeyCode.Return=13，core 不解释，只透传）
        process_keys(&mut s, &[KeyEvent { key_code: 13, modifiers: MOD_CTRL, is_down: true, pad: [0, 0] }], &mut out);
        let kd = out.iter().find(|e| e.event_type == EVT_KEY_DOWN).expect("keydown");
        assert_eq!(kd.node_id, 1, "keydown@焦点 A");
        assert_eq!(kd.touch_id, 13, "key_code 复用 touch_id");
        assert_eq!(kd.pad[0], MOD_CTRL, "modifiers 复用 pad[0]");
    }

    #[test]
    fn keydown_no_focus_dropped() {
        let mut s = two_focusable_scene();
        let mut out = Vec::new();
        // 无焦点 + keydown → 丢弃
        process_keys(&mut s, &[KeyEvent { key_code: 13, modifiers: 0, is_down: true, pad: [0, 0] }], &mut out);
        assert!(out.iter().all(|e| e.event_type != EVT_KEY_DOWN), "无焦点 keydown 丢弃");
    }

    #[test]
    fn tab_consumed_no_keydown() {
        let mut s = two_focusable_scene();
        let mut out = Vec::new();
        process_keys(&mut s, &[KeyEvent { key_code: KEY_TAB, modifiers: 0, is_down: true, pad: [0, 0] }], &mut out);
        assert!(out.iter().all(|e| e.event_type != EVT_KEY_DOWN), "Tab 被导航消费，不发 keydown");
        assert!(out.iter().any(|e| e.event_type == EVT_FOCUS_IN), "Tab → FocusIn");
    }

    #[test]
    fn click_to_focus_focusable_node() {
        // pointer-down 命中 tabindex=0 节点 → FocusIn@该节点
        let mut s = two_focusable_scene();   // A(1)@0,0,50,50 tabindex=0
        let mut ps = PointerState::new();
        let out = ps.process(&mut s, &[PointerEvent { kind: PointerKind::Down, x: 25.0, y: 25.0, button: 0, pad: [0, 0], touch_id: -1 }]);
        assert!(out.iter().any(|e| e.event_type == EVT_FOCUS_IN && e.node_id == 1), "down@A(tabindex=0) → FocusIn@A");
        assert_eq!(s.focused_node, Some(NodeId(1)));
    }

    #[test]
    fn click_non_focusable_no_blur() {
        // 焦点 A，pointer-down 不可聚焦节点（btn 无 tabindex）→ 不夺焦（不发 FocusOut）
        let mut s = one_button_scene();   // btn(1) 无 tabindex，root(0) 无 tabindex
        let mut ps = PointerState::new();
        // 先聚焦 root（编程模拟）——root 无 tabindex，但 focus_node 可强制（测 click-to-focus 不夺焦）
        let mut tmp = Vec::new();
        focus_node(&mut s, Some(NodeId(0)), &mut tmp);
        // down@btn(1)（不可聚焦）→ 不应 FocusOut root
        let out = ps.process(&mut s, &[PointerEvent { kind: PointerKind::Down, x: 50.0, y: 50.0, button: 0, pad: [0, 0], touch_id: -1 }]);
        assert!(out.iter().all(|e| e.event_type != EVT_FOCUS_OUT), "down 不可聚焦节点 → 不夺焦（无 FocusOut）");
        assert_eq!(s.focused_node, Some(NodeId(0)), "焦点保持 root");
    }

    #[test]
    fn click_disabled_focusable_no_focus() {
        // disabled 可聚焦节点 → pointer-down 不聚焦
        let mut s = two_focusable_scene();
        s.nodes[1].disabled = true;   // A disabled（tabindex=0）
        let mut ps = PointerState::new();
        let out = ps.process(&mut s, &[PointerEvent { kind: PointerKind::Down, x: 25.0, y: 25.0, button: 0, pad: [0, 0], touch_id: -1 }]);
        assert!(out.iter().all(|e| e.event_type != EVT_FOCUS_IN), "disabled 可聚焦 → down 不聚焦");
        assert_eq!(s.focused_node, None);
    }
```

- [ ] **Step 12: 跑测验证失败**

Run: `cargo test -p loomgui_core --lib input::tests::click_to_focus_focusable_node`
Expected: FAIL（Down arm 无 click-to-focus 逻辑，不发 FocusIn）。

- [ ] **Step 13: 改 PointerKind::Down arm 加 click-to-focus**

input.rs `process` 的 `PointerKind::Down` arm（既有 drag_target 计算后、`if let Some(n) = hit` 发 Down 事件前）加 click-to-focus。注意借用：`slot.down_targets` 是 Vec<NodeId>（Copy），find 用 `.copied()` 拿 owned NodeId，不持 scene 借用，故可之后调 `focus_node(scene, ...)`：

```rust
                    slot.drag_target = slot.down_targets.iter()
                        .find(|&&n| n.0 < scene.nodes.len() && scene.nodes[n.0].draggable && !scene.nodes[n.0].disabled)
                        .copied();
                    slot.drag_testing = slot.drag_target.is_some();
                    slot.dragging = false;
                    // v1d.2：click-to-focus——pointer-down 命中 tabindex>=0 节点 → 聚焦（照 fgui+DOM）。
                    // 沿 down_targets（leaf 优先，同 drag_target 模式）找最近可聚焦非 disabled 节点。
                    // 不可聚焦/`-1` → 不夺焦（照 DOM：点空白不 blur）。
                    let focus_target = slot.down_targets.iter()
                        .find(|&&n| n.0 < scene.nodes.len()
                            && !scene.nodes[n.0].disabled
                            && matches!(scene.nodes[n.0].tabindex, Some(t) if t >= 0))
                        .copied();
                    if let Some(t) = focus_target {
                        focus_node(scene, Some(t), &mut out);
                    }
                    if let Some(n) = hit {
```

> **借用核对**：`slot.down_targets.iter().find(...).copied()` 返回 `Option<NodeId>`（owned），借用 `slot` 在 find 结束即释放。之后 `focus_node(scene, ...)` 借 `&mut scene`——`slot` 是 `self.slots[slot_idx]`（`&mut self` 内），`scene` 是另一参数（`&mut Scene`）。`slot` 与 `scene` 是不同对象，无别名。但 `slot` 借自 `self`，`focus_node` 不借 `self`——OK。`out` 是 `process` 的局部 `&mut Vec`。✅ 编译应过。

- [ ] **Step 14: 跑 input 全测**

Run: `cargo test -p loomgui_core --lib input`
Expected: PASS（新 ~11 测 + 既有 v1c/v1d.1 全过）。

> **注**：`cargo test -p loomgui_core` 全局此时 stage.rs 直接构造 Scene 字面量 + Stage 还没接 process_keys（T5 改）。stage.rs 测可能失败（缺 focused_node）。本 task 验 input 模块测过。

- [ ] **Step 15: Commit**

```bash
git add loomgui_core/src/input.rs
git commit -m "feat(v1d.2-T4): core 键盘+焦点检测 — KeyEvent/Tab链/click-to-focus/process_keys/focus_node"
```

---

### Task 5: Stage set_key_input/request_focus/blur + tick 管线 + FFI 3 函数 + version v1d.2 + abi 测

**Files:**
- Modify: `loomgui_core/src/stage.rs`（Stage 结构字段、set_key_input/request_focus/blur、tick_and_render 管线、既有 Scene 字面量补 focused_node）
- Modify: `loomgui_ffi_c/src/lib.rs`（version v1d.2、set_key_input/request_focus/focused_node FFI、既有 abi_tests 手搓 entries 6→7-tuple、version 测、新 abi 测）

**Interfaces:**
- Consumes: T4 的 `process_keys`/`focus_node`/`KeyEvent`/`KEY_TAB`。
- Produces: `Stage::set_key_input`/`request_focus`/`blur`、FFI `loomgui_stage_set_key_input`/`loomgui_stage_request_focus`/`loomgui_stage_focused_node`、version "v1d.2"。T6（C#）依赖 FFI 函数。

- [ ] **Step 1: 改 stage.rs 既有 Scene 字面量补 focused_node**

stage.rs tests mod 内直接构造 `Scene { roots, nodes, dynamic_rules }` 的字面量（`is_pointer_on_ui_false_when_miss` 等）补 `focused_node: None`。

- [ ] **Step 2: 改 Stage 结构 + new 加字段**

stage.rs Stage 结构（`last_events` 字段后）加：

```rust
    /// v1c.1：本帧 tick 产出的事件序列（process 返回）；last_events/borrow_events 读。
    pub last_events: Vec<EventRecord>,
    /// v1d.2：set_key_input 缓存的本帧键盘输入；tick 消费后 clear。
    pub pending_keys: Vec<crate::input::KeyEvent>,
    /// v1d.2：编程聚焦/清焦点请求（request_focus/blur tick 外调记，tick 最前消费）。
    /// 外层 Some=有请求；内层 Some(id)=聚焦某节点 / None=清焦点。
    pub pending_focus_request: Option<Option<NodeId>>,
}
```

`Stage::new`（`last_events: Vec::new(),` 后）补：

```rust
            last_events: Vec::new(),
            pending_keys: Vec::new(),
            pending_focus_request: None,
        })
    }
```

- [ ] **Step 3: 加 set_key_input/request_focus/blur 方法**

stage.rs impl Stage 加（`cancel_click` 旁）：

```rust
    /// v1d.2：缓存本帧键盘输入（tick 前调；覆盖式）。
    pub fn set_key_input(&mut self, keys: &[crate::input::KeyEvent]) {
        self.pending_keys.clear();
        self.pending_keys.extend_from_slice(keys);
    }

    /// v1d.2：编程聚焦（照 fgui RequestFocus）。强制聚焦任意非 disabled 节点
    /// （含 tabindex=None/-1——request_focus 是编程 API，不查 tabindex）。
    /// disabled 拒 / 越界跳过。记 pending_focus_request，下 tick 最前消费（不直接写 last_events）。
    pub fn request_focus(&mut self, node_id: NodeId) {
        if let Some(scene) = self.scene.as_ref() {
            if node_id.0 >= scene.nodes.len() {
                return;
            }
            if scene.nodes[node_id.0].disabled {
                return; // §3.5 disabled 拒
            }
        } else {
            return;
        }
        self.pending_focus_request = Some(Some(node_id));
    }

    /// v1d.2：编程清焦点。记 pending_focus_request = Some(None)，下 tick 消费。
    pub fn blur(&mut self) {
        self.pending_focus_request = Some(None);
    }
```

- [ ] **Step 4: 改 tick_and_render 管线**

stage.rs `tick_and_render` 改（加 pending_focus_request 消费最前 + process_keys）：

```rust
    pub fn tick_and_render(&mut self) -> FrameData {
        let scene = self.scene.as_mut().expect("load first");
        let mut out: Vec<EventRecord> = Vec::new();
        // v1d.2：消费 pending_focus_request（编程聚焦/清焦点，tick 外 request_focus/blur 记）
        if let Some(req) = self.pending_focus_request.take() {
            crate::input::focus_node(scene, req, &mut out);
        }
        // 1. solve（先解 layout_rect，hit_test 要用）
        solve(scene, &self.font, self.root_size, &self.textures);
        let input = std::mem::take(&mut self.pending_input);
        let mut ptr_out = self.pointer_state.process(scene, &input);
        out.append(&mut ptr_out);
        // v1d.2：键盘事件（keydown/up + Tab 导航 + FocusIn/Out）
        let keys = std::mem::take(&mut self.pending_keys);
        crate::input::process_keys(scene, &keys, &mut out);
        self.last_events = out;
        // 4. 伪类重匹配（按新 hover/active/focused 改 Node.style）
        rematch_pseudo_classes(scene);
        // 5. 渲染
        build_render_nodes(scene, &self.font, &self.textures)
    }
```

> `focus_node`/`process_keys` 是 input.rs 模块级函数——需 `pub(crate)`。T4 Step 4/9 已加 `pub(crate)`。stage.rs 用 `crate::input::focus_node`/`crate::input::process_keys` 全路径调。

- [ ] **Step 5: 跑 core 全测**

Run: `cargo test -p loomgui_core`
Expected: PASS（core 全绿——T1-T5 core 部分齐了）。若 stage.rs 既有测因管线变化失败，修测（如 set_input_hover 测读 last_events 顺序）。

- [ ] **Step 6: 改 FFI version v1d.2**

`loomgui_ffi_c/src/lib.rs` `loomgui_version`：

```rust
        .get_or_init(|| CString::new("v1d.2").unwrap())
```

version 测改（`version_returns_c_string_v1d1` → v1d.2）：

```rust
    #[test]
    fn version_returns_c_string_v1d2() {
        unsafe {
            let s = CStr::from_ptr(loomgui_version() as *const i8);
            assert_eq!(s.to_str().unwrap(), "v1d.2");
        }
    }
```

`version_is_v1d_1` 测改 `version_is_v1d_2`：

```rust
    /// v1d.2：version 字符串 == "v1d.2"。
    #[test]
    fn version_is_v1d_2() {
        let p = loomgui_version();
        let len = (0..).take_while(|&i| unsafe { *p.add(i) != 0 }).count();
        let s = std::str::from_utf8(unsafe { std::slice::from_raw_parts(p, len) }).unwrap();
        assert_eq!(s, "v1d.2");
    }
```

- [ ] **Step 7: 改 FFI 既有 abi_tests 手搓 entries 6→7-tuple**

`lib.rs` abi_tests mod 内所有手搓 entries（`load_package_builds_blob_from_package`/`atlas_count_and_info_round_trip`/`is_pointer_on_ui_true_on_hit_false_on_miss`/`node_parent_returns_chain_and_sentinel`/`find_node_by_id_round_trip`）补末位 tabindex（6→7-tuple，每条 `, None`）。

- [ ] **Step 8: 加 FFI 3 函数**

`lib.rs`（`loomgui_stage_set_input` 旁，或 `cancel_click` 后）加：

```rust
/// v1d.2：注入本帧键盘事件（扁平 KeyEvent 数组）。tick 前调。null/len=0 = 无键盘输入。
///
/// **常驻（不 gate）：**输入是 runtime 稳定入口。
#[no_mangle]
pub extern "C" fn loomgui_stage_set_key_input(h: *mut StageHandle, keys: *const loomgui_core::input::KeyEvent, len: usize) {
    if h.is_null() { return; }
    let sh = unsafe { &mut *h };
    if keys.is_null() || len == 0 {
        sh.stage.set_key_input(&[]);
        return;
    }
    let ks = unsafe { std::slice::from_raw_parts(keys, len) };
    sh.stage.set_key_input(ks);
}

/// v1d.2：编程聚焦节点（照 fgui RequestFocus）。强制聚焦任意非 disabled 节点
/// （含 tabindex=None/-1）；disabled 拒；越界跳过。null 句柄 → no-op。
///
/// **常驻（不 gate）。**
#[no_mangle]
pub extern "C" fn loomgui_stage_request_focus(h: *mut StageHandle, node_id: u32) {
    if h.is_null() { return; }
    let sh = unsafe { &mut *h };
    sh.stage.request_focus(NodeId(node_id as usize));
}

/// v1d.2：读当前焦点节点。无焦点/无 scene → 0xFFFF_FFFF（sentinel，同 node_parent）。null 句柄 → sentinel。
///
/// **常驻（不 gate）。**
#[no_mangle]
pub extern "C" fn loomgui_stage_focused_node(h: *const StageHandle) -> u32 {
    const NONE: u32 = 0xFFFF_FFFF;
    if h.is_null() { return NONE; }
    let sh = unsafe { &*h };
    match &sh.stage.scene {
        Some(scene) => scene.focused_node.map(|n| n.0 as u32).unwrap_or(NONE),
        None => NONE,
    }
}
```

`lib.rs` 顶部 `use` 加 `KeyEvent`：

```rust
use loomgui_core::input::{EventRecord, KeyEvent, PointerEvent};
```

- [ ] **Step 9: 加 FFI abi 新测**

`lib.rs` abi_tests mod 末尾加：

```rust
    /// v1d.2：KeyEvent sizeof 8B + EventRecord 仍 20B / PointerEvent 16B。
    #[test]
    fn key_event_sizeof_and_unchanged() {
        use loomgui_core::input::{EventRecord, KeyEvent, PointerEvent};
        use std::mem::size_of;
        assert_eq!(size_of::<KeyEvent>(), 8, "KeyEvent 8B");
        assert_eq!(size_of::<EventRecord>(), 20, "EventRecord 20B 不变");
        assert_eq!(size_of::<PointerEvent>(), 16, "PointerEvent 16B 不变");
    }

    /// v1d.2：EVT 常量值锁（12/13/14/15）。
    #[test]
    fn evt_constants_v1d2() {
        assert_eq!(loomgui_core::input::EVT_KEY_DOWN, 12);
        assert_eq!(loomgui_core::input::EVT_KEY_UP, 13);
        assert_eq!(loomgui_core::input::EVT_FOCUS_IN, 14);
        assert_eq!(loomgui_core::input::EVT_FOCUS_OUT, 15);
    }

    /// v1d.2：key 事件 round-trip——set_key_input(Enter down) + 聚焦 + tick → borrow_events 含 KeyDown@焦点。
    #[cfg(feature = "parse")]
    #[test]
    fn key_event_round_trip() {
        use loomgui_core::input::{KeyEvent, EVT_KEY_DOWN};
        let (fp, fplen) = font_path();
        let h = loomgui_stage_new(fp.as_ptr() as *const u8, fplen, 200.0, 100.0);
        assert!(!h.is_null());
        // btn tabindex=0 可聚焦
        let html = b"<button class=\"btn\" tabindex=\"0\">OK</button>";
        let css = b".btn{width:100px;height:50px;}";
        loomgui_stage_load_html(h, html.as_ptr() as *const u8, html.len(), css.as_ptr() as *const u8, css.len());
        // 先编程聚焦 btn（node 0=root? 不——parse 产 root + btn + Text 子。找 btn：用 find_node_by_id 更稳）
        // 用 click-to-focus：Down@btn → tick → 焦点 btn
        use loomgui_core::input::{PointerEvent, PointerKind};
        loomgui_stage_set_input(h, [PointerEvent { kind: PointerKind::Down, x: 50.0, y: 25.0, button: 0, pad: [0, 0], touch_id: -1 }].as_ptr(), 1);
        loomgui_stage_tick(h, 0.0);
        // 现在焦点应 btn（node 1）。再 Enter keydown + tick
        loomgui_stage_set_key_input(h, [KeyEvent { key_code: 13, modifiers: 0, is_down: true, pad: [0, 0] }].as_ptr(), 1);
        loomgui_stage_tick(h, 0.0);
        let mut len = 0usize;
        let p = loomgui_stage_borrow_events(h, &mut len);
        let recs = unsafe { std::slice::from_raw_parts(p as *const loomgui_core::input::EventRecord, len) };
        assert!(recs.iter().any(|e| e.event_type == EVT_KEY_DOWN), "聚焦 btn + Enter down → KeyDown@btn");
        loomgui_stage_free(h);
    }

    /// v1d.2：Tab 导航 round-trip——两可聚焦 btn + Tab → borrow_events 含 FocusIn（无 KeyDown）。
    #[cfg(feature = "parse")]
    #[test]
    fn tab_navigation_round_trip() {
        use loomgui_core::input::{KeyEvent, EVT_FOCUS_IN, EVT_KEY_DOWN, KEY_TAB};
        let (fp, fplen) = font_path();
        let h = loomgui_stage_new(fp.as_ptr() as *const u8, fplen, 200.0, 100.0);
        let html = b"<button class=\"a\" tabindex=\"0\">A</button><button class=\"b\" tabindex=\"0\">B</button>";
        let css = b"button{width:50px;height:30px;}";
        loomgui_stage_load_html(h, html.as_ptr() as *const u8, html.len(), css.as_ptr() as *const u8, css.len());
        // Tab → 焦点首个可聚焦（A，node 1）
        loomgui_stage_set_key_input(h, [KeyEvent { key_code: KEY_TAB, modifiers: 0, is_down: true, pad: [0, 0] }].as_ptr(), 1);
        loomgui_stage_tick(h, 0.0);
        let mut len = 0usize;
        let p = loomgui_stage_borrow_events(h, &mut len);
        let recs = unsafe { std::slice::from_raw_parts(p as *const loomgui_core::input::EventRecord, len) };
        assert!(recs.iter().any(|e| e.event_type == EVT_FOCUS_IN), "Tab → FocusIn");
        assert!(recs.iter().all(|e| e.event_type != EVT_KEY_DOWN), "Tab 被消费，无 KeyDown");
        // focused_node 读首个可聚焦（A=node 1，parse 序：root(0), button.a(1), Text, button.b, Text）
        // node 1 = button.a
        assert_eq!(loomgui_stage_focused_node(h), 1, "Tab → 焦点 button.a(node 1)");
        loomgui_stage_free(h);
    }

    /// v1d.2：request_focus + focused_node round-trip。
    #[cfg(feature = "parse")]
    #[test]
    fn request_focus_round_trip() {
        let (fp, fplen) = font_path();
        let h = loomgui_stage_new(fp.as_ptr() as *const u8, fplen, 200.0, 100.0);
        let html = b"<button id=\"ok\" tabindex=\"0\">OK</button>";
        let css = b"button{width:50px;height:30px;}";
        loomgui_stage_load_html(h, html.as_ptr() as *const u8, html.len(), css.as_ptr() as *const u8, css.len());
        let id = std::ffi::CString::new("ok").unwrap();
        let ok_node = loomgui_stage_find_node_by_id(h, id.as_ptr() as *const u8, id.as_bytes().len());
        assert_ne!(ok_node, 0xFFFF_FFFF, "find ok");
        loomgui_stage_request_focus(h, ok_node);
        assert_eq!(loomgui_stage_focused_node(h), 0xFFFF_FFFF, "request_focus 后未 tick → focused_node 仍 sentinel");
        loomgui_stage_tick(h, 0.0);
        assert_eq!(loomgui_stage_focused_node(h), ok_node, "tick 后焦点 = ok");
        loomgui_stage_free(h);
    }
```

> **注**：`request_focus_round_trip` 验 R3 修正——request_focus 记 pending_focus_request，未 tick 时 focused_node 仍 sentinel（pending 未消费）；tick 后消费生效。

- [ ] **Step 10: 跑 ffi 全测**

Run: `cargo test -p loomgui_ffi_c`
Expected: PASS（含新 5 测 + 既有全过）。

- [ ] **Step 11: 重编 .dll + 提交**

```bash
cargo build -p loomgui_ffi_c --release && cp target/release/loomgui_ffi_c.dll loomgui_unity/Assets/Plugins/LoomGUI/
git add loomgui_core/src/stage.rs loomgui_ffi_c/src/lib.rs loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
git commit -m "feat(v1d.2-T5): Stage 键盘管线 + FFI set_key_input/request_focus/focused_node + v1d.2 + .dll"
```

> **.dll 须重编提交**（两机串行：家里机拉新 .dll 才能测）。新 FFI 函数（set_key_input/request_focus/focused_node）+ 新 `KeyEvent` struct → csbindgen Unity reimport 时自动扫 `lib.rs` regen `LoomGUIBindings.cs`（生成 `KeyEvent` struct + 3 函数签名）。家里机开 Unity 触发 reimport。

---

### Task 6: C# EventType +4 + DispatchPending + LoomEvent modifiers + EventContext KeyCode/Modifiers + LoomInputCollector 键盘采集

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomEventHandler.cs`（EventType、LoomEvent、EventContext、DispatchPending）
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomInputCollector.cs`（键盘采集 CollectKeys）
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs`（LateUpdate 调 CollectKeys——确认既有 Collect 调用点旁加）

> **csbindgen 依赖**：T5 加的 `KeyEvent` struct + 3 FFI 函数由 csbindgen 在 Unity reimport 时生成进 `LoomGUIBindings.cs`（`Bindings.KeyEvent { key_code, modifiers, is_down, pad0, pad1 }` + `loomgui_stage_set_key_input`/`request_focus`/`focused_node`）。本 task C# 用这些生成符号。**家里机首次开 Unity reimport 后才有**——本机静态写代码不编译（无 Unity），家里机验。

**Interfaces:**
- Consumes: T5 的 FFI `set_key_input`/`request_focus`/`focused_node` + csbindgen 生成的 `Bindings.KeyEvent`。
- Produces: C# `EventType.KeyDown/KeyUp/FocusIn/FocusOut`、`LoomEvent.modifiers`、`EventContext.KeyCode/Modifiers`、键盘采集。

- [ ] **Step 1: EventType +4**

`LoomEventHandler.cs` EventType enum（`LongPress = 9,` 后）加：

```csharp
        LongPress = 9,
        // v1d.2：键盘 + 焦点（core 检测，C# 路由）。
        KeyDown = 12,
        KeyUp = 13,
        FocusIn = 14,
        FocusOut = 15,
    }
```

- [ ] **Step 2: LoomEvent 加 modifiers 字段（读 Rust pad[0]）**

`LoomEventHandler.cs` LoomEvent struct（clickCount 后）加 `modifiers`。对齐 Rust EventRecord `pad[0]@6`（click_count@5, pad[0]@6, pad[1]@7, touch_id@8）：

```csharp
    [StructLayout(LayoutKind.Sequential)]
    public struct LoomEvent
    {
        public uint nodeId;
        public EventType type;
        public byte clickCount;   // Rust EventRecord @5
        public byte modifiers;    // v1d.2：Rust EventRecord pad[0] @6（key 事件 modifiers；其余=0）
        // @7 隐式 padding 对齐 touch_id @8
        public int touch_id;      // Rust EventRecord @8（key 事件复用装 key_code）
        public float x;
        public float y;
    }
```

> **对齐核对**：nodeId@0(4) + type@4(1) + clickCount@5(1) + modifiers@6(1) + [pad@7] + touch_id@8(4) + x@12 + y@16 = 20B。与 Rust 一致。既有 PointerEvent/Click 事件 modifiers=0，无影响。

- [ ] **Step 3: EventContext 加 KeyCode/Modifiers**

`LoomEventHandler.cs` EventContext（clickCount 旁）加字段 + 填充。先加字段：

```csharp
        public byte clickCount;
        public uint keyCode;          // v1d.2：key 事件的 KeyCode（EventRecord.touch_id 复用）
        public byte modifiers;         // v1d.2：key 事件 modifiers（EventRecord.pad[0]）
        public bool isDoubleClick => clickCount > 1;
```

`BubbleRoute`/`DirectDispatch` 内 ctx 填充处加（既有 `ctx.clickCount = evt.clickCount;` 旁）：

```csharp
            ctx.clickCount = evt.clickCount; ctx.keyCode = (uint)evt.touch_id; ctx.modifiers = evt.modifiers; ctx.x = evt.x; ctx.y = evt.y;
```

两处（BubbleRoute line ~180 + DirectDispatch line ~206）都改。

- [ ] **Step 4: DispatchPending switch +4 case**

`LoomEventHandler.cs` DispatchPending switch（`case EventType.LongPress:` 那组后）加 keydown/up/focus 走 BubbleRoute：

```csharp
                    // v1d.1：drag + longpress 走 BubbleRoute
                    case EventType.DragStart:
                    case EventType.DragMove:
                    case EventType.DragEnd:
                    case EventType.LongPress:
                        BubbleRoute(evt); break;
                    // v1d.2：keydown/up + focus/blur 走 BubbleRoute（core 算好 node_id，bubble 让祖先接）
                    case EventType.KeyDown:
                    case EventType.KeyUp:
                    case EventType.FocusIn:
                    case EventType.FocusOut:
                        BubbleRoute(evt); break;
                    case EventType.Move:
```

- [ ] **Step 5: LoomInputCollector 加键盘采集**

`LoomInputCollector.cs` 加 `CollectKeys` 方法（既有 `Collect` 旁）。兼容新旧输入系统：

```csharp
        /// v1d.2：采集本帧键盘 → set_key_input。KeyDown/Up 事件 + modifiers。
        /// 兼容新旧输入系统（同 Collect）。本帧无键事件 → set_key_input(null,0)（core 无键盘输入）。
        public void CollectKeys(System.IntPtr stage)
        {
            if (stage == System.IntPtr.Zero) return;
            var keys = new System.Collections.Generic.List<Bindings.KeyEvent>();
            byte mods = CurrentModifiers();
#if ENABLE_INPUT_SYSTEM
            var kb = UnityEngine.InputSystem.Keyboard.current;
            if (kb != null)
            {
                // 遍历常见键——csbindgen KeyEvent.key_code = (uint)UnityEngine.KeyCode。
                // ponytail: 全键遍历按 wasPressedThisFrame/wasReleasedThisFrame 采 down/up。
                foreach (UnityEngine.KeyCode kc in KeyList)
                {
                    bool down = kb[kc].wasPressedThisFrame;
                    bool up = kb[kc].wasReleasedThisFrame;
                    if (down || up)
                        keys.Add(new Bindings.KeyEvent { key_code = (uint)kc, modifiers = mods, is_down = down, pad0 = 0, pad1 = 0 });
                }
            }
#else
            foreach (UnityEngine.KeyCode kc in KeyList)
            {
                bool down = UnityEngine.Input.GetKeyDown(kc);
                bool up = UnityEngine.Input.GetKeyUp(kc);
                if (down || up)
                    keys.Add(new Bindings.KeyEvent { key_code = (uint)kc, modifiers = mods, is_down = down, pad0 = 0, pad1 = 0 });
            }
#endif
            if (keys.Count == 0)
            {
                Native.loomgui_stage_set_key_input((Bindings.StageHandle*)stage, null, 0);
                return;
            }
            var arr = keys.ToArray();
            fixed (Bindings.KeyEvent* p = arr)
            {
                Native.loomgui_stage_set_key_input((Bindings.StageHandle*)stage, p, (nuint)arr.Length);
            }
        }

        /// 当前 modifiers 位掩码（bit0=shift/bit1=ctrl/bit2=alt）。core MOD_SHIFT/CTRL/ALT 同值。
        static byte CurrentModifiers()
        {
            byte m = 0;
#if ENABLE_INPUT_SYSTEM
            var kb = UnityEngine.InputSystem.Keyboard.current;
            if (kb == null) return 0;
            if (kb.leftShiftKey.isPressed || kb.rightShiftKey.isPressed) m |= 0x01;
            if (kb.leftCtrlKey.isPressed || kb.rightCtrlKey.isPressed) m |= 0x02;
            if (kb.leftAltKey.isPressed || kb.rightAltKey.isPressed) m |= 0x04;
#else
            if (UnityEngine.Input.GetKey(UnityEngine.KeyCode.LeftShift) || UnityEngine.Input.GetKey(UnityEngine.KeyCode.RightShift)) m |= 0x01;
            if (UnityEngine.Input.GetKey(UnityEngine.KeyCode.LeftControl) || UnityEngine.Input.GetKey(UnityEngine.KeyCode.RightControl)) m |= 0x02;
            if (UnityEngine.Input.GetKey(UnityEngine.KeyCode.LeftAlt) || UnityEngine.Input.GetKey(UnityEngine.KeyCode.RightAlt)) m |= 0x04;
#endif
            return m;
        }

        /// 采集的键白名单（Tab + 字母 + Enter/Space/Esc/方向 + 数字）。避免全 KeyCode 枚举遍历（数百个）开销。
        /// ponytail: 显式白名单而非全枚举——绝大多数键业务不关心，白名单够用且省 CPU。
        static readonly UnityEngine.KeyCode[] KeyList = {
            UnityEngine.KeyCode.Tab,
            UnityEngine.KeyCode.Return, UnityEngine.KeyCode.Space, UnityEngine.KeyCode.Escape,
            UnityEngine.KeyCode.LeftArrow, UnityEngine.KeyCode.RightArrow, UnityEngine.KeyCode.UpArrow, UnityEngine.KeyCode.DownArrow,
            UnityEngine.KeyCode.A, UnityEngine.KeyCode.B, UnityEngine.KeyCode.C, UnityEngine.KeyCode.D, UnityEngine.KeyCode.E,
            UnityEngine.KeyCode.F, UnityEngine.KeyCode.G, UnityEngine.KeyCode.H, UnityEngine.KeyCode.I, UnityEngine.KeyCode.J,
            UnityEngine.KeyCode.K, UnityEngine.KeyCode.L, UnityEngine.KeyCode.M, UnityEngine.KeyCode.N, UnityEngine.KeyCode.O,
            UnityEngine.KeyCode.P, UnityEngine.KeyCode.Q, UnityEngine.KeyCode.R, UnityEngine.KeyCode.S, UnityEngine.KeyCode.T,
            UnityEngine.KeyCode.U, UnityEngine.KeyCode.V, UnityEngine.KeyCode.W, UnityEngine.KeyCode.X, UnityEngine.KeyCode.Y, UnityEngine.KeyCode.Z,
            UnityEngine.KeyCode.Alpha0, UnityEngine.KeyCode.Alpha1, UnityEngine.KeyCode.Alpha2, UnityEngine.KeyCode.Alpha3, UnityEngine.KeyCode.Alpha4,
            UnityEngine.KeyCode.Alpha5, UnityEngine.KeyCode.Alpha6, UnityEngine.KeyCode.Alpha7, UnityEngine.KeyCode.Alpha8, UnityEngine.KeyCode.Alpha9,
        };
```

> **KeyCode.Tab 值**：Unity `KeyCode.Tab` 枚举值。core `KEY_TAB=9`。确认：Unity KeyCode.Tab 实际值。读 Unity 文档/源码——`KeyCode.Tab = 9`。C# 传 `(uint)KeyCode.Tab` = 9，core KEY_TAB=9 匹配。✅（T4 已注）

- [ ] **Step 6: LoomStage LateUpdate 调 CollectKeys**

读 `LoomStage.cs` 找既有 `Collect` 调用点（`_collector.Collect((IntPtr)_stage, ...)` 旁），加 `CollectKeys`：

```csharp
            _collector.Collect((System.IntPtr)_stage, rootSize, _safeArea);
            _collector.CollectKeys((System.IntPtr)_stage);   // v1d.2：键盘采集
```

> 实施时读 LoomStage.cs 确认 `_collector` 字段名 + Collect 调用上下文（tick 前）。CollectKeys 在 Collect 后、tick 前调（与指针输入同时帧）。

- [ ] **Step 7: 静态核对（本机无 Unity 不编译）**

本机无 Unity，C# 不编译。人工核对：
- EventType 4 值（12-15）不与既有冲突。
- LoomEvent modifiers @6 对齐 Rust pad[0]。
- EventContext KeyCode/Modifiers 填充两处都改。
- DispatchPending 4 case 加对位置。
- CollectKeys/CurrentModifiers/KeyList 语法 + 新旧输入系统宏。
- csbindgen 生成的 `Bindings.KeyEvent` 字段名（key_code/modifiers/is_down/pad0/pad1）—— csbindgen 从 Rust `KeyEvent { key_code, modifiers, is_down, pad }` 生成，pad:[u8;2] → pad0/pad1（照 PointerEvent 模式）。

家里机编译验证（T7 验收）。

- [ ] **Step 8: Commit**

```bash
git add loomgui_unity/Assets/LoomGUI/Runtime/LoomEventHandler.cs loomgui_unity/Assets/LoomGUI/Runtime/LoomInputCollector.cs loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs
git commit -m "feat(v1d.2-T6): C# EventType +4 + LoomEvent.modifiers + 键盘采集 CollectKeys"
```

---

### Task 7: C# EditMode 测（KeyDown/FocusIn BubbleRoute）+ 家里机验收文档

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Tests/LoomEventHandlerTests.cs`（+2 测）
- Create: `docs/v1d.2-home-verification.md`

**Interfaces:**
- Consumes: T6 的 EventType.KeyDown/FocusIn + EventContext。
- Produces: C# EditMode 测 + 家里机 PlayMode 验收清单。

- [ ] **Step 1: 写 C# EditMode 测——KeyDown/FocusIn BubbleRoute**

`LoomEventHandlerTests.cs`（既有 v1d.1 drag/longpress 测旁）加。照 `DragStart_BubbleRoute_ReachesAncestors` 模式（手搓 LoomEvent + DispatchPending + 验祖先链收）：

```csharp
        [Test]
        public void KeyDown_BubbleRoute_ReachesAncestors()
        {
            // child(2) KeyDown → child + parent(1) + root(0) 都收（bubble）。
            var h = BuildStage();   // 既有 helper：root(0)+parent(1)+child(2)，_parentCache 已 mock 或 node_parent 返链
            var received = new System.Collections.Generic.List<uint>();
            h.AddListener(2, EventType.KeyDown, ctx => received.Add(ctx.currentTarget));
            h.AddListener(1, EventType.KeyDown, ctx => received.Add(ctx.currentTarget));
            h.AddListener(0, EventType.KeyDown, ctx => received.Add(ctx.currentTarget));
            // 手搓 KeyDown@child(2) LoomEvent（key_code 复用 touch_id=Return=13，modifiers=0）
            DispatchOne(h, new LoomEvent { nodeId = 2, type = EventType.KeyDown, clickCount = 0, modifiers = 0, touch_id = 13, x = 0f, y = 0f });
            Assert.IsTrue(received.Contains(2), "child 收 KeyDown");
            Assert.IsTrue(received.Contains(1), "parent 收 KeyDown（bubble）");
            Assert.IsTrue(received.Contains(0), "root 收 KeyDown（bubble）");
        }

        [Test]
        public void FocusIn_BubbleRoute_ReachesAncestors()
        {
            var h = BuildStage();
            var received = new System.Collections.Generic.List<uint>();
            h.AddListener(2, EventType.FocusIn, ctx => received.Add(ctx.currentTarget));
            h.AddListener(1, EventType.FocusIn, ctx => received.Add(ctx.currentTarget));
            h.AddListener(0, EventType.FocusIn, ctx => received.Add(ctx.currentTarget));
            DispatchOne(h, new LoomEvent { nodeId = 2, type = EventType.FocusIn, clickCount = 0, modifiers = 0, touch_id = 0, x = 0f, y = 0f });
            Assert.IsTrue(received.Contains(2), "child 收 FocusIn");
            Assert.IsTrue(received.Contains(1), "parent 收 FocusIn（bubble）");
            Assert.IsTrue(received.Contains(0), "root 收 FocusIn（bubble）");
        }
```

> **helper 确认**：读 `LoomEventHandlerTests.cs` 既有 `DragStart_BubbleRoute_ReachesAncestors` 看它怎么手搓 LoomEvent + DispatchPending（`DispatchOne`/`BuildStage` helper 名 + LoomEvent 字段序）。照搬模式——LoomEvent 加了 `modifiers` 字段后，既有手搓 LoomEvent 字面量也要加 `modifiers = 0`（StructLayout 字段序变，既有字面量缺 modifiers 会编译失败或错位）。**实施时**：既有所有 `new LoomEvent { ... }` 字面量补 `modifiers = 0`。

- [ ] **Step 2: 既有 LoomEvent 字面量补 modifiers**

`LoomEventHandlerTests.cs` 内所有 `new LoomEvent { nodeId=..., type=..., clickCount=..., touch_id=..., x=..., y=... }` 字面量在 clickCount 后补 `modifiers = 0`（T6 加字段后字段序变，缺则错位）。逐个改。

- [ ] **Step 3: 静态核对（本机无 Unity）**

人工核对：2 新测 + 既有字面量全补 modifiers。家里机 EditMode Run All 验（T7 验收文档 §4）。

- [ ] **Step 4: 写家里机验收文档**

创建 `docs/v1d.2-home-verification.md`，照 `docs/v1d.1-home-verification.md` 结构。要点：

```markdown
# v1d.2 家里机验收文档

> 本机（无 Unity）已完成 v1d.2 全部实现 + per-task review + final Ready。core+ffi cargo test 全绿；
> C# 代码本机未编译/未跑（家里机验）。
> spec：docs/superpowers/specs/2026-06-24-v1d.2-keyboard-focus-design.md
> plan：docs/superpowers/plans/2026-06-24-v1d.2-keyboard-focus.md

## 0. v1d.2 是什么
键盘 keydown/up + 焦点（tabindex opt-in）+ click-to-focus + Tab/Shift+Tab 导航 + :focus 伪类 + FocusIn/Out。
检测全 core，C# 只路由。key 事件复用 EventRecord 流（EVT 12-15）。

## 关键行为变化（v1d.1→v1d.2）
- tabindex opt-in：只有 tabindex 属性的节点可聚焦/进 Tab 链（默认不可聚焦）。
- click-to-focus：pointer-down 命中 tabindex>=0 节点 → 自动聚焦（发 FocusIn）。
- Tab 导航：Tab/Shift+Tab 按 tabindex 序遍历（正整数升序后 0 组），wrap。Tab 被消费不发 keydown。
- keydown/up：有焦点才发（无焦点丢弃）。
- :focus 伪类：聚焦节点 :focus 规则匹配（rematch 每帧）。
- request_focus：编程聚焦（强制，含 tabindex=None/-1）；disabled 拒。

## 1. 前置
- core+ffi 全绿（含 v1d.2 新测：tab链/key事件/focus/click-to-focus/:focus/pkg v5/abi key/tab/request_focus）
- .dll 已重编 commit（v1d.2，含 KeyEvent + 3 FFI + version v1d.2）
- final review：Ready

## 2. 拉代码
git fetch origin && git checkout main && git pull origin main
> pull 前关 Unity（坑 10）。

## 3. 打开 Unity
- .dll 已 commit，无需重编
- 新 FFI 函数（set_key_input/request_focus/focused_node）+ KeyEvent struct → csbindgen reimport 时 regen LoomGUIBindings.cs
- 若 csbindgen 报找不到：删 LoomGUIBindings.cs 触发 reimport
- 等 reimport 完

## 4. EditMode 测试（先填 BuildStage font_path）
Window → General → Test Runner → EditMode → Run All，应全绿。
### 4.1 填 BuildStage font_path（同 v1d.1 §4.1）
### 4.2 LoomEventHandlerTests（20 测）
- 既有 18（v1d.1）+ 2 新（KeyDown_BubbleRoute/FocusIn_BubbleRoute）

## 5. PlayMode 验收
### 5.1 配置 LoomStage（同 v1d.1，_usePackage/_pkgFile/_font/_safeArea）
### 5.2 tabindex + click-to-focus
sample HTML 加 <button tabindex="0"> 可聚焦元素，注册 listener：
  h.AddListener(id, EventType.FocusIn, ctx => Debug.Log($"focus in {ctx.target}"));
1. 鼠标点 tabindex 节点 → Console 见 focus in
2. 点不可聚焦节点 → 无 focus 事件（不夺焦）
### 5.3 Tab 导航
多个 tabindex 节点，按 Tab 键 → 焦点按序移动（:focus 样式变化可见）
Shift+Tab 反向
### 5.4 keydown/up
聚焦后按 Enter/字母 → listener 收 KeyDown（ctx.keyCode = 该键）
无焦点按键 → 无 KeyDown（丢弃）
### 5.5 :focus 伪类
.btn:focus { background-color: blue; } + 聚焦 btn → 背景变蓝
### 5.6 request_focus（编程）
h 调 Native.loomgui_stage_request_focus → 下 tick 焦点变化

## 6. 回归 v1c.1-v1d.1
- 既有 hover/active/disabled/click/drag/longpress/safe-area 不破
- tabindex 无属性的节点行为不变（不可聚焦，照旧）

## 7. 风险点 + 排查
| 风险 | 排查 |
|---|---|
| csbindgen 报 KeyEvent 找不到 | 删 LoomGUIBindings.cs 触发 reimport；确认 .dll 是 v1d.2（md5sum） |
| Tab 不触发导航 | CollectKeys 未调（LoomStage LateUpdate 漏）；或 ENABLE_INPUT_SYSTEM 宏下 Keyboard.current null |
| keydown 不发 | 无焦点（先 click-to-focus 或 request_focus）；或键不在 KeyList 白名单 |
| :focus 不生效 | rematch 未跑（tick 管线）；或 .btn:focus 规则没进 dynamic（extract_dynamic_rules 漏 pseudo_focus） |
| pkg.bin v5 被拒 | 旧 .pkg.bin 是 v4——用新打包器重打 interact sample |
| LoomEvent 字段错位 | 既有 LoomEvent 字面量没补 modifiers=0（T7 Step 2） |

## 8. 报坑流程（同 v1d.1 §8）
## 9. commit 序列（v1d.2）—— final review 后填
## 10. 验收结论
```

- [ ] **Step 5: Commit**

```bash
git add loomgui_unity/Assets/LoomGUI/Tests/LoomEventHandlerTests.cs docs/v1d.2-home-verification.md
git commit -m "test(v1d.2-T7): C# KeyDown/FocusIn BubbleRoute 测 + 家里机验收文档"
```

---

### Task 8: final whole-branch review + session-summary + design §10.3 同步

**Files:**
- Review: 全分支 diff（`git merge-base main HEAD`..HEAD 的 review-package）
- Modify: `.claude/skills/knowledge-reference/SKILL.md`（session-summary：v1d.2 经验）
- Modify: `docs/design/00-main-design.md`（§10.3 +v1d.2 状态块）

> 本 task 是 review + 文档收尾，非编码。用 subagent-driven-development 的 final code-reviewer（opus）审全分支。

- [ ] **Step 1: final whole-branch review**

dispatch final code-reviewer subagent（opus），review-package = `git merge-base main HEAD`..HEAD。审：
- 跨 task 集成点：T1 Node 字段 → T2 pkg → T3 :focus → T4 input → T5 Stage/FFI → T6 C# 类型一致性（tabindex/focused/pseudo_focus/KeyEvent 字段名跨 Rust/C# 对齐）。
- ABI 不变契约：EventRecord 20B / PointerEvent 16B / KeyEvent 8B（ffi sizeof 测锁）。
- pkg.bin v5 NodeBlock tabindex 写读对齐（i32::MIN 哨兵）。
- focus_node/process_keys 借用安全（input.rs process 内 &mut scene + focus_node）。
- request_focus pending_focus_request 时机（R3 解法）。
- Tab 消费语义（R2）。
- 测试覆盖 spec §8 全项。

- [ ] **Step 2: 修 final review 的 Critical/Important findings**

dispatch fix subagent（若 review 返 findings）。re-review 至 Ready。

- [ ] **Step 3: session-summary（knowledge-reference）**

跑 session-summary skill。总结 v1d.2 经验进 `.claude/skills/knowledge-reference/SKILL.md`：
- §2.15 事件/命中/输入层 +v1d.2 段（焦点/键盘/Tab 链/:focus/focus_node 三处共用/process_keys）。
- §3 新 API 适配（若有 csbindgen KeyEvent 生成坑）。
- §5 新坑（编号递增）：若有（如 csbindgen pad 字段名、LoomEvent 字段序变致既有字面量错位）。
- §7 ledger v1d.2 ✅。

- [ ] **Step 4: design §10.3 同步**

`docs/design/00-main-design.md` §10.3 加 v1d.2 实现状态块（照 v1d.1 模式）：keydown/up + 焦点 + Tab + :focus + FocusIn/Out 已实现，ABI/pkg v5/FFI 3 函数。

- [ ] **Step 5: Commit + push**

```bash
git add .claude/skills/knowledge-reference/SKILL.md docs/design/00-main-design.md
git commit -m "docs(v1d.2): session-summary + design §10.3 同步"
git push origin main
```

> v1d.2 完成。家里机按 `docs/v1d.2-home-verification.md` 验收。

---


