# v1c.2 事件冒泡 / DOM 三阶段路由 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 v1c.1 事件层补 DOM 三阶段路由（capture+bubble + stop_propagation）+ RollOver/Out 祖先链 diff；路由放 C# 业务侧（方向 A），核心改动极小。

**Architecture:** 核心产 target 事件 + RollOver/Out 多目标 diff（命中 diff 留核心）；C# `LoomEventHandler` 沿 `node_parent` 链 bubble/capture 路由（fgui `EventDispatcher` 重实现）。EventRecord/event blob 零改，只新增 `node_parent` FFI + 改 `hover_diff`。

**Tech Stack:** Rust（`loomgui_core/src/input.rs` / `loomgui_ffi_c/src/lib.rs`）、C#（`loomgui_unity/Assets/LoomGUI/Runtime/LoomEventHandler.cs` / `LoomStage.cs`）、Unity 6.5；对照 fgui `temp/FairyGUI-unity/Assets/Scripts/{Event,Core/Stage}.cs`。

**Spec:** `docs/superpowers/specs/2026-06-23-v1c.2-event-bubbling-design.md`（设计契约，本 plan 的源头）。

## Global Constraints

（从 spec §0/§9 提取，每 task 隐含遵守）

- **方向 A**：bubble/capture 路由 + listener 表在 **C# 业务侧**；核心只保留命中 hit_test + 命中 diff（hover/active 状态 + RollOver/Out 产出）+ 伪类 rematch。
- **EventRecord / event blob / frame blob / .pkg.bin v3 / MirrorPool / shader 零改**——不碰渲染树 SOA。
- **EventRecord POD 不加字段**：`node_id` 在 bubble 类=target（C# 沿链展开），在 RollOver/Out=diff 出的具体节点（C# 直派）。
- **FFI `node_parent` 常驻不 gate**（runtime 入口，坑 21）；根/越界/无 scene → sentinel `0xFFFF_FFFF`。
- **listener 委托引用 remove**（非 ListenerId，同 fgui）；多播委托（capture + bubble 两组）。
- **capture 阶段不检查 stop**（照 fgui `BubbleEvent` line 302-311）；bubble 阶段 stop break。
- **RollOver/Out 直派不 bubble**（核心已 diff 多目标，对齐 fgui `DispatchEvent`）。
- **两机分工**：core `cargo test --workspace` 本机自检（全绿才 push）；C# 路由逻辑家里机 EditMode Test Runner + PlayMode 验（本机无 Unity）。
- **改 Rust 后必重编 + commit .dll**（坑 10 两机变体：本机 `cargo build -p loomgui_ffi_c --release` + cp + commit；家里机 pull）。
- **对照 fgui**：实现 C# 路由前读 `temp/FairyGUI-unity/Assets/Scripts/Event/{EventDispatcher,EventContext,EventBridge}.cs`（知识库 §0 工作准则）。

---

## Task 1: core hover_diff 祖先链 diff（点 1）

**Files:**
- Modify: `loomgui_core/src/input.rs`（`PointerState` 字段 + `Default` + `hover_diff` 重写 + 新增 `ancestor_chain`）
- Test: `loomgui_core/src/input.rs` `mod tests`

**Interfaces:**
- Consumes: `hit_test(scene, point)`（hit.rs，v1c.1）、`Node.parent: Option<NodeId>`（scene）、`set_hovered_chain`（input.rs 现有，不改）
- Produces: `PointerState.last_hovered_chain: Vec<NodeId>`（替代 `last_hovered: Option<NodeId>`）；`hover_diff` 产 RollOver/Out **多条**（RollOut=旧链独有节点、RollOver=新链独有节点，共同祖先段不产事件）

- [ ] **Step 1: 写失败测试（既有测用 any 断言仍绿，新增 4 个祖先链 diff 测）**

在 `loomgui_core/src/input.rs` 的 `mod tests` 末尾追加（`button_with_text_child_scene` 等既有 helper 复用；新增三层嵌套 helper）：

```rust
    /// v1c.2: root + parent(100x100) + child(50x50 in parent)。验 hover 祖先链 diff。
    fn nested_scene() -> Scene {
        let mut root = Node::default();
        root.id = NodeId(0);
        root.layout_rect = Rect { x: 0.0, y: 0.0, w: 200.0, h: 200.0 };
        let mut parent = Node::default();
        parent.id = NodeId(1);
        parent.parent = Some(NodeId(0));
        parent.layout_rect = Rect { x: 0.0, y: 0.0, w: 100.0, h: 100.0 };
        let mut child = Node::default();
        child.id = NodeId(2);
        child.parent = Some(NodeId(1));
        child.layout_rect = Rect { x: 0.0, y: 0.0, w: 50.0, h: 50.0 };
        parent.children = vec![NodeId(2)];
        root.children = vec![NodeId(1)];
        Scene { roots: vec![NodeId(0)], nodes: vec![root, parent, child], dynamic_rules: Default::default() }
    }

    #[test]
    fn hover_into_child_no_rollout_parent() {
        // 点 1 回归：hover parent 区(75,75) → 链 [parent,root]；移进 child 区(10,10) → 链 [child,parent,root]。
        // 共同 parent,root → 不产 RollOut(parent)；child 新 → RollOver(child)。
        let mut s = nested_scene();
        let mut ps = PointerState::new();
        ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 75.0, y: 75.0, button: 0 }]);
        let out = ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 10.0, y: 10.0, button: 0 }]);
        assert!(!out.iter().any(|e| e.event_type == EVT_ROLL_OUT), "进子 → 不产任何 RollOut");
        assert!(out.iter().any(|e| e.event_type == EVT_ROLL_OVER && e.node_id == 2), "进子 → RollOver(child)");
    }

    #[test]
    fn hover_between_siblings_old_chain_rollout() {
        // 兄弟 A/B：hover A → RollOver(A)+RollOver(root)；移到 B → RollOut(A)+RollOver(B)（root 共同不产）。
        let mut root = Node::default();
        root.id = NodeId(0);
        root.layout_rect = Rect { x: 0.0, y: 0.0, w: 200.0, h: 200.0 };
        let mut a = Node::default();
        a.id = NodeId(1); a.parent = Some(NodeId(0));
        a.layout_rect = Rect { x: 0.0, y: 0.0, w: 50.0, h: 50.0 };
        let mut b = Node::default();
        b.id = NodeId(2); b.parent = Some(NodeId(0));
        b.layout_rect = Rect { x: 100.0, y: 100.0, w: 50.0, h: 50.0 };
        root.children = vec![NodeId(1), NodeId(2)];
        let mut s = Scene { roots: vec![NodeId(0)], nodes: vec![root, a, b], dynamic_rules: Default::default() };
        let mut ps = PointerState::new();
        ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 25.0, y: 25.0, button: 0 }]);  // 命中 A
        let out = ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 125.0, y: 125.0, button: 0 }]);  // 命中 B
        assert!(out.iter().any(|e| e.event_type == EVT_ROLL_OUT && e.node_id == 1), "移到 B → RollOut(A)");
        assert!(out.iter().any(|e| e.event_type == EVT_ROLL_OVER && e.node_id == 2), "移到 B → RollOver(B)");
        assert!(!out.iter().any(|e| e.node_id == 0), "root 共同祖先 → 不产事件");
    }

    #[test]
    fn hover_chain_idempotent() {
        // 同点 Move 两次 → 第二次无事件（链不变）。
        let mut s = nested_scene();
        let mut ps = PointerState::new();
        ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 10.0, y: 10.0, button: 0 }]);
        let out = ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 10.0, y: 10.0, button: 0 }]);
        assert!(out.is_empty(), "同点 Move → 无事件（幂等）");
    }

    #[test]
    fn hover_out_of_ui_rollout_whole_chain() {
        // hover child → 链 [child,parent,root]；移出根外 → 空链 → 整链 RollOut。
        let mut s = nested_scene();
        let mut ps = PointerState::new();
        ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 10.0, y: 10.0, button: 0 }]);
        let out = ps.process(&mut s, &[PointerEvent { kind: PointerKind::Move, x: 300.0, y: 300.0, button: 0 }]);  // 根外
        assert!(out.iter().any(|e| e.event_type == EVT_ROLL_OUT && e.node_id == 2), "移出 → RollOut(child)");
        assert!(out.iter().any(|e| e.event_type == EVT_ROLL_OUT && e.node_id == 1), "移出 → RollOut(parent)");
        assert!(!out.iter().any(|e| e.event_type == EVT_ROLL_OVER), "移出 → 无 RollOver");
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p loomgui_core input`
Expected: FAIL — `last_hovered` 字段编译错（Step 3 才改），或新测因单点 hover_diff 行为不通过（RollOver/Out 多条未实现）。

- [ ] **Step 3: 实现 — PointerState 字段 + Default + ancestor_chain + hover_diff 重写**

改 `loomgui_core/src/input.rs`：

(a) `PointerState` 字段（约 line 43-50）`last_hovered: Option<NodeId>` → `last_hovered_chain: Vec<NodeId>`：
```rust
#[derive(Debug, Clone)]
pub struct PointerState {
    pub last_pos: (f32, f32),
    pub is_down: bool,
    pub down_node: Option<NodeId>,
    pub down_pos: (f32, f32),
    pub last_hovered_chain: Vec<NodeId>,   // v1c.2: 上帧 hovered 链（target→root），替代 last_hovered: Option<NodeId>
}
```

(b) `Default`（约 line 52-62）：
```rust
impl Default for PointerState {
    fn default() -> Self {
        Self {
            last_pos: (0.0, 0.0),
            is_down: false,
            down_node: None,
            down_pos: (0.0, 0.0),
            last_hovered_chain: Vec::new(),
        }
    }
}
```

(c) 新增 free function `ancestor_chain`（放在 `set_active_chain` 后，约 line 89 后）：
```rust
/// target 起沿 Node.parent 至 root 收集 NodeId 链（含 target）；target=None → 空链。
fn ancestor_chain(scene: &Scene, target: Option<NodeId>) -> Vec<NodeId> {
    let mut chain = Vec::new();
    let mut cur = target;
    while let Some(id) = cur {
        if id.0 >= scene.nodes.len() { break; }   // 防御（脏 scene）
        chain.push(id);
        cur = scene.nodes[id.0].parent;
    }
    chain
}
```

(d) 重写 `hover_diff`（约 line 100-125，整段替换）：
```rust
    /// hover diff：比较 cur_hover 祖先链与 last_hovered_chain，产 RollOut(旧链独有)/RollOver(新链独有)。
    /// 共同祖先段不产事件（鼠标从父进子 → 父不 RollOut，对齐 fgui HandleRollOver + CSS :hover 祖先语义）。
    /// diff 无变化时幂等无输出（每帧可安全重复调）。
    fn hover_diff(&mut self, scene: &mut Scene, out: &mut Vec<EventRecord>) {
        let cur_hover = hit_test(scene, self.last_pos);
        let new_chain = ancestor_chain(scene, cur_hover);
        if new_chain == self.last_hovered_chain {
            return;
        }
        for n in &self.last_hovered_chain {
            if !new_chain.contains(n) {
                out.push(EventRecord { node_id: n.0 as u32, event_type: EVT_ROLL_OUT, x: self.last_pos.0, y: self.last_pos.1 });
            }
        }
        for n in &new_chain {
            if !self.last_hovered_chain.contains(n) {
                out.push(EventRecord { node_id: n.0 as u32, event_type: EVT_ROLL_OVER, x: self.last_pos.0, y: self.last_pos.1 });
            }
        }
        // hovered 状态沿祖先链设（供伪类重匹配，.btn:hover 匹配 btn 即使命中其文字子）。
        set_hovered_chain(scene, cur_hover);
        self.last_hovered_chain = new_chain;
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p loomgui_core`
Expected: PASS — 全部既有 input 测 + 4 新测绿（既有测用 `any`/`contains` 断言，对 RollOver/Out 多条仍成立）。

- [ ] **Step 5: commit**

```bash
git add loomgui_core/src/input.rs
git commit -m "feat(v1c.2-T1): core hover_diff 祖先链 diff — RollOver/Out 多目标（点1）"
```

---

## Task 2: FFI node_parent + version v1c.2

**Files:**
- Modify: `loomgui_ffi_c/src/lib.rs`（新增 `loomgui_node_parent` + `loomgui_version` v1c.1→v1c.2 + 改 version 测）
- Test: `loomgui_ffi_c/src/lib.rs` `abi_tests`

**Interfaces:**
- Consumes: `StageHandle.stage.scene`（`Option<Scene>`）、`Node.parent`（scene）
- Produces: `loomgui_node_parent(h: *const StageHandle, node_id: u32) -> u32`（C# 路由沿链用，Task 4 消费）；`loomgui_version()` 返 `"v1c.2"`

- [ ] **Step 1: 改 version 测期望（RED）**

改 `loomgui_ffi_c/src/lib.rs` `mod tests` 的 `version_returns_c_string_v1c1`（约 line 298-304），测名 + 断言改 v1c.2：
```rust
    #[test]
    fn version_returns_c_string_v1c2() {
        unsafe {
            let s = CStr::from_ptr(loomgui_version() as *const i8);
            assert_eq!(s.to_str().unwrap(), "v1c.2");
        }
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p loomgui_ffi_c version`
Expected: FAIL — `version_returns_c_string_v1c2` 找不到（重命名）+ 断言 "v1c.1" != "v1c.2"。

- [ ] **Step 3: 实现 — version v1c.2 + node_parent**

(a) `loomgui_version`（约 line 19）`"v1c.1"` → `"v1c.2"`：
```rust
        .get_or_init(|| CString::new("v1c.2").unwrap())
```

(b) 新增 `loomgui_node_parent`（放在 `loomgui_stage_set_node_disabled` 后，`loomgui_shutdown` 前，约 line 273 后）：
```rust
/// 返 parent node_id（v1c.2：C# 事件路由沿链用，spec §4.2）。根/越界/无 scene → 0xFFFF_FFFF（sentinel）。
///
/// **常驻（不 gate）：**runtime 稳定入口，`--no-default-features` 构建的 .dll 仍有本函数（坑 21）。
#[no_mangle]
pub extern "C" fn loomgui_node_parent(h: *const StageHandle, node_id: u32) -> u32 {
    const ROOT_SENTINEL: u32 = 0xFFFF_FFFF;
    if h.is_null() {
        return ROOT_SENTINEL;
    }
    let sh = unsafe { &*h };
    match &sh.stage.scene {
        Some(scene) => {
            let idx = node_id as usize;
            if idx < scene.nodes.len() {
                scene.nodes[idx].parent.map(|p| p.0 as u32).unwrap_or(ROOT_SENTINEL)
            } else {
                ROOT_SENTINEL
            }
        }
        None => ROOT_SENTINEL,
    }
}
```

- [ ] **Step 4: 加 node_parent abi 测试**

在 `abi_tests` 末尾追加（手搓 root>child 树，不走 parse）：
```rust
    /// node_parent 契约（v1c.2）：child.parent==root；root.parent==sentinel；OOB==sentinel。
    #[test]
    fn node_parent_returns_chain_and_sentinel() {
        use loomgui_core::asset::{write_package, AtlasSection};
        use loomgui_core::scene::{NodeKind, Scene};
        use loomgui_core::style::{resolved::ResolvedStyle, dynamic::DynamicRuleTable};
        let (fp, fplen) = font_path();
        let entries = vec![
            (None, NodeKind::Container, ResolvedStyle::default(), Vec::new(), None),
            (Some(0), NodeKind::Container, ResolvedStyle::default(), Vec::new(), None),
        ];
        let pkg = write_package(&Scene::build(&entries), (100.0, 50.0), &AtlasSection::default(), &DynamicRuleTable::default());
        let h = loomgui_stage_new(fp.as_ptr() as *const u8, fplen, 100.0, 50.0);
        assert!(!h.is_null());
        assert_eq!(loomgui_stage_load_package(h, pkg.as_ptr(), pkg.len()), 0);
        assert_eq!(loomgui_node_parent(h, 1), 0, "child(1).parent == root(0)");
        assert_eq!(loomgui_node_parent(h, 0), 0xFFFF_FFFF, "root(0).parent == sentinel");
        assert_eq!(loomgui_node_parent(h, 99), 0xFFFF_FFFF, "OOB == sentinel");
        loomgui_stage_free(h);
    }
```

并在既有 `no_default_features_builds` 测（约 line 517-530）末尾、`loomgui_stage_free(h)` 前加一行验 node_parent 常驻可调（无 scene → sentinel，不 panic）：
```rust
        assert_eq!(loomgui_node_parent(h, 0), 0xFFFF_FFFF, "无 scene → sentinel，不 panic");
```

- [ ] **Step 5: 跑测试 + no-default-features 构建门**

Run: `cargo test -p loomgui_ffi_c`
Expected: PASS — version v1c.2 + node_parent 测绿。

Run: `cargo build -p loomgui_ffi_c --no-default-features`
Expected: 编译通过（证 node_parent 常驻不 gate，坑 21）。

Run: `cargo test --workspace`
Expected: PASS — 全 workspace 绿（验 hover_diff 改动没破坏其他 crate）。

- [ ] **Step 6: commit**

```bash
git add loomgui_ffi_c/src/lib.rs
git commit -m "feat(v1c.2-T2): FFI node_parent（C#路由沿链）+ version v1c.2"
```


## Task 3: C# EventContext + EventBridge + Phase（基础类型）

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomEventHandler.cs`（在现有 `EventType` enum + `LoomEvent` struct 之后追加类型定义；**不删**现有内容）
- Test: `loomgui_unity/Assets/LoomGUI/Tests/LoomEventHandlerTests.cs`

**Interfaces:**
- Produces: `Phase : byte`（Capture/Target/Bubble）、`EventCallback(EventContext)` delegate、`EventContext`（target/currentTarget/phase/type/x/y + StopPropagation/PreventDefault + 对象池 Get/Return + SubsystemRegistration 清池）、`EventBridge`（多播 capture/bubble，Add/Remove/AddCapture/RemoveCapture/CallBubble/CallCapture/isEmpty）

> **对照 fgui**：`temp/FairyGUI-unity/Assets/Scripts/Event/EventContext.cs`（对象池 line 74-102）+ `EventBridge.cs`（多播委托 line 36-45）。读这两个再写。

- [ ] **Step 1: 写失败测试（家里机 Test Runner 验；本机写测代码，标 RED 待家里机）**

在 `LoomEventHandlerTests.cs` 追加：
```csharp
        [Test]
        public void EventContext_Pool_ReusesInstances()
        {
            var a = LoomGUI.EventContext.Get();
            a.target = 7;
            LoomGUI.EventContext.Return(a);
            var b = LoomGUI.EventContext.Get();
            Assert.AreSame(a, b, "池复用同一实例");
            Assert.AreEqual(0u, b.target, "Get 时重置标志（target 不重置，但 stop/prevent 重置）");
            LoomGUI.EventContext.Return(b);
        }

        [Test]
        public void EventBridge_AddMultipleCallbacks_AllInvoked()
        {
            var bridge = new LoomGUI.EventBridge();
            int hits = 0;
            LoomGUI.EventCallback cb1 = _ => hits++;
            LoomGUI.EventCallback cb2 = _ => hits++;
            bridge.Add(cb1); bridge.Add(cb2);
            bridge.CallBubble(null);
            Assert.AreEqual(2, hits, "多播：两个 cb 都调");
            bridge.Remove(cb1);
            hits = 0; bridge.CallBubble(null);
            Assert.AreEqual(1, hits, "Remove cb1 后只 cb2");
        }
```

- [ ] **Step 2: 实现类型定义**

在 `LoomEventHandler.cs` 的 `LoomEvent` struct 定义之后、`LoomEventHandler` class 之前追加：
```csharp
    public enum Phase : byte { Capture = 0, Target = 1, Bubble = 2 }
    public delegate void EventCallback(EventContext ctx);

    /// EventContext（照 fgui EventContext.cs，对象池复用）。
    public class EventContext
    {
        public uint target;            // 原始命中（EventRecord.node_id）
        public uint currentTarget;     // 路由当前节点
        public Phase phase;
        public EventType type;
        public float x, y;
        internal bool _stopsPropagation, _defaultPrevented;
        public void StopPropagation() => _stopsPropagation = true;
        public void PreventDefault() => _defaultPrevented = true;

        static readonly System.Collections.Generic.Stack<EventContext> _pool = new();
        internal static EventContext Get()
        {
            var ctx = _pool.Count > 0 ? _pool.Pop() : new EventContext();
            ctx._stopsPropagation = false; ctx._defaultPrevented = false;
            return ctx;
        }
        internal static void Return(EventContext ctx) => _pool.Push(ctx);

        [UnityEngine.RuntimeInitializeOnLoadMethod(UnityEngine.RuntimeInitializeLoadType.SubsystemRegistration)]
        static void ClearPool() => _pool.Clear();   // Domain reload 清池（照 fgui EventContext.cs:96-102）
    }

    /// EventBridge（照 fgui EventBridge.cs）：capture + bubble 两组多播委托。
    internal class EventBridge
    {
        EventCallback _bubble, _capture;
        public void Add(EventCallback cb) { _bubble -= cb; _bubble += cb; }       // -= 去重
        public void AddCapture(EventCallback cb) { _capture -= cb; _capture += cb; }
        public void Remove(EventCallback cb) => _bubble -= cb;
        public void RemoveCapture(EventCallback cb) => _capture -= cb;
        public bool isEmpty => _bubble == null && _capture == null;
        public void CallBubble(EventContext ctx) { if (_bubble != null) _bubble(ctx); }
        public void CallCapture(EventContext ctx) { if (_capture != null) _capture(ctx); }
    }
```

- [ ] **Step 3: 家里机 Test Runner 验（本机无 Unity，跳过运行；代码审查 + core 自检为信心 commit）**

> 本机命令（仅编译检查 C# 语法不可能——需 Unity；故本机 Step 3 = 人工核代码，commit，家里机 pull 后 Test Runner 验 RED→GREEN）。家里机：`Window → General → Test Runner → EditMode → Run`，`EventContext_Pool_ReusesInstances` + `EventBridge_AddMultipleCallbacks_AllInvoked` 绿。

- [ ] **Step 4: commit**

```bash
git add loomgui_unity/Assets/LoomGUI/Runtime/LoomEventHandler.cs loomgui_unity/Assets/LoomGUI/Tests/LoomEventHandlerTests.cs
git commit -m "feat(v1c.2-T3): C# EventContext(对象池)+EventBridge(多播) — 照 fgui"
```

---

## Task 4: C# 路由（LoomEventHandler 重写 — bubble/capture/stop/listener 表）

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomEventHandler.cs`（重写 `LoomEventHandler` class：listener 表 + _parentCache + _handle + BubbleRoute/DirectDispatch/AncestorChain/DispatchPending）
- Test: `loomgui_unity/Assets/LoomGUI/Tests/LoomEventHandlerTests.cs`

**Interfaces:**
- Consumes: `Native.loomgui_node_parent(handle, nodeId)`（csbindgen 在 Task 2 重编时生成；签名 `extern static extern uint loomgui_node_parent(IntPtr h, uint nodeId)`，实现者核对生成的绑定名）、`EventContext`/`EventBridge`（Task 3）、`EventType`/`LoomEvent`（现有）
- Produces: `LoomEventHandler.AddListener/AddCapture/RemoveListener/RemoveCapture/SetHandle/DispatchPending`；**删除** v1c.1 的单 callback `AddListener(nodeId,type,Action<LoomEvent>)`（breaking，sample 在 Task 5 改）

> **对照 fgui**：`EventDispatcher.cs` `BubbleEvent`（line 286-350，capture 反向全跑 + bubble 正向 stop）+ `InternalDispatchEvent`（line 205-247，直派单节点 capture+bubble）+ `GetChainBridges`（line 470-513，沿 parent 链收 bridge）。

- [ ] **Step 1: 写失败测试（家里机验）**

在 `LoomEventHandlerTests.cs` 追加（路由测需 native node_parent，家里机 EditMode with .dll 跑；本机写代码）：
```csharp
        // 辅助：手搓 handler + handle（家里机 EditMode 需真 .dll；本机写测代码不跑）
        // node_parent 由核心 FFI 提供（Task 2），手搓场景 root(0)>parent(1)>child(2)
        // 测假设 Native.loomgui_node_parent 已加载（.dll 在 Plugins）

        [Test]
        public void BubbleRoute_ReachesAllAncestors()
        {
            var h = new LoomEventHandler();
            // ...（家里机：构造含 3 节点的 Stage handle，SetHandle 后测）
            // child(2) Down → bubble: child(2) Target, parent(1) Bubble, root(0) Bubble 都收
            // 断言：3 个节点 listener 都被调，phase 序 Capture(root)>Target(child)>Bubble(parent/root)
        }

        [Test]
        public void StopPropagation_BreaksBubbleButNotCapture()
        {
            // child bubble 回调 StopPropagation → parent/root bubble 不收；
            // 但 capture 阶段（root→child 反向）已全跑（照 fgui line 302-311 不检查 stop）
        }

        [Test]
        public void RollOver_DirectDispatch_NoBubble()
        {
            // RollOver(child) → 只 child 收，parent/root 不收（直派非 bubble）
        }

        [Test]
        public void AddCapture_FiresInCapturePhaseBeforeTarget()
        {
            // root AddCapture(Down) → capture 阶段先于 child Target 收
        }

        [Test]
        public void DelegateRemove_StopsReceiving()
        {
            // RemoveListener(nodeId, type, cb) 后 cb 不再被调
        }
```

> **测试完整化说明**：上述测需 Stage handle + scene（root>parent>child）。家里机实现者用 `LoomStage` 装载手搓包（root>parent>child 三 Container），`SetHandle` 后注册 listener、`DispatchPending` 喂手搓 `LoomEvent[]`（或真 set_input→tick→borrow_events）。本机只写测骨架 + 断言意图；家里机补全 handle 装载（参考既有 `LoomEventHandlerTests.cs` v1c.1 测的 setup 风格）。

- [ ] **Step 2: 实现 — 重写 LoomEventHandler class**

替换 `LoomEventHandler.cs` 中整个 `public class LoomEventHandler { ... }`（v1c.1 单 callback 版，约 line 32-65）为：
```csharp
    /// C# 事件路由（v1c.2 方向 A，照 fgui EventDispatcher）。listener 表 + bubble/capture 路由。
    public class LoomEventHandler
    {
        readonly System.Collections.Generic.Dictionary<uint, System.Collections.Generic.Dictionary<EventType, EventBridge>> _listeners = new();
        readonly System.Collections.Generic.Dictionary<uint, uint> _parentCache = new();   // node_parent 缓存
        IntPtr _handle;   // node_parent FFI 用（LoomStage 初始化/load 后 SetHandle）

        public void SetHandle(IntPtr handle) { _handle = handle; _parentCache.Clear(); }

        public void AddListener(uint nodeId, EventType type, EventCallback cb) => GetBridge(nodeId, type).Add(cb);
        public void AddCapture(uint nodeId, EventType type, EventCallback cb) => GetBridge(nodeId, type).AddCapture(cb);
        public void RemoveListener(uint nodeId, EventType type, EventCallback cb) => TryGetBridge(nodeId, type)?.Remove(cb);
        public void RemoveCapture(uint nodeId, EventType type, EventCallback cb) => TryGetBridge(nodeId, type)?.RemoveCapture(cb);

        EventBridge GetBridge(uint nodeId, EventType type)
        {
            if (!_listeners.TryGetValue(nodeId, out var byType))
                _listeners[nodeId] = byType = new System.Collections.Generic.Dictionary<EventType, EventBridge>();
            if (!byType.TryGetValue(type, out var bridge))
                byType[type] = bridge = new EventBridge();
            return bridge;
        }
        EventBridge TryGetBridge(uint nodeId, EventType type)
            => (_listeners.TryGetValue(nodeId, out var byType) && byType.TryGetValue(type, out var bridge)) ? bridge : null;

        /// 读 EventRecord[] buffer → 按 type 分流：Down/Up/Move/Click → BubbleRoute；RollOver/Out → DirectDispatch。
        public void DispatchPending(IntPtr ptr, int count)
        {
            if (ptr == IntPtr.Zero || count <= 0) return;
            int recSize = System.Runtime.InteropServices.Marshal.SizeOf<LoomEvent>();
            for (int i = 0; i < count; i++)
            {
                var evt = System.Runtime.InteropServices.Marshal.PtrToStructure<LoomEvent>(ptr + i * recSize);
                switch (evt.type)
                {
                    case EventType.Down: case EventType.Up:
                    case EventType.Move: case EventType.Click: BubbleRoute(evt); break;
                    case EventType.RollOver: case EventType.RollOut: DirectDispatch(evt); break;
                }
            }
        }

        /// bubble 类事件：capture(根→target 反向，不检查 stop) + bubble(target→root 正向，stop break)。照 fgui BubbleEvent。
        void BubbleRoute(LoomEvent evt)
        {
            var chain = AncestorChain(evt.nodeId);   // [target, ..., root]
            var ctx = EventContext.Get();
            ctx.target = evt.nodeId; ctx.type = evt.type; ctx.x = evt.x; ctx.y = evt.y;
            // capture 阶段：根→target 反向，全跑（照 fgui line 302-311 不检查 stop）
            for (int i = chain.Count - 1; i >= 0; i--)
            {
                ctx.currentTarget = chain[i]; ctx.phase = Phase.Capture;
                TryGetBridge(chain[i], evt.type)?.CallCapture(ctx);
            }
            // bubble 阶段：target→root 正向，stop break（照 fgui line 315-328）
            if (!ctx._stopsPropagation)
                for (int i = 0; i < chain.Count; i++)
                {
                    ctx.currentTarget = chain[i];
                    ctx.phase = (chain[i] == ctx.target) ? Phase.Target : Phase.Bubble;
                    TryGetBridge(chain[i], evt.type)?.CallBubble(ctx);
                    if (ctx._stopsPropagation) break;
                }
            EventContext.Return(ctx);
        }

        /// RollOver/Out 直派：核心已 diff 多目标，每条单节点跑 capture+bubble 回调（不沿链）。照 fgui DispatchEvent。
        void DirectDispatch(LoomEvent evt)
        {
            var ctx = EventContext.Get();
            ctx.target = ctx.currentTarget = evt.nodeId; ctx.phase = Phase.Target;
            ctx.type = evt.type; ctx.x = evt.x; ctx.y = evt.y;
            var bridge = TryGetBridge(evt.nodeId, evt.type);
            if (bridge != null) { bridge.CallCapture(ctx); bridge.CallBubble(ctx); }
            EventContext.Return(ctx);
        }

        /// target 起沿 node_parent 至 root 收集 [target, ..., root]。sentinel 0xFFFFFFFF 止。
        System.Collections.Generic.List<uint> AncestorChain(uint target)
        {
            var chain = new System.Collections.Generic.List<uint>();
            uint c = target;
            while (c != 0xFFFF_FFFF) { chain.Add(c); c = ParentOf(c); }
            return chain;
        }
        uint ParentOf(uint nodeId)
        {
            if (!_parentCache.TryGetValue(nodeId, out var p))
                _parentCache[nodeId] = p = Native.loomgui_node_parent(_handle, nodeId);   // csbindgen 绑定（Task 2 生成）
            return p;
        }
    }
```

> **Native.loomgui_node_parent 绑定**：csbindgen 扫 `loomgui_ffi_c/src/lib.rs` 的 `#[no_mangle] extern "C"` 生成。Task 2 重编 .dll 时 `build.rs` 跑 csbindgen 更新 `Bindings/LoomGUIBindings.cs`（或等价 Native 类）。实现者核对生成的方法名（可能是 `Native.loomgui_node_parent` 或带前缀），统一调用名。

- [ ] **Step 3: 家里机 Test Runner 验（本机跳过运行）**

> 本机：核代码 + core 自检（Task 1/2 已绿）为信心 commit。家里机：EditMode Test Runner 跑 Task 3+4 全测（含路由测，需 .dll 在 Plugins——Task 6 重编后）。若 native 绑定名不符，家里机报编译错 → 回本机修绑定调用名。

- [ ] **Step 4: commit**

```bash
git add loomgui_unity/Assets/LoomGUI/Runtime/LoomEventHandler.cs loomgui_unity/Assets/LoomGUI/Tests/LoomEventHandlerTests.cs
git commit -m "feat(v1c.2-T4): C# bubble/capture 路由 + stop + 多callback + 委托remove — 照 fgui EventDispatcher"
```


## Task 5: LoomStage 接 SetHandle + interact sample 扩展

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs`（初始化 + load 后调 `handler.SetHandle`）
- Modify: `loomgui_pkg/samples/interact/`（page.html / page.css 扩展嵌套结构）
- Modify: sample C# driver（v1c.1 interact 的 listener 注册处，加 bubble/stop/capture 演示）
- Rebuild: `loomgui_unity/Assets/StreamingAssets/loom_interact.pkg.bin`（重打）

**Interfaces:**
- Consumes: `LoomEventHandler.SetHandle`（Task 4）、`loomgui_pkg` CLI（v1b.1）
- Produces: LoomStage 每次场景重建后清 C# parentCache；sample 演示冒泡/stop/capture 供家里机 PlayMode 验收

- [ ] **Step 1: LoomStage 接 SetHandle**

在 `LoomStage.cs` 两处调 `_handler.SetHandle(_stageHandle)`（`_handler` 是 v1c.1 持有的 `LoomEventHandler`，`_stageHandle` 是 `loomgui_stage_new` 返回的指针）：
1. **Awake/初始化**（`_stageHandle` 创建后、首 tick 前）。
2. **`load_package`/`load_inline` 成功后**（scene 重建 → 清 parentCache，否则 C# 沿旧 parent 链路由）。

```csharp
// 在 _stageHandle 赋值后 + 每次 load 成功后：
_handler.SetHandle(_stageHandle);
```

> 实现者按 LoomStage.cs 现有结构定位（v1c.1 已持 `_handler`，只需补 SetHandle 调用点）。

- [ ] **Step 2: 扩展 interact sample HTML/CSS（嵌套结构）**

在 `loomgui_pkg/samples/interact/page.html` 加嵌套（外层 `outer` div 包 `btn`，演示冒泡 + hover 祖先链）：
```html
<div class="root">
  <div class="outer">
    <button class="btn">Click</button>
  </button>
</div>
```
`page.css` 加：
```css
.outer { width: 200px; height: 120px; background-color: #444444; padding: 20px; }
.outer:hover { background-color: #666666; }
.btn { width: 100px; height: 50px; background-color: #00aa00; }
.btn:hover { background-color: #00ff00; }
.btn:active { background-color: #007700; }
```

- [ ] **Step 3: sample C# driver 加 listener（演示 bubble/stop/capture）**

在 v1c.1 interact sample 的 listener 注册处（C# driver，位置按 v1c.1 现状）加：
```csharp
// nodeId 按 build 序（root=0, outer=1, btn=2...），家里机 PlayMode 调试确认实际 id
handler.AddListener(outerId, EventType.Click, ctx => UnityEngine.Debug.Log("outer bubble 收到 click"));
handler.AddListener(btnId, EventType.Click, ctx => {
    UnityEngine.Debug.Log("btn click");
    ctx.StopPropagation();   // 演示：btn stop → outer 不收
});
handler.AddCapture(outerId, EventType.Click, ctx => UnityEngine.Debug.Log("outer capture 先于 btn"));
```

- [ ] **Step 4: 重打 interact pkg.bin**

Run（本机）:
```bash
cargo run -p loomgui_pkg -- samples/interact/page.html samples/interact/page.css -o loomgui_unity/Assets/StreamingAssets/loom_interact.pkg.bin -w 1080 -h 1920
```
Expected: 重生成 `loom_interact.pkg.bin`（+ 若有图，atlas.png 旁挂）。

- [ ] **Step 5: commit（不含 .dll，.dll 在 Task 6）**

```bash
git add loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs loomgui_pkg/samples/interact/ loomgui_unity/Assets/StreamingAssets/loom_interact.pkg.bin
git commit -m "feat(v1c.2-T5): LoomStage SetHandle + interact sample 嵌套冒泡/stop/capture 演示"
```

---

## Task 6: 重编 .dll + 全量验证 + push 准备

**Files:**
- Rebuild + commit: `loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll`（坑 10 两机变体）

- [ ] **Step 1: 全量 core 验证（本机闭环）**

Run: `cargo test --workspace`
Expected: PASS — 全绿（hover_diff T1 + node_parent T2 + 既有全测）。

Run: `cargo build -p loomgui_ffi_c --no-default-features`
Expected: 编译通过（node_parent 常驻，坑 21）。

Run: `cargo build -p loomgui_ffi_c --release`
Expected: 产出 `target/release/loomgui_ffi_c.dll`（Windows）。

- [ ] **Step 2: 换 .dll + 验 freshness（坑 10）**

```bash
# 对比 fresh build vs committed（应不同——Rust 改了 node_parent + version）
md5sum target/release/loomgui_ffi_c.dll loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
# 拷 fresh build 进 Plugins
cp target/release/loomgui_ffi_c.dll loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
```
Expected: 拷后两文件 md5 一致；committed .dll 是 v1c.2（含 node_parent）。

> 本机无 Unity，.dll 不被锁（坑 10「Unity 锁 .dll」不触发）。家里机 pull 时 Unity 关着即安全。

- [ ] **Step 3: commit .dll**

```bash
git add loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
git commit -m "chore(v1c.2-T6): release .dll v1c.2（node_parent + hover_diff）— 坑10 两机变体"
```

- [ ] **Step 4: push 前总核**

Run: `git log --oneline -7`
Expected: 6 个 task commit（T1-T6）+ spec/design commit 序列清晰。

Run: `cargo test --workspace`（最后一次全绿确认）
Expected: PASS。

- [ ] **Step 5: push（交付家里机验 PlayMode）**

```bash
git push
```

> 家里机 pull 后：
> 1. Unity 打开 loomgui_unity 工程（.dll 已是 v1c.2，无需重编）。
> 2. EditMode Test Runner 跑 Task 3+4 C# 测（含路由测）→ 全绿。
> 3. PlayMode 验收 interact sample（spec §8.3 五条）：①hover btn → outer 不 RollOut（祖先链 diff）②click btn → outer 也收 Click（bubble）③btn StopPropagation → outer 不收 ④outer AddCapture → capture 先于 btn ⑤回归 v1c.1 伪类 :hover/:active 仍正常。
> 4. 家里机报回的坑 → 本机修 → 重编 .dll → 再 push（循环）。

---

## 验收对照（v1 spec §4 + v1c.2 spec §8）

- **v1c.2 本身**：bubble/capture 两阶段路由 + stop + 多 callback + RollOver/Out 祖先链 diff + EventContext + 委托 remove — 全落地。
- **v1 验收 #3（hover/active 反馈）**：v1c.1 已达；v1c.2 修 RollOver/Out 祖先链 diff（嵌套不再误 RollOut 父）。
- **零改契约**：EventRecord/event blob/frame blob/.pkg.bin v3/MirrorPool/shader 全零改（验证：`git diff` 应只在 input.rs/lig.rs/LoomEventHandler.cs/LoomStage.cs/sample/.dll）。


