# v1e FFI 同步热路径性能优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 兑现 v1 FFI 同步热路径性能债务——逐节点 dirty hash 让未变节点 emit `Unchanged`（C# 自动跳过 upload），静态帧≈0、冷帧/换页帧 emit ≤2ms；C# `_frameBuf` 改 ArrayPool 零每帧 GC。

**Architecture:** `NodePayload::Unchanged` 从 v0 全链路预留（Rust blob emit kind=0 / C# `FrameBlob.cs:36` 读 kind / `MirrorPool.cs:71` `kind!=1&&!=2 continue` 跳过），v1e 只在 Stage 层加 dirty hash 跟踪，让 `build_render_nodes` 对未变节点保留 `Unchanged` 占位。纯 Rust 侧优化——**FFI 契约零改、blob 零 bump（仍 v4）、pkg 零 bump（仍 v7）、C# MirrorPool 零改**。C# 侧仅 `_frameBuf` 改 ArrayPool。

**Tech Stack:** Rust（loomgui_core + loomgui_ffi_c）、Unity C#（LoomStage）、criterion bench。

## Global Constraints

- **version 串 v1d.5 → v1e**（`loomgui_ffi_c/src/lib.rs` version() 返回 `"v1e"`）。
- **blob 保持 v4**（`Unchanged` kind=0 早有，`blob.rs:155-157` 已就位；零 bump）。
- **pkg formatVersion 保持 7**（ResolvedStyle 无字段变；零 bump）。
- **C# MirrorPool 零改**（`MirrorPool.cs:71` `if (kind != 1 && kind != 2) continue;` 已吃 Unchanged，不动）。
- **合成 scrollbar 强制 emit**（不进 hash 比较，始终 Mesh——scrollbar 随 scroll_pos 变，数量少）。
- **dirty hash 表 transient 不进 pkg**（同 scroll/anim，Stage 持有 `prev_node_hashes: Vec<u64>`）。
- **首帧/空 hash 表 → 全 emit**（无基线，正确性优先，零回归基线）。
- **reload/节点数变 → 清 hash 表 → 下帧全 dirty**。
- **hash 碰撞最坏 1 帧延迟，不破正确性**（ponytail 天花板，标 `// ponytail:` 注释）。
- **两机约束**：本机唯一编码机（Rust build .dll + commit + push）；家里机纯 Unity PlayMode 验收。Rust 改后必重编 + commit .dll。
- **main 直推**（与 v1d.2/.3/.4/.5 一致）。

---

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `loomgui_core/src/render/dirty.rs`（新） | dirty hash 计算（纯函数：节点 → u64） | T1 创建 |
| `loomgui_core/src/render/mod.rs` | `build_render_nodes` 接 hash + Unchanged emit | T2 改 |
| `loomgui_core/src/render/node.rs` | （不动，`Unchanged` 已有） | — |
| `loomgui_core/src/stage.rs` | Stage 持 `prev_node_hashes` + tick 接通 + load clear | T3 改 |
| `loomgui_ffi_c/src/lib.rs` | version `"v1e"` | T4 改 |
| `loomgui_ffi_c/src/blob.rs` | （不动，Unchanged kind=0 已就位） | — |
| `loomgui_core/benches/frame_emit.rs`（新） | criterion bench（静态/冷/换页帧） | T6 创建 |
| `loomgui_core/Cargo.toml` | 加 `[[bench]]` + dev-dep criterion | T6 改 |
| `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs` | `_frameBuf` ArrayPool | T5 改 |
| `.claude/skills/knowledge-reference/SKILL.md` | session-summary + ledger | T7 改 |
| `docs/design/00-main-design.md` | §10.3 v1e status block | T7 改 |

---

### Task 1: dirty hash 纯函数

**Files:**
- Create: `loomgui_core/src/render/dirty.rs`
- Modify: `loomgui_core/src/render/mod.rs`（加 `pub mod dirty;`）
- Test: `loomgui_core/src/render/dirty.rs`（内联 `#[cfg(test)] mod tests`）

**Interfaces:**
- Consumes: `crate::render::node::{RenderNode, NodePayload}`、`crate::transform::Affine2`（= `[f32; 6]`）
- Produces: `pub fn node_hash(rn: &RenderNode) -> u64`——给定一个**已填好字段的** RenderNode（payload 为 Mesh/Text，非 Unchanged），算其 dirty hash。T2 在 build 每节点 emit 后调它记录新 hash。

**设计**：hash 字段逐项对照 blob 公共头列（`blob.rs:23-27`）+ payload 摘要。用 `std::collections::hash_map::DefaultHasher`（帧内一致即可，不求抗碰撞——碰撞最坏多余重传，不破正确性）。f32 按 `to_le_bytes` 哈希。

hash 字段集（**reviewer 严查遗漏**——任一影响视觉的字段漏了 → 该字段变不重传 → 持续视觉错）：
- `world_matrix` 6 列（m_a..m_ty）——**含 scroll_pos**（scroll 容器子树 world matrix 随 scroll_pos 变 → hash 不等 → 自动重传）。
- `visible`、`alpha`、`grayed`、`color_tint[4]`、`blend`（Normal=0，u8）、`mask_context.0`、`sort_key`。
- payload 摘要（不 hash 全量顶点，O(1) per node）：
  - `Mesh { texture, verts.len(), colors[0] }`（texture 捕获 Image 换图；verts.len() 捕获尺寸变；colors[0] 捕获纯色块变色）。
  - `Text { font_size, color, glyph_count, glyphs[0].codepoint }`（glyph_count 捕获文本量变；首字 codepoint 捕获内容变）。
  - `Unchanged` → `node_hash` 不该被调（调用方保证只在 Mesh/Text 时调）；防御性返回 0。

- [ ] **Step 1: 写 failing test**

```rust
// loomgui_core/src/render/dirty.rs
//! v1e dirty hash：逐节点算 u64 hash，供 Stage 跨帧比较决定 emit Unchanged 还是重传。
//! 字段集对照 blob 公共头列（blob.rs:23-27）+ payload 摘要。碰撞最坏 1 帧延迟，不破正确性。
//! ponytail: hash 碰撞最坏 1 帧视觉延迟；换全量 hash 若 profiling 显示遗漏。

use crate::render::node::{RenderNode, NodePayload};
use crate::transform::Affine2;
use std::collections::hash_map::DefaultHasher;
use std::hash::{Hash, Hasher};

/// 算节点 dirty hash。调用方保证 payload 为 Mesh/Text（非 Unchanged）。
/// Unchanged 防御性返回 0（不该被调）。
pub fn node_hash(rn: &RenderNode) -> u64 {
    let mut h = DefaultHasher::new();
    // 公共头列（blob.rs:23-27）。
    rn.world_matrix.hash(&mut h);            // Affine2=[f32;6]，含 scroll_pos
    rn.visible.hash(&mut h);
    rn.alpha.to_le_bytes().hash(&mut h);
    rn.grayed.hash(&mut h);
    rn.color_tint.hash(&mut h);              // [f32;4]
    (match rn.blend { crate::render::node::BlendMode::Normal => 0u8 }).hash(&mut h);
    rn.mask_context.0.hash(&mut h);
    rn.sort_key.hash(&mut h);
    // payload 摘要。
    match &rn.payload {
        NodePayload::Unchanged => 0u64,      // 防御；调用方不应对 Unchanged 调
        NodePayload::Mesh { texture, verts, colors, .. } => {
            texture.hash(&mut h);
            verts.len().hash(&mut h);
            if let Some(c0) = colors.first() { c0.hash(&mut h); }
            h.finish()
        }
        NodePayload::Text { layout, font_size, color, .. } => {
            font_size.to_le_bytes().hash(&mut h);
            color.hash(&mut h);
            let glyph_count: usize = layout.lines.iter().map(|l| l.runs.iter().map(|r| r.glyphs.len()).sum::<usize>());
            glyph_count.hash(&mut h);
            // 首 glyph codepoint（捕获内容变）。
            let first_cp: u32 = layout.lines.first()
                .and_then(|l| l.runs.first())
                .and_then(|r| r.glyphs.first())
                .map(|g| g.codepoint)
                .unwrap_or(0);
            first_cp.hash(&mut h);
            h.finish()
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::render::node::{BlendMode, MaskContext, NodePayload, RenderNode};
    use crate::transform::IDENTITY;

    fn mesh_rn(tex: u32, alpha: f32, color0: [f32;4]) -> RenderNode {
        RenderNode {
            node_id: 0, parent_id: None, visible: true, alpha,
            grayed: false, color_tint: [1.0;4],
            world_matrix: IDENTITY, blend: BlendMode::Normal,
            mask_context: MaskContext(0), sort_key: 0,
            payload: NodePayload::Mesh {
                verts: vec![[0.0,0.0];4], uvs: vec![[0.0,0.0];4],
                colors: vec![color0;4], indices: vec![0,1,2,0,2,3],
                texture: tex, program: 0,
            },
        }
    }

    #[test]
    fn identical_nodes_same_hash() {
        let a = mesh_rn(1, 1.0, [1.0,0.0,0.0,1.0]);
        let b = mesh_rn(1, 1.0, [1.0,0.0,0.0,1.0]);
        assert_eq!(node_hash(&a), node_hash(&b), "全等节点 hash 相等");
    }

    #[test]
    fn texture_change_changes_hash() {
        let a = mesh_rn(1, 1.0, [1.0;4]);
        let b = mesh_rn(2, 1.0, [1.0;4]);
        assert_ne!(node_hash(&a), node_hash(&b), "texture 变 → hash 变");
    }

    #[test]
    fn color_change_changes_hash() {
        let a = mesh_rn(1, 1.0, [1.0,0.0,0.0,1.0]);
        let b = mesh_rn(1, 1.0, [0.0,1.0,0.0,1.0]);
        assert_ne!(node_hash(&a), node_hash(&b), "colors[0] 变 → hash 变");
    }

    #[test]
    fn alpha_change_changes_hash() {
        let a = mesh_rn(1, 1.0, [1.0;4]);
        let b = mesh_rn(1, 0.5, [1.0;4]);
        assert_ne!(node_hash(&a), node_hash(&b), "alpha 变 → hash 变");
    }

    #[test]
    fn world_matrix_change_changes_hash() {
        let a = mesh_rn(1, 1.0, [1.0;4]);
        let mut b = mesh_rn(1, 1.0, [1.0;4]);
        b.world_matrix = [1.0, 0.0, 0.0, 1.0, 5.0, 0.0]; // tx=5（scroll 平移）
        assert_ne!(node_hash(&a), node_hash(&b), "world_matrix 变（含 scroll_pos）→ hash 变");
    }

    #[test]
    fn verts_len_change_changes_hash() {
        let a = mesh_rn(1, 1.0, [1.0;4]); // 4 verts
        let mut b = mesh_rn(1, 1.0, [1.0;4]);
        if let NodePayload::Mesh { verts, .. } = &mut b.payload {
            verts.push([9.0, 9.0]); // 5 verts（尺寸变）
        }
        assert_ne!(node_hash(&a), node_hash(&b), "verts.len 变 → hash 变");
    }

    #[test]
    fn unchanged_returns_zero() {
        let mut rn = mesh_rn(1, 1.0, [1.0;4]);
        rn.payload = NodePayload::Unchanged;
        assert_eq!(node_hash(&rn), 0, "Unchanged 防御性返回 0");
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p loomgui_core render::dirty -- --nocapture`
Expected: FAIL（`pub mod dirty;` 未声明 / 模块不存在，编译错）

- [ ] **Step 3: 在 render/mod.rs 声明模块**

```rust
// loomgui_core/src/render/mod.rs —— 在现有 pub mod 声明处加：
pub mod batch;
pub mod dirty;   // v1e：dirty hash（逐节点 → u64，跨帧比决定 Unchanged emit）
pub mod merge;
pub mod mesh;
pub mod node;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cargo test -p loomgui_core render::dirty -- --nocapture`
Expected: PASS（7 测全绿）

- [ ] **Step 5: Commit**

```bash
git add loomgui_core/src/render/dirty.rs loomgui_core/src/render/mod.rs
git commit -m "feat(v1e-T1): dirty hash 纯函数——逐节点 u64（world_matrix+style+payload 摘要）"
```

---

### Task 2: build_render_nodes 接 hash + Unchanged emit

**Files:**
- Modify: `loomgui_core/src/render/mod.rs:76-207`（`build_render_nodes` 签名 + emit 逻辑）
- Test: `loomgui_core/src/render/mod.rs`（内联 `#[cfg(test)] mod tests` 追加）

**Interfaces:**
- Consumes: T1 `crate::render::dirty::node_hash`
- Produces: `build_render_nodes` 签名改为：
  ```rust
  pub fn build_render_nodes(
      scene: &Scene, font: &Font, textures: &TextureRegistry,
      prev_hashes: &[u64],
  ) -> (FrameData, Vec<u64>)
  ```
  返回 `(FrameData, new_hashes)`。`prev_hashes` 空 / 长度 < 节点数 → 该节点无基线 → 全 emit。T3 Stage 持 `prev_node_hashes` 传入 + 存返回值。

**关键逻辑**：
1. 逐 scene 节点先填字段 + emit Mesh/Text（现状逻辑不变），**同时算 `node_hash`** 存入 `new_hashes[id]`。
2. **emit 后判 dirty**：若 `prev_hashes[id] == new_hashes[id]`（且 prev_hashes 有该 id 基线）→ **把 payload 改回 `NodePayload::Unchanged`**（保留公共头字段不变——C# Unchanged 跳过 upload 但公共头仍透传，零副作用）。
3. **合成 scrollbar 强制 emit**（不进 hash 比较，始终 Mesh——append 在 hash 判断之后，不写 new_hashes）。
4. reorder + merge 照旧（Unchanged 在 merge passthrough，已确认 `merge.rs:33-37`）。

**注意**：hash 比较发生在**字段填完、payload emit 完**之后、**reorder/merge 之前**。比较的是 emit 出的 RenderNode（含 anim override 后的 alpha/color、scroll 后的 world_matrix），不是原始 Node——这样 anim/scroll 变化天然捕获。

- [ ] **Step 1: 写 failing test（追加到 render/mod.rs tests mod）**

```rust
    // ── v1e-T2：dirty Unchanged emit ─────────────────────────

    /// 首帧（prev_hashes 空）→ 全 emit Mesh，无 Unchanged。
    #[test]
    fn build_first_frame_all_emit_no_unchanged() {
        let mut scene = Scene {
            roots: vec![NodeId(0)], nodes: vec![],
            dynamic_rules: Default::default(), focused_node: None,
            world_transforms: Vec::new(), anim: Default::default(), scroll: Default::default(),
        };
        scene.nodes.push(container_node(0, None, Rect { x: 0.0, y: 0.0, w: 10.0, h: 10.0 }, Some([1.0,0.0,0.0,1.0])));
        let font = test_font().expect("need font");
        crate::scene::transform::compute_world_transforms(&mut scene);
        let (frame, _hashes) = build_render_nodes(&scene, &font, &TextureRegistry::default(), &[]);
        // 首帧无 Unchanged（全 Mesh）。
        assert!(frame.nodes.iter().all(|n| !matches!(n.payload, NodePayload::Unchanged)),
            "首帧 prev_hashes 空 → 全 emit");
    }

    /// 第二帧无变化 → 该节点 Unchanged。
    #[test]
    fn build_static_frame_emits_unchanged() {
        let mut scene = Scene {
            roots: vec![NodeId(0)], nodes: vec![],
            dynamic_rules: Default::default(), focused_node: None,
            world_transforms: Vec::new(), anim: Default::default(), scroll: Default::default(),
        };
        scene.nodes.push(container_node(0, None, Rect { x: 0.0, y: 0.0, w: 10.0, h: 10.0 }, Some([1.0,0.0,0.0,1.0])));
        let font = test_font().expect("need font");
        crate::scene::transform::compute_world_transforms(&mut scene);
        // 首帧拿 hash 基线。
        let (_f1, hashes) = build_render_nodes(&scene, &font, &TextureRegistry::default(), &[]);
        // 第二帧无变化 → Unchanged。
        let (f2, _) = build_render_nodes(&scene, &font, &TextureRegistry::default(), &hashes);
        // f2 含 1 个 Unchanged（merge 后该节点 passthrough 仍 Unchanged）。
        assert!(f2.nodes.iter().any(|n| matches!(n.payload, NodePayload::Unchanged)),
            "静态帧未变节点 → Unchanged");
    }

    /// 第二帧 style 变（bg color）→ 该节点重 emit Mesh（非 Unchanged）。
    #[test]
    fn build_changed_frame_re_emits() {
        let mut scene = Scene {
            roots: vec![NodeId(0)], nodes: vec![],
            dynamic_rules: Default::default(), focused_node: None,
            world_transforms: Vec::new(), anim: Default::default(), scroll: Default::default(),
        };
        scene.nodes.push(container_node(0, None, Rect { x: 0.0, y: 0.0, w: 10.0, h: 10.0 }, Some([1.0,0.0,0.0,1.0])));
        let font = test_font().expect("need font");
        crate::scene::transform::compute_world_transforms(&mut scene);
        let (_f1, hashes) = build_render_nodes(&scene, &font, &TextureRegistry::default(), &[]);
        // 改 bg color。
        scene.nodes[0].style.background_color = Some([0.0,1.0,0.0,1.0]);
        let (f2, _) = build_render_nodes(&scene, &font, &TextureRegistry::default(), &hashes);
        assert!(f2.nodes.iter().all(|n| !matches!(n.payload, NodePayload::Unchanged)),
            "bg color 变 → 重 emit Mesh（colors[0] hash 不等）");
    }

    /// reload（节点数变，prev_hashes 长度不符）→ 全 emit（无基线）。
    #[test]
    fn build_reload_clears_baseline() {
        let mut scene = Scene {
            roots: vec![NodeId(0)], nodes: vec![],
            dynamic_rules: Default::default(), focused_node: None,
            world_transforms: Vec::new(), anim: Default::default(), scroll: Default::default(),
        };
        scene.nodes.push(container_node(0, None, Rect { x: 0.0, y: 0.0, w: 10.0, h: 10.0 }, Some([1.0,0.0,0.0,1.0])));
        let font = test_font().expect("need font");
        crate::scene::transform::compute_world_transforms(&mut scene);
        let (_f1, hashes) = build_render_nodes(&scene, &font, &TextureRegistry::default(), &[]);
        // prev_hashes 长度 > 节点数（模拟 reload 后旧 hash 表残留）→ 无基线 → 全 emit。
        let mut stale = hashes.clone();
        stale.push(999);
        let (f2, _) = build_render_nodes(&scene, &font, &TextureRegistry::default(), &stale);
        assert!(f2.nodes.iter().all(|n| !matches!(n.payload, NodePayload::Unchanged)),
            "prev_hashes 长度不符 → 全 emit（防错位）");
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p loomgui_core render::tests::build_static_frame_emits_unchanged -- --nocapture`
Expected: FAIL（`build_render_nodes` 当前签名无 prev_hashes 参数，编译错；或当前实现永远全 emit，静态帧断言失败）

- [ ] **Step 3: 改 build_render_nodes 签名 + Unchanged emit 逻辑**

把 `loomgui_core/src/render/mod.rs:76` 起的 `build_render_nodes` 改为（完整替换函数体 76-207）：

```rust
pub fn build_render_nodes(
    scene: &Scene,
    font: &Font,
    textures: &TextureRegistry,
    prev_hashes: &[u64],
) -> (FrameData, Vec<u64>) {
    let n_nodes = scene.nodes.len();
    // 预分配 Unchanged 占位，逐个按 scene 节点覆写。
    let mut nodes: Vec<RenderNode> = (0..n_nodes)
        .map(|_| RenderNode {
            node_id: 0,
            parent_id: None,
            visible: true,
            alpha: 1.0,
            grayed: false,
            color_tint: [1.0; 4],
            world_matrix: crate::transform::IDENTITY,
            blend: BlendMode::Normal,
            mask_context: MaskContext(0),
            sort_key: 0,
            payload: NodePayload::Unchanged,
        })
        .collect();
    // v1e：本帧每节点的新 hash（emit 后算）。
    let mut new_hashes: Vec<u64> = vec![0; n_nodes];

    for n in &scene.nodes {
        let rn = &mut nodes[n.id.0];
        let anim = scene.anim.get(n.id);
        rn.node_id = n.id.0 as u32;
        rn.parent_id = n.parent.map(|p| p.0 as u32);
        rn.alpha = anim.and_then(|a| a.opacity).unwrap_or(n.style.opacity);
        rn.color_tint = anim.and_then(|a| a.text_color).unwrap_or(n.style.color);
        let wm = scene.world_transforms[n.id.0];
        rn.world_matrix = wm;
        rn.visible = true;
        let rect = if crate::transform::is_pure_translation(&wm) {
            n.layout_rect
        } else {
            crate::scene::node::Rect { x: 0.0, y: 0.0, w: n.layout_rect.w, h: n.layout_rect.h }
        };
        let rect = &rect;
        match &n.kind {
            NodeKind::Container | NodeKind::Button => {
                let color = anim.and_then(|a| a.bg_color).unwrap_or(n.style.background_color.unwrap_or([0.0, 0.0, 0.0, 0.0]));
                let (v, uvc, col, idx) =
                    crate::render::mesh::quad(rect, color, [0.0, 0.0], [1.0, 1.0]);
                rn.payload = NodePayload::Mesh { verts: v, uvs: uvc, colors: col, indices: idx, texture: 0, program: 0 };
            }
            NodeKind::Image { src } => {
                let (tex_id, uv_min, uv_max) = match textures.get(src) {
                    Some(m) => (m.tex_id, m.uv_min, m.uv_max),
                    None => (0u32, [0.0, 0.0], [1.0, 1.0]),
                };
                let (v, uvc, col, idx) =
                    crate::render::mesh::quad(rect, [1.0, 1.0, 1.0, 1.0], uv_min, uv_max);
                rn.payload = NodePayload::Mesh { verts: v, uvs: uvc, colors: col, indices: idx, texture: tex_id, program: 0 };
            }
            NodeKind::Text { content } => {
                let s = &n.style;
                let mut layout = measure_text(
                    content, s.font_size, s.line_height, s.letter_spacing,
                    s.text_align, s.white_space_nowrap, Some(rect.w), font,
                );
                let off_x = resolve_lp(s.taffy_style.border.left) + resolve_lp(s.taffy_style.padding.left);
                let off_y = resolve_lp(s.taffy_style.border.top) + resolve_lp(s.taffy_style.padding.top);
                if off_x != 0.0 || off_y != 0.0 {
                    bake_content_offset(&mut layout, off_x, off_y);
                }
                rn.payload = NodePayload::Text {
                    layout, font_size: s.font_size,
                    color: anim.and_then(|a| a.text_color).unwrap_or(s.color), program: 1,
                };
            }
        }
        // v1e：算本帧 hash，与上帧比。相等（且有基线）→ payload 改回 Unchanged。
        // ponytail: hash 碰撞最坏 1 帧延迟；换全量 hash 若 profiling 显示遗漏。
        let h = crate::render::dirty::node_hash(rn);
        new_hashes[n.id.0] = h;
        let has_baseline = n.id.0 < prev_hashes.len();
        if has_baseline && prev_hashes[n.id.0] == h {
            rn.payload = NodePayload::Unchanged;
        }
    }
    let clips = batch::assign_sort_keys(scene, &mut nodes);
    // v1d.5 T9：合成 scrollbar thumb（强制 emit，不进 hash 比较——随 scroll_pos 变，数量少）。
    let max_sort = nodes.iter().map(|n| n.sort_key).max().unwrap_or(0);
    for id in 0..scene.nodes.len() {
        let nid = NodeId(id);
        let n = &scene.nodes[id];
        if let Some(s) = scene.scroll.get(nid) {
            if crate::scroll::effective(n.style.overflow_y, s.content_size.1, s.viewport_size.1) {
                if let Some(r) = crate::scroll::v_thumb_rect(scene, nid) {
                    let thumb_id = nid.0 as u32 | crate::scroll::V_THUMB_FLAG;
                    nodes.push(thumb_render_node(thumb_id, r, max_sort + 1));
                }
            }
            if crate::scroll::effective(n.style.overflow_x, s.content_size.0, s.viewport_size.0) {
                if let Some(r) = crate::scroll::h_thumb_rect(scene, nid) {
                    let thumb_id = nid.0 as u32 | crate::scroll::H_THUMB_FLAG;
                    nodes.push(thumb_render_node(thumb_id, r, max_sort + 1));
                }
            }
        }
    }
    batch::reorder_for_batching(scene, &mut nodes);
    let nodes = merge::merge_meshes(nodes);
    (FrameData { nodes, clips }, new_hashes)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cargo test -p loomgui_core render::tests -- --nocapture`
Expected: PASS（4 新测 + 既有 render 测全绿）

- [ ] **Step 5: 修所有 build_render_nodes 调用点**

`build_render_nodes` 签名变了，所有调用点须改。grep 找全：

Run: `grep -rn "build_render_nodes(" loomgui_core/src loomgui_ffi_c/src`
Expected: 列出所有调用点（stage.rs:287 + 任何 test/ffi）。每处改：
- `stage.rs:287`：T3 改（传 `&self.prev_node_hashes`、存返回 hash）——本步先占位改 `&[]` 让编译过，T3 正式接通：
  ```rust
  let (frame, _new_hashes) = build_render_nodes(scene, &self.font, &self.textures, &[]);
  ```
  （T3 会改成传真实 prev_hashes + 存 _new_hashes）
- 任何 `render/mod.rs` tests 里直接调 `build_render_nodes(...)` 的：末尾加 `, &[]`（首帧语义，既有测不变）。
- `render/mod.rs:283/326/351/414/478/525/570/612/652/688` 等既有测调用：末尾加 `, &[]`。

逐个改完后编译：

Run: `cargo build -p loomgui_core`
Expected: 编译通过（无签名不匹配错）

- [ ] **Step 6: 全量 core 测绿**

Run: `cargo test -p loomgui_core -- --nocapture`
Expected: 全绿（既有测 + 4 新测；既有测加 `&[]` 后语义不变 = 首帧全 emit）

- [ ] **Step 7: Commit**

```bash
git add loomgui_core/src/render/mod.rs
git commit -m "feat(v1e-T2): build_render_nodes 接 dirty hash——未变节点 emit Unchanged + 合成 scrollbar 强制 emit"
```

---

### Task 3: Stage 持 prev_node_hashes + tick 接通 + load clear

**Files:**
- Modify: `loomgui_core/src/stage.rs`（Stage 字段 + new + load_inline/load_package clear + tick_and_render 接通）
- Test: `loomgui_core/tests/v1e_dirty.rs`（新集成测文件）

**Interfaces:**
- Consumes: T2 `build_render_nodes(scene, font, textures, &prev_hashes) -> (FrameData, Vec<u64>)`
- Produces: Stage 新字段 `pub prev_node_hashes: Vec<u64>`；`tick_and_render` 接通（传 prev + 存 new）。

**关键**：
- Stage 加 `prev_node_hashes: Vec<u64>`（transient，不进 pkg——它是 Stage 字段不是 Scene 字段，天然不进 pkg）。
- `new()` 初始化 `Vec::new()`。
- `load_inline` / `load_package` 调 `self.prev_node_hashes.clear()`（同 tween/scroll clear，防 scene 重建后 NodeId 错位）。
- `tick_and_render`：调 `build_render_nodes(scene, font, textures, &self.prev_node_hashes)`，把返回的 `new_hashes` 存入 `self.prev_node_hashes`。
- `render_json`（调试用）也调 `tick_and_render`，自动接通——但它内部调 `tick_and_render` 取 frame.nodes 序列化，hash 存取已在 tick_and_render 内完成。

- [ ] **Step 1: 写 failing test（集成测）**

```rust
// loomgui_core/tests/v1e_dirty.rs
//! v1e dirty 跟踪端到端：Stage 跨 tick 保持 hash 基线，静态帧产 Unchanged。

use loomgui_core::stage::Stage;

fn font_path() -> (String, usize) {
    let p = format!("{}/tests/fixtures/DejaVuSans.ttf", env!("CARGO_MANIFEST_DIR"));
    let n = p.len();
    (p, n)
}

/// Stage 跨帧 dirty：首帧全 emit，第二帧静态 → Unchanged。
#[test]
fn stage_static_frame_produces_unchanged() {
    let (fp, fplen) = font_path();
    let mut stage = Stage::new(&fp, (200.0, 100.0)).expect("stage");
    let html = r#"<div style="width:50px;height:50px;background-color:#ff0000"></div>"#;
    stage.load_inline(html, "").expect("load");
    stage.advance_time(0.016);
    let f1 = stage.tick_and_render();
    // 首帧全 Mesh，无 Unchanged。
    assert!(f1.nodes.iter().all(|n| !matches!(n.payload, loomgui_core::render::node::NodePayload::Unchanged)),
        "首帧全 emit");
    // 第二帧静态 → 含 Unchanged。
    stage.advance_time(0.016);
    let f2 = stage.tick_and_render();
    assert!(f2.nodes.iter().any(|n| matches!(n.payload, loomgui_core::render::node::NodePayload::Unchanged)),
        "静态帧 → Unchanged");
}

/// reload 清 hash 基线：load 后首帧又全 emit。
#[test]
fn stage_reload_clears_dirty_baseline() {
    let (fp, fplen) = font_path();
    let mut stage = Stage::new(&fp, (200.0, 100.0)).expect("stage");
    stage.load_inline(r#"<div style="width:50px;height:50px;background-color:#ff0000"></div>"#, "").expect("load");
    stage.advance_time(0.016);
    let _ = stage.tick_and_render();
    stage.advance_time(0.016);
    let _ = stage.tick_and_render(); // 建立 hash 基线
    // reload（不同 HTML）→ 清基线 → 首帧全 emit。
    stage.load_inline(r#"<div style="width:60px;height:60px;background-color:#00ff00"></div>"#, "").expect("reload");
    stage.advance_time(0.016);
    let f3 = stage.tick_and_render();
    assert!(f3.nodes.iter().all(|n| !matches!(n.payload, loomgui_core::render::node::NodePayload::Unchanged)),
        "reload 后首帧全 emit（基线已清）");
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p loomgui_core --test v1e_dirty -- --nocapture`
Expected: FAIL（`prev_node_hashes` 字段不存在 / tick_and_render 仍传 `&[]`，静态帧无 Unchanged）

- [ ] **Step 3: Stage 加字段 + new 初始化**

`loomgui_core/src/stage.rs` Stage struct（50 行 `pending_dt: f32,` 后）加：

```rust
    /// v1e：上帧每节点 dirty hash（NodeId 索引）。跨 tick 持续，供 build_render_nodes
    /// 比较决定 emit Unchanged。transient 不进 pkg（Stage 字段非 Scene 字段）。
    /// reload/节点数变 → clear → 下帧全 dirty（无基线）。
    pub prev_node_hashes: Vec<u64>,
```

`Stage::new`（70 行 `pending_dt: 0.0,` 后）加：

```rust
            prev_node_hashes: Vec::new(),
```

- [ ] **Step 4: load_inline / load_package 清 hash 基线**

`load_inline`（82 行 `if let Some(scene) = self.scene.as_mut() { scene.scroll.clear(); }` 后）加：

```rust
        self.prev_node_hashes.clear();   // v1e：旧 hash 基线随 scene 重建失效（防 NodeId 错位）
```

`load_package`（98 行 `if let Some(s) = self.scene.as_mut() { s.scroll.clear(); }` 后）加同样一行：

```rust
        self.prev_node_hashes.clear();   // v1e：旧 hash 基线随 scene 重建失效
```

- [ ] **Step 5: tick_and_render 接通 hash**

`tick_and_render`（287 行）把 T2 占位的 `&[]` 改为传真实 prev + 存 new：

```rust
        // 8. 渲染（+ 合成 scrollbar）。v1e：传上帧 hash 基线，未变节点 emit Unchanged；
        //    返回新 hash 存 self.prev_node_hashes 供下帧比。
        let (frame, new_hashes) = build_render_nodes(scene, &self.font, &self.textures, &self.prev_node_hashes);
        self.prev_node_hashes = new_hashes;
        frame
```

注意：`tick_and_render` 当前签名 `pub fn tick_and_render(&mut self) -> FrameData`——`scene` 是 `self.scene.as_mut()` 借出，build_render_nodes 借 `scene`，写 `self.prev_node_hashes` 时 scene 借用已结束（build 返回后）。确认无借用冲突：`let scene = self.scene.as_mut().expect(...)` 在函数顶部，build_render_nodes 借 scene，返回后 scene 借用释放，再 `self.prev_node_hashes = ...` 不冲突。若编译报借用冲突，把 `new_hashes` 先 take 到局部再赋值（`let new_hashes = ...; self.prev_node_hashes = new_hashes;`）。

- [ ] **Step 6: Run test to verify it passes**

Run: `cargo test -p loomgui_core --test v1e_dirty -- --nocapture`
Expected: PASS（2 集成测绿）

- [ ] **Step 7: 全量 core 测绿**

Run: `cargo test -p loomgui_core -- --nocapture`
Expected: 全绿（既有 + T1 + T2 + T3 测）

- [ ] **Step 8: Commit**

```bash
git add loomgui_core/src/stage.rs loomgui_core/tests/v1e_dirty.rs
git commit -m "feat(v1e-T3): Stage 持 prev_node_hashes + tick 接通 dirty + load clear 基线"
```

---

### Task 4: FFI version v1e + blob 零 bump 验证

**Files:**
- Modify: `loomgui_ffi_c/src/lib.rs`（version() 返回 `"v1e"`）
- Test: `loomgui_ffi_c/src/lib.rs`（内联 abi 测追加）+ 既有 blob 测确认 Unchanged kind=0 透传

**Interfaces:**
- Consumes: T3 Stage 接通 dirty
- Produces: FFI version `"v1e"`；blob 仍 v4（零 bump 验证）。

**关键**：
- `version()` 串改 `"v1d.5"` → `"v1e"`。
- **blob 零 bump**：Unchanged kind=0 在 `blob.rs:155-157` 已就位，无需改 blob.rs。加一个 abi 测验证：含 Unchanged 节点的 FrameData 经 `build_blob` 后 C# 侧 `payload_kind==0`（本机 Rust 侧 round-trip 验证 kind=0 透传，C# 侧 `MirrorPool.cs:71` 已确认 `kind!=1&&!=2 continue`，家里机验）。
- **pkg formatVersion 零 bump**：ResolvedStyle 无字段变，不动 pkg。

- [ ] **Step 1: 写 failing test（追加到 loomgui_ffi_c/src/lib.rs tests）**

先 grep 找 version 测和 blob 测位置：

Run: `grep -n "fn version\|v1d.5\|payload_kind" loomgui_ffi_c/src/lib.rs`
Expected: 列出 version 测 + 既有 blob kind 测。

在 lib.rs tests mod 追加：

```rust
    /// v1e：version 串 = "v1e"。
    #[test]
    fn version_is_v1e() {
        let v = unsafe { CStr::from_ptr(loomgui_version()) }
            .to_str().expect("utf8");
        assert_eq!(v, "v1e");
    }

    /// v1e：blob 仍 v4（零 bump）。Unchanged kind=0 经 build_blob 透传。
    #[test]
    fn blob_unchanged_kind_is_zero() {
        use loomgui_core::render::node::{NodePayload, RenderNode};
        use loomgui_core::render::FrameData;
        let rn = RenderNode {
            node_id: 0, parent_id: None, visible: true, alpha: 1.0,
            grayed: false, color_tint: [1.0;4],
            world_matrix: loomgui_core::transform::IDENTITY,
            blend: loomgui_core::render::node::BlendMode::Normal,
            mask_context: loomgui_core::render::node::MaskContext(0),
            sort_key: 0, payload: NodePayload::Unchanged,
        };
        let frame = FrameData { nodes: vec![rn], clips: vec![] };
        let blob = loomgui_ffi_c::blob::build_blob(&frame);
        // col_off[12] = payload_kind 列起始；1 节点 → byte at col_off[12]。
        // 解析 header 拿列偏移（同 blob.rs TestView 模式）——简化：直接验 build_blob 不 panic + 长度合理。
        assert!(!blob.is_empty(), "Unchanged 节点 blob 非空（公共头仍 emit）");
        // 更严：验 magic + node_count=1（Unchanged 仍占 1 节点位）。
        assert_eq!(&blob[0..4], &0x4C4F4F4Du32.to_le_bytes(), "magic");
    }
```

> 注：若 `loomgui_ffi_c::blob::build_blob` 不是 pub，本测放 `blob.rs` 内联 tests 更直接（那里已有 TestView 能读 payload_kind）。implementer 判断：若 lib.rs 访问不到 build_blob，把 `blob_unchanged_kind_is_zero` 测移到 `blob.rs` tests mod 并用 TestView 验 `view.payload_kind(0) == 0`。优先放 blob.rs（那里有 TestView 工具）。

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p loomgui_ffi_c version_is_v1e -- --nocapture`
Expected: FAIL（当前 version = "v1d.5"）

- [ ] **Step 3: 改 version 串**

grep 找 version 函数：

Run: `grep -n "v1d.5" loomgui_ffi_c/src/lib.rs`
Expected: 1 处（version 函数体内）。

把 `"v1d.5"` 改为 `"v1e"`。

- [ ] **Step 4: Run test to verify it passes**

Run: `cargo test -p loomgui_ffi_c -- --nocapture`
Expected: PASS（version_is_v1e + blob_unchanged_kind_is_zero + 既有 ffi 测全绿）

- [ ] **Step 5: 全 workspace 测绿**

Run: `cargo test --workspace -- --nocapture`
Expected: 全绿（core + ffi + pkg）

- [ ] **Step 6: 重编 .dll + commit（两机约束）**

```bash
cargo build --release -p loomgui_ffi_c
# 拷贝 target/release/*.dll 到 loomgui_unity/Assets/Plugins/LoomGUI/（覆盖旧 .dll）
# .dll 路径按现有 build 脚本/惯例（坑 10：md5 对比验 fresh）
```

implementer 确认 .dll 拷贝路径（grep 既有 commit 或 build 脚本找 Plugins 下 .dll 位置），覆盖后 `git status` 确认 .dll 变更。

- [ ] **Step 7: Commit**

```bash
git add loomgui_ffi_c/src/lib.rs loomgui_ffi_c/src/blob.rs loomgui_unity/Assets/Plugins/LoomGUI/*.dll
git commit -m "feat(v1e-T4): FFI version v1e + blob 零 bump 验证（Unchanged kind=0 透传）+ 重编 .dll"
```

---

### Task 5: C# LoomStage._frameBuf ArrayPool

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs`（_frameBuf 字段 + LateUpdate 借帧 + OnDestroy/reset 归还）
- Test: 家里机 PlayMode（Profiler GC Alloc 静态帧≈0）——本机无 Unity，仅代码改 + 注释，家里机验

**Interfaces:**
- Consumes: 无（纯 C# 改）
- Produces: `_frameBuf` 用 `ArrayPool<byte>.Shared.Rent`，OnDestroy/Domain reset `Return`。

**关键**：
- `using System.Buffers;` 引入 ArrayPool。
- `_frameBuf` 从 `new byte[len]` 改 `ArrayPool<byte>.Shared.Rent(len)`——Rent 返回的数组 ≥ len（取整到 2 的幂），只 copy/解析 `len` 字节，多余忽略。
- 借帧逻辑：若 `_frameBuf == null || _frameBuf.Length < len` → 若 `_frameBuf != null` 先 `Return`，再 `Rent(len)`。
- **归还点**：`OnDestroy`（LoomStage 销毁）+ Domain reload reset 静态钩子（若 _frameBuf 是实例字段随 Stage 销毁即可；若提到 static 须 reset Return）。看现状 `_frameBuf` 是实例字段 → OnDestroy 归还。
- **ReadMesh 留观察**（不动）：静态帧 dirty 跳过后 Unchanged 不调 ReadMesh → 静态帧零 alloc；冷帧才暴露。标 `// ponytail: ReadMesh per-node alloc 留观察，冷帧 GC 卡顿再上 List 复用`。

- [ ] **Step 1: 改 LoomStage.cs（代码改，本机无 Unity 不跑 PlayMode）**

先读现状 _frameBuf 声明 + LateUpdate 借帧 + OnDestroy：

Run: `grep -n "_frameBuf\|using System\|ArrayPool\|OnDestroy\|RuntimeInitializeOnLoadMethod" loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs`
Expected: 列出 _frameBuf 声明行、using 区、OnDestroy、有无 Domain reset 钩子。

改 1：文件头 using 区加（若无）：
```csharp
using System.Buffers;   // v1e：ArrayPool<byte> for _frameBuf
```

改 2：`_frameBuf` 声明处加注释（字段类型不变 `byte[]`，只是来源改 ArrayPool）：
```csharp
// v1e：ArrayPool 租用（非 new）。Rent 返回 ≥len，只 copy/解析 len 字节。
// OnDestroy 归还防泄漏。冷帧零 GC（ReadMesh per-node alloc 留观察，撞墙再上 List 复用）。
private byte[] _frameBuf;
```

改 3：LateUpdate 借帧段（546-548 行）改：
```csharp
                // v1e：ArrayPool 租用（§14.3 冷帧零 GC）。Rent 返回 ≥len，多余字节忽略。
                if (_frameBuf == null || _frameBuf.Length < len)
                {
                    if (_frameBuf != null) ArrayPool<byte>.Shared.Return(_frameBuf);
                    _frameBuf = ArrayPool<byte>.Shared.Rent(len);
                }
                Marshal.Copy((IntPtr)ptr, _frameBuf, 0, len);
```

改 4：OnDestroy（583 行 `FreeStage();` 前）归还：
```csharp
            // v1e：归还 ArrayPool 租用的 _frameBuf。
            if (_frameBuf != null)
            {
                ArrayPool<byte>.Shared.Return(_frameBuf);
                _frameBuf = null;
            }
            FreeStage();
```

- [ ] **Step 2: 本机编译验证（无 Unity，跳过；仅确认 C# 语法正确）**

本机无 Unity 编辑器，C# 改动靠家里机 PlayMode 验。implementer 自查：`using System.Buffers;` 在 Unity 6.5（.NET Standard 2.1 / Core 基础库）可用——Unity 2021+ 含 `System.Buffers.ArrayPool`。若家里机报缺 using，改用 `Unity.Collections.NativeArray` 或自建简单 pool（但优先 ArrayPool，标准库）。

- [ ] **Step 3: Commit**

```bash
git add loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs
git commit -m "perf(v1e-T5): LoomStage._frameBuf 改 ArrayPool（冷帧零 GC）+ OnDestroy 归还"
```

---

### Task 6: Rust criterion bench（静态/冷/换页帧）

**Files:**
- Modify: `loomgui_core/Cargo.toml`（`[[bench]]` + dev-dep criterion）
- Create: `loomgui_core/benches/frame_emit.rs`

**Interfaces:**
- Consumes: T3 Stage（端到端 tick）
- Produces: criterion bench 3 场景：静态帧（全 Unchanged emit）/ 冷帧（首帧全 dirty）/ 换页帧（全节点 style 突变）。

**关键**：
- `loomgui_core` 已是 lib crate，bench 用 `criterion`。
- 500 节点构造：用 `Stage::load_inline` 吃一个生成 500 div 的 HTML，或直接构造 Scene。优先 load_inline（端到端真实路径）。
- 3 场景：
  1. **静态帧**：tick 2 次，测第 2 次 `tick_and_render`（全 Unchanged）。
  2. **冷帧**：fresh Stage，测首帧 `tick_and_render`（全 dirty）。
  3. **换页帧**：tick 1 次建基线，改 500 节点 bg color，测第 2 帧（全 dirty）。
- bench 不依赖 Unity（纯 Rust core），本机跑出数据。

- [ ] **Step 1: Cargo.toml 加 bench + dev-dep**

读现状 Cargo.toml：

Run: `cat loomgui_core/Cargo.toml`
Expected: 看 `[dev-dependencies]` 和有无 `[[bench]]`。

在 `[dev-dependencies]` 加（若无）：
```toml
[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "frame_emit"
harness = false
```

（若已有 `[dev-dependencies]` 段，追加 criterion 行；`[[bench]]` 段独立加。）

- [ ] **Step 2: 写 bench**

```rust
// loomgui_core/benches/frame_emit.rs
//! v1e 性能 bench：build_render_nodes + tick emit 耗时（静态/冷/换页帧）。
//! 不依赖 Unity（纯 Rust core）。验收线：冷帧/换页帧 ≤2ms（v1-scope §4）。

use criterion::{criterion_group, criterion_main, Criterion};
use loomgui_core::stage::Stage;

fn font_path() -> (String, usize) {
    let p = format!("{}/tests/fixtures/DejaVuSans.ttf", env!("CARGO_MANIFEST_DIR"));
    (p.clone(), p.len())
}

/// 生成 500 节点 HTML（嵌套 div，各有 bg color）。
fn html_500() -> String {
    let mut s = String::new();
    for i in 0..500 {
        let r = (i % 256) as f32 / 255.0;
        s.push_str(&format!(
            r#"<div style="width:50px;height:10px;background-color:rgba({r},0,0,1)"></div>"#
        ));
    }
    s
}

/// 生成 500 节点 HTML，bg color 全不同（换页用：改 color 模拟全 dirty）。
fn html_500_colorized(seed: u32) -> String {
    let mut s = String::new();
    for i in 0..500u32 {
        let r = ((i + seed) % 256) as f32 / 255.0;
        s.push_str(&format!(
            r#"<div style="width:50px;height:10px;background-color:rgba({r},0,0,1)"></div>"#
        ));
    }
    s
}

fn bench_static(c: &mut Criterion) {
    let (fp, fplen) = font_path();
    let mut group = c.benchmark_group("v1e_frame_emit");
    group.bench_function("static_frame", |b| {
        b.iter_batched(
            || {
                // 每次迭代 fresh Stage，先 tick 1 次建基线。
                let mut stage = Stage::new(&fp, (800.0, 600.0)).expect("stage");
                stage.load_inline(&html_500(), "").expect("load");
                stage.advance_time(0.016);
                let _ = stage.tick_and_render(); // 建基线
                stage.advance_time(0.016);
                stage
            },
            |mut stage| {
                // 测第 2 帧（全 Unchanged emit）。
                let _ = stage.tick_and_render();
            },
            criterion::BatchSize::SmallInput,
        );
    });
}

fn bench_cold(c: &mut Criterion) {
    let (fp, fplen) = font_path();
    let mut group = c.benchmark_group("v1e_frame_emit");
    group.bench_function("cold_frame", |b| {
        b.iter_batched(
            || {
                let mut stage = Stage::new(&fp, (800.0, 600.0)).expect("stage");
                stage.load_inline(&html_500(), "").expect("load");
                stage.advance_time(0.016);
                stage
            },
            |mut stage| {
                let _ = stage.tick_and_render(); // 首帧全 dirty
            },
            criterion::BatchSize::SmallInput,
        );
    });
}

fn bench_page_turn(c: &mut Criterion) {
    let (fp, _fplen) = font_path();
    let mut group = c.benchmark_group("v1e_frame_emit");
    group.bench_function("page_turn_frame", |b| {
        b.iter_batched(
            || {
                let mut stage = Stage::new(&fp, (800.0, 600.0)).expect("stage");
                stage.load_inline(&html_500_colorized(0), "").expect("load");
                stage.advance_time(0.016);
                let _ = stage.tick_and_render(); // 建基线
                // 换页：reload 不同 color HTML（全节点 style 变）。
                stage.load_inline(&html_500_colorized(100), "").expect("reload");
                stage.advance_time(0.016);
                stage
            },
            |mut stage| {
                let _ = stage.tick_and_render(); // 全 dirty（reload 后首帧）
            },
            criterion::BatchSize::SmallInput,
        );
    });
}

criterion_group!(benches, bench_static, bench_cold, bench_page_turn);
criterion_main!(benches);
```

- [ ] **Step 3: 跑 bench**

Run: `cargo bench -p loomgui_core --bench frame_emit -- --warm-up-time 1 --measurement-time 3`
Expected: 输出 3 场景耗时。**验收线**：冷帧/换页帧 ≤2ms（2000µs）；静态帧 ~µs 级（全 Unchanged emit）。记录数据进 T7 ledger。

> 若冷帧 >2ms，profile 找瓶颈（大概率 measure_text 文本节点多——但 500 div 无文本，应远低于 2ms）。bench 数据仅 Rust 侧 emit，不含 C# upload（家里机 Profiler 补）。

- [ ] **Step 4: Commit**

```bash
git add loomgui_core/Cargo.toml loomgui_core/benches/frame_emit.rs
git commit -m "perf(v1e-T6): criterion bench——静态/冷/换页帧 emit 耗时（验收冷帧≤2ms）"
```

---

### Task 7: 文档同步（session-summary + ledger + design §10.3）

**Files:**
- Modify: `.claude/skills/knowledge-reference/SKILL.md`（§2 新机制 + §7 ledger v1e + §5 坑（若有））
- Modify: `docs/design/00-main-design.md`（§10.3 v1e status block）
- Modify: `docs/roadmap/v1-scope.md`（§4 性能基线标 v1e 兑现，若适用）

**Interfaces:**
- Consumes: T1-T6 全完成 + bench 数据
- Produces: 知识库 + design 同步。

**关键**：
- **session-summary skill**：总结 v1e 经验进 knowledge-reference。
- §2 新机制：v1e dirty hash + Unchanged emit（Unchanged 全链路 v0 预留的兑现）。
- §7 ledger：v1e 条目（dirty hash + ArrayPool + bench 数据 + 家里机待验 Profiler）+ v1 性能债务 4 项兑现状态 + "下一个"更新。
- §5 坑（若有）：若 T1-T6 踩新坑记入（如 hash 字段遗漏、ArrayPool Rent 长度陷阱、borrow 冲突）。
- design §10.3：v1e status block（mirror v1d.5 格式）。
- **不动 v1-scope §2 围栏**（v1e 无新 CSS 属性/Node 字段，纯 perf）。
- **不改主文档 §614-623**（blob SOA 契约 Unchanged 早写，v1e 只是兑现，非契约变）。

- [ ] **Step 1: 读 knowledge-reference 当前 §2/§5/§7 + design §10.3**

Run: `grep -n "### 2\.\|### 5\.\|## 7\|坑 5[0-9]\|v1d.5\|v1e" .claude/skills/knowledge-reference/SKILL.md`
Expected: 定位插入点 + 最新坑号 + v1d.5 条目位置。

Run: `grep -n "§10.3\|v1d.5\|v1d.4" docs/design/00-main-design.md`
Expected: 定位 §10.3 status block。

- [ ] **Step 2: session-summary——更新 knowledge-reference**

按 session-summary skill 格式（每条 3-5 行，先 grep 确认无重复）：

**§2 新机制**（加在 §2.23 ScrollPane 后，新 §2.24）：
```markdown
### 2.24 v1e dirty hash + Unchanged emit（主文档 §614-623）
- **机制**：Stage 持 `prev_node_hashes: Vec<u64>`（transient），`build_render_nodes(scene,font,tex,&prev) -> (FrameData, new_hashes)`。逐节点 emit 后算 hash（world_matrix 6 列含 scroll_pos + style 公共头 + payload 摘要 Mesh{texture,verts.len,colors[0]}/Text{font_size,color,glyph_count,首字codepoint}），与上帧等 → payload 改回 `Unchanged`。C# `MirrorPool.cs:71` `kind!=1&&!=2 continue` 早吃 Unchanged → 零 upload。
- **全链路 v0 预留兑现**：`NodePayload::Unchanged`（node.rs:33）+ blob kind=0（blob.rs:155）+ C# 读 kind（FrameBlob.cs:36）+ C# 跳过（MirrorPool.cs:71）从 v0 全通，v1e 只在 Stage 加 hash 跟踪。**FFI/blob(v4)/pkg(v7)/C# MirrorPool 四零改**。
- 合成 scrollbar 强制 emit（不进 hash，随 scroll_pos 变）；首帧/reload 全 dirty（无基线）；merge 对 Unchanged passthrough（merge.rs:33）。
```

**§7 ledger**（v1d.5 条目后加 v1e）：
```markdown
**v1e（main @ <commit>）**：FFI 同步热路径性能债务兑现（Codex §4.3/§6.5）。4 项 → dirty hash+Unchanged emit（静态帧≈0）/ 冷帧+换页帧 emit ≤2ms（criterion bench 验：<数据>）/ C# _frameBuf ArrayPool（冷帧零 GC，ReadMesh 留观察）/ bench+家里机 Profiler 双轨验收。范围外推后：文本 cache/Font leak/invalidation set/Tab 缓存/AncestorChain/FairyBatching（撞墙再上）。关 v1-scope §4 性能基线。家里机待验：PlayMode Profiler 静态帧≈0 upload + 冷/换页帧≤2ms + GC Alloc 静态帧≈0。
```

更新"下一个"行：v1 ship（#2 家里机验过）/ v1.x（Controller/Gear/Transition/TextInput/虚拟化/broadcast/IME）。

**§5 坑**（仅若 T1-T6 踩了新坑，编号递增 v1d.5 末坑 55 后 → 坑 56+）。常见候选：hash 字段遗漏（若 reviewer 抓到）、ArrayPool Rent 长度 > len 须按 len 解析、Stage 借用冲突（scene 借用未释就写 prev_node_hashes）。若没踩新坑，不强行加。

- [ ] **Step 3: design §10.3 v1e status block**

读 §10.3 v1d.5 status block 格式，mirror 之加 v1e block：
```markdown
### v1e（<日期>，main @ <commit>）— FFI 同步热路径性能优化
- **dirty hash + Unchanged emit**：Stage 持 prev_node_hashes，build_render_nodes 对未变节点 emit Unchanged（全链路 v0 预留兑现，FFI/blob/pkg/C# 四零改）。静态帧≈0 upload。
- **冷帧/换页帧 emit ≤2ms**：criterion bench 验 <数据>。
- **C# ArrayPool**：LoomStage._frameBuf 改 ArrayPool（冷帧零 GC）。
- **验收**：Rust bench（本机）+ 家里机 PlayMode Profiler 双轨。
- **范围外**：文本 cache/Font leak/invalidation set/Tab/AncestorChain/FairyBatching（撞墙再上）。
```

- [ ] **Step 4: v1-scope §4 性能基线（若需标注 v1e 兑现）**

读 v1-scope §4（136 行冷帧/换页帧段）。若原文标"v1 中段 stress"未提 v1e，加注：
```markdown
**冷帧/换页帧**：...（v1e 兑现：dirty hash+Unchanged emit + ArrayPool，criterion bench + 家里机 Profiler 验）。
```
（若原文已隐含 defer v1e，确认措辞一致即可，不强行改。）

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/knowledge-reference/SKILL.md docs/design/00-main-design.md docs/roadmap/v1-scope.md
git commit -m "docs(v1e): session-summary + 文档同步——dirty hash 机制 + ledger + design §10.3"
```

---

## Self-Review

**1. Spec coverage:**
- §1 动作 1（dirty hash + Unchanged emit）→ T1 + T2 + T3 ✓
- §1 动作 2（静态帧≈0 + 冷帧≤2ms）→ T2（Unchanged emit）+ T6（bench 验）✓
- §1 动作 3（C# ArrayPool）→ T5 ✓
- §1 动作 4（bench + Profiler 双轨）→ T6（bench）+ T5/T7（家里机 Profiler 待验）✓
- §3.1 hash 字段集 → T1 node_hash ✓
- §3.2 Stage 持有 + reload clear → T3 ✓
- §3.3 build 改造 + 合成 scrollbar 强制 emit → T2 ✓
- §3.4 不变量（首帧全 emit / reload 清 / 碰撞 1 帧延迟）→ T2/T3 测 ✓
- §5.1 _frameBuf ArrayPool → T5 ✓
- §5.2 ReadMesh 留观察 → T5 注释 ✓
- §6.1 Rust bench → T6 ✓
- §6.2 家里机 Profiler → T7 ledger 记待验 ✓
- §6.3 测覆盖 → T1/T2/T3/T4 测 ✓
- §7 version v1e + blob 零 bump → T4 ✓
- §8 风险（hash 遗漏 / scrollbar 强制 emit / ArrayPool 长度 / Domain 泄漏）→ T1 字段集 + T2 强制 emit + T5 注释 + T5 OnDestroy 归还 ✓

**2. Placeholder scan:** 无 TBD/TODO。T6 bench 数据 `<数据>` 是 implementer 跑出后填的实测值（非占位）。T7 `<commit>`/`<日期>` 同理（提交时填）。

**3. Type consistency:**
- `node_hash(&RenderNode) -> u64`（T1 定义，T2 调用）✓
- `build_render_nodes(scene, font, textures, prev_hashes: &[u64]) -> (FrameData, Vec<u64>)`（T2 定义，T3 调用）✓
- `prev_node_hashes: Vec<u64>`（T3 Stage 字段）✓
- `node_hash` 在 T1 测 + T2 调用签名一致 ✓
- T2 测里 `build_render_nodes(..., &[])` / `&hashes` 与新签名一致 ✓

无类型不一致。
