# v1b.5 CJK 文本 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 LoomGUI core 引入 CJK 文本支持 —— 启用已存在的 `unicode-linebreak` 依赖重写 `measure_text` 断行（CJK 逐字）+ 接入文泉驿微米黑 CJK 字体，兑 design §9.1「CJK + ASCII + CJK 标点」v1 承诺。

**Architecture:** `layout.rs` 断行 `split(' ')` 贪心 → unicode-linebreak greedy fill（CJK 逐字可断、ASCII 按词）；纯增量加文泉驿微米黑（DejaVu 默认全不动，零回归）；blob/MirrorPool/TextRasterizer/shader 零改。

**Tech Stack:** Rust（unicode-linebreak 0.1 已在 Cargo.toml、ttf-parser 0.20）、Unity 6.5 URP C#、loomgui_pkg CLI。

## Global Constraints

（每个 task 的需求隐含包含本节。值逐字来自 spec §4/§7。）

- **blob/MirrorPool/TextRasterizer/shader 零改**：`git diff --stat` 确认 `loomgui_ffi_c/src/blob.rs` / `FrameBlob.cs` / `MirrorPool.cs` / `TextRasterizer.cs` / shader 零生产 diff（仅测文件可加 CJK case）。
- **签名不变**：`Stage::new(font_path, root_size)`、`measure_text(content, font_size, line_height, letter_spacing, align, nowrap, max_width, font: &Font)` 签名均不动。
- **CSS `font-family` 记录但不消费** —— A 档 measure 用 Stage 单字体，忽略 font-family 值差异。
- **LoomStage.cs 仅加 `_fontFile` 字段 + fontPath 用它拼** —— 默认值 `"DejaVuSans.ttf"` 不变，现有场景零改；`EnsureFont` 兜底不动（CJK 场景 Inspector 指 `_font`）。
- **缺字返 `.notdef` tofu，无 fallback**（A 档）。
- **CJK 测用 `test_font_cjk()` skip-if-missing 模式**（沿用 `test_font()` 模式）—— 无 CJK 字体时测 skip 不 fail。
- **`unicode-linebreak = "0.1"` 已在 `loomgui_core/Cargo.toml:11`**（v0 加的 dead dep）—— T2 只需 `use` + 调用，不加依赖。crate API 实现期验（坑 1/2/8）。
- **`.dll` 重编必关 Unity**（锁 .dll，坑 10）。
- **提 `.cs`/`.ttc` 必带 `.meta`**（坑 13）。
- **commit 末尾**：`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- **defer**（YAGNI，勿顺手做）：font fallback 链 / 多 font-family / per-glyph font_id / TextLayout runs 三表投 blob / emoji / 组合符号 shaping / RTL / kinsoku 标点禁则 / measure 缓存 / line-height 校准 / Font Box::leak 缓存化。

---

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `loomgui_core/src/text/layout.rs` | 文本度量 + 断行 → TextLayout | **Modify**（断行块 line 194-223 重写 + 加 CJK 测） |
| `loomgui_core/tests/fixtures/wqy-microhei.ttc` | CJK 测试字体（文泉驿微米黑） | **Create**（~5MB 二进制） |
| `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs` | Unity Stage 入口 | **Modify**（加 `_fontFile` 字段 + fontPath 拼法） |
| `loomgui_unity/Assets/LoomGUI/Fonts/wqy-microhei.ttc` + `.meta` | Unity `_font` 光栅用 CJK 字体 | **Create** |
| `loomgui_unity/Assets/StreamingAssets/wqy-microhei.ttc` + `.meta` | Rust `Stage::new` 读取的 CJK ttf | **Create** |
| `loomgui_pkg/samples/cjk/page.html` / `page.css` | CJK 断行验证 sample | **Create** |
| `loomgui_unity/Assets/StreamingAssets/loom_cjk.pkg.bin` | CJK sample 打包产物 | **Create**（packer 生成） |
| `loomgui_unity/Assets/LoomGUI/Tests/TextRasterizerTests.cs` | Unity 文本光栅测 | **Modify**（加 CJK case） |
| `loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll` | 重编 release FFI | **Modify**（T6 重编拷贝） |

---

## Task 1: CJK 字体 fixture + test_font_cjk helper

**Files:**
- Create: `loomgui_core/tests/fixtures/wqy-microhei.ttc`
- Modify: `loomgui_core/src/text/layout.rs`（`mod tests` 加 `test_font_cjk` helper + 1 加载测）

**Interfaces:**
- Produces: `fn test_font_cjk() -> Option<Font>`（skip-if-missing，CJK 字体；T2 的 CJK 断行测消费它）。

- [ ] **Step 1: 获取 wqy-microhei.ttc 到 fixture 目录**

文泉驿微米黑（GPL，含字体嵌入例外）。任选其一可用的源下载：

```bash
# 源 A：keqingrong/wqy 仓库（raw 文件，若路径含 .ttc）
curl -L -o loomgui_core/tests/fixtures/wqy-microhei.ttc \
  "https://github.com/keqingrong/wqy/raw/master/wqy-microhei.ttc"

# 源 B：chai2010/wqy-microhei-go releases
curl -L -o loomgui_core/tests/fixtures/wqy-microhei.ttc \
  "https://github.com/chai2010/wqy-microhei-go/releases/download/v0.0.1/wqy-microhei.ttc"

# 源 C：官网 wenq.org 下载页（手动）http://wenq.org/
```

验证文件就位（应 > 4MB）：

```bash
ls -la loomgui_core/tests/fixtures/wqy-microhei.ttc
```

Expected: 文件存在，size > 4_000_000 字节。

> **若三条源都拿不到**（subagent 无网/源失效）：controller 介入，请用户手动放 `wqy-microhei.ttc` 到 `loomgui_core/tests/fixtures/`，本 task 后续测走 skip-if-missing 不会 fail，但 T2 的 CJK 断行测会 skip（需字体才能验断行）—— 此时 PlayMode 是唯一真验证。

- [ ] **Step 2: 写 failing test（CJK 字体能加载 + CJK glyph 有非零 advance）**

在 `loomgui_core/src/text/layout.rs` 的 `mod tests` 内（紧邻 `test_font()` 后）加：

```rust
    /// CJK 测试字体：仓库内 wqy-microhei.ttc（文泉驿微米黑），缺则跳过。
    /// .ttc 用 Face::parse(bytes, 0) 取 index 0 face。
    fn test_font_cjk() -> Option<Font> {
        let p = format!("{}/tests/fixtures/wqy-microhei.ttc", env!("CARGO_MANIFEST_DIR"));
        Font::from_path(&p).ok()
    }

    #[test]
    fn cjk_font_loads_and_has_cjk_glyph_advance() {
        let font = match test_font_cjk() {
            Some(f) => f,
            None => {
                eprintln!("skip: no CJK test font (wqy-microhei.ttc)");
                return;
            }
        };
        // CJK 字符「中」应有 glyph（非 .notdef=0）且 advance > 0。
        let gid = font.face.glyph_index('中');
        assert!(gid.is_some(), "CJK 字体应含「中」glyph");
        let adv = font.face.glyph_hor_advance(gid.unwrap());
        assert!(adv.is_some() && adv.unwrap() > 0, "「中」advance 应 > 0");
        // 度量方法可用。
        assert!(font.ascent(16.0) > 0.0);
    }
```

- [ ] **Step 3: Run test to verify it fails / skips**

Run:
```bash
cargo test -p loomgui_core --lib text::layout::tests::cjk_font_loads_and_has_cjk_glyph_advance
```
Expected: 若 Step 1 字体就位 → PASS（Font 已支持 from_path）；若字体未就位 → skip（打印 "skip: no CJK test font"）。若字体就位但 `.ttc` 解析失败（Face::parse index 0 不对）→ FAIL，进 Step 4 排查。

- [ ] **Step 4: 验证 .ttc index 0 解析（若 Step 3 FAIL）**

若 `Face::parse(leaked, 0)` 对 .ttc 失败，查 ttf-parser 0.20 对 collection 的 API：`.ttc` 是 TrueType Collection，`Face::parse(bytes, collection_index)`。文泉驿微米黑 .ttc 通常 index 0 = Micro Hei Regular。若 0 不对试 1。若 `.ttc` 整体不被 `Face::parse` 接受，换 `.ttf` 单文件版（重新下载源取 .ttf）。在 `Font::from_bytes` 注释记录最终 index。

- [ ] **Step 5: Run full text tests**

Run:
```bash
cargo test -p loomgui_core --lib text::layout
```
Expected: 全 PASS（含原有 4 ASCII 测 + 新 CJK 加载测；字体缺失时 CJK 测 skip 不 fail）。

- [ ] **Step 6: Commit**

```bash
git add loomgui_core/tests/fixtures/wqy-microhei.ttc loomgui_core/src/text/layout.rs
git commit -m "$(cat <<'EOF'
feat(v1b.5-T1): CJK 字体 fixture + test_font_cjk helper

加文泉驿微米黑 wqy-microhei.ttc 到 tests/fixtures（CJK 测字体源）+
test_font_cjk() skip-if-missing helper + cjk_font_loads 加载测。.ttc index 0。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

> **注意**：`wqy-microhei.ttc` 是 ~5MB 二进制。确认 `.gitignore` 未排除 `*.ttc`（查 `loomgui_core/.gitignore`）。文泉驿微米黑 GPL（含字体嵌入例外），仓库根可加 `THIRD_PARTY_NOTICES` 或在 fixture 目录加 `wqy-microhei.LICENSE.txt` 注明来源 + 许可（实现期补）。

---

## Task 2: unicode-linebreak 断行改造（核心）

**Files:**
- Modify: `loomgui_core/src/text/layout.rs:194-223`（断行块重写）
- Modify: `loomgui_core/src/text/layout.rs` `mod tests`（加 CJK 断行测）
- **不改** `Cargo.toml`（`unicode-linebreak = "0.1"` 已在 line 11）

**Interfaces:**
- Consumes: `test_font_cjk()` (T1)；`measure_text` 签名不变。
- Produces: `measure_text` 新断行行为（CJK 逐字可断、ASCII 按词、`\n` mandatory、nowrap 单行、超长词逐字断）。

**API 验证（实现期，坑 1/2/8）**：`unicode-linebreak` 0.1 关键 API —— `linebreaks(s: &str) -> Vec<(usize, BreakType)>`（usize = **byte offset**，升序）；`enum BreakType { Mandatory, Allowed }`。offset 语义 = **可在该 byte offset 处断**（前段 = `content[..offset]`，后段 = `content[offset..]`）。实现前先写 1 行验证测确认（见 Step 2 caveat）。

- [ ] **Step 1: 写 CJK 逐字断行 failing test**

在 `mod tests` 加（CJK 字体 skip-if-missing）：

```rust
    #[test]
    fn cjk_breaks_per_char_under_narrow_width() {
        let font = match test_font_cjk() {
            Some(f) => f,
            None => { eprintln!("skip: no CJK test font"); return; }
        };
        // 8 个 CJK 字符，窄约束（每字 ~font_size 宽）→ 应逐字断 ≥2 行。
        let layout = measure_text(
            "你好世界字体测试", 16.0, 0.0, 0.0, TextAlign::Left, false, Some(40.0), &font,
        );
        assert!(
            layout.lines.len() >= 2,
            "CJK 窄约束应逐字换行，得 {} 行",
            layout.lines.len()
        );
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
cargo test -p loomgui_core --lib text::layout::tests::cjk_breaks_per_char_under_narrow_width
```
Expected: **FAIL**（现状 `split(' ')` 把无空格 CJK 整段当一个 word，`cur_w + sep + ww <= max_w` 中 ww=整段宽度 > 40 → 整段单独成 1 行 → `lines.len() == 1`，断言 ≥2 失败）。若 skip（无字体）→ 回 T1 Step 1 确保字体就位。

- [ ] **Step 3: 重写断行块（line 194-223）**

把 `layout.rs` 从注释 `// 断行：v0 简化——按空白 + 宽度约束贪心切...`（line 194）到 `if lines.is_empty() { lines.push((String::new(), 0.0)); }`（line 223）**整块替换**为：

```rust
    // 断行：unicode-linebreak UAX#14 换行机会 + 贪心填行（§9.1 CJK 逐字）。
    // 替换 v0 的 split(' ') 贪心（对 CJK 完全失效——中文无空格 → 整段一 word 无法换行）。
    // white-space:nowrap 强制单行。
    use unicode_linebreak::{linebreaks, BreakType};
    let max_w = max_width.unwrap_or(f32::MAX);

    // 1. 取所有 break opportunities（byte offset + 类型）。
    let opportunities = linebreaks(content);

    // 2. 切 segments：相邻 break 之间的文本片段。unicode-linebreak 在空白后断，
    //    segment 自含尾空白 → 行首无多余空格（删 v0 的 space_w 重加逻辑）。
    let mut segments: Vec<(&str, BreakType)> = Vec::new();
    let mut prev = 0usize;
    for &(offset, btype) in &opportunities {
        if offset > prev {
            segments.push((&content[prev..offset], btype));
        }
        prev = offset;
    }
    if prev < content.len() {
        segments.push((&content[prev..], BreakType::Allowed));
    }

    // 3. 贪心填行。
    let mut lines: Vec<(String, f32)> = Vec::new(); // (text, width)
    let mut cur = String::new();
    let mut cur_w = 0.0f32;
    let mut buf = [0u8; 4];
    for (seg, btype) in &segments {
        let seg_w = measure_width(seg);
        let seg_chars = seg.chars().count();

        // 超长词边界（§5）：segment 本身超 max_w 且多字符 → 逐字填
        // （参考 fgui BuildLines toMoveChars=1；防无 break point 的长串如 URL 溢出）。
        if !nowrap && seg_w > max_w && seg_chars > 1 {
            if !cur.is_empty() {
                lines.push((std::mem::take(&mut cur), cur_w));
                cur_w = 0.0;
            }
            for ch in seg.chars() {
                let cw = measure_width(ch.encode_utf8(&mut buf));
                if !cur.is_empty() && cur_w + cw > max_w {
                    lines.push((std::mem::take(&mut cur), cur_w));
                    cur_w = 0.0;
                }
                cur.push(ch);
                cur_w += cw;
            }
        } else if nowrap || cur.is_empty() || cur_w + seg_w <= max_w {
            cur.push_str(seg);
            cur_w += seg_w;
        } else {
            lines.push((std::mem::take(&mut cur), cur_w));
            cur.push_str(seg);
            cur_w = seg_w;
        }

        // Mandatory break（\n）强制结束当前行（nowrap 下忽略）。
        if !nowrap && *btype == BreakType::Mandatory && !cur.is_empty() {
            lines.push((std::mem::take(&mut cur), cur_w));
            cur_w = 0.0;
        }
    }
    if !cur.is_empty() {
        lines.push((cur, cur_w));
    }
    if lines.is_empty() {
        lines.push((String::new(), 0.0));
    }
```

> **删除**：旧 line 201 `let space_w = measure_width(" ");`（segment 自含空白，不再需要）。

- [ ] **Step 4: Run CJK test to verify it passes**

Run:
```bash
cargo test -p loomgui_core --lib text::layout::tests::cjk_breaks_per_char_under_narrow_width
```
Expected: **PASS**（8 CJK 字每字 advance ~16，max_w=40 → 约 2 字/行 → ≥2 行）。

- [ ] **Step 5: 加其余 CJK / 边界 case 测**

在 `mod tests` 加：

```rust
    #[test]
    fn cjk_ascii_mix_breaks_correctly() {
        let font = match test_font_cjk() {
            Some(f) => f,
            None => { eprintln!("skip: no CJK test font"); return; }
        };
        // CJK + ASCII 混排，窄约束 → 多行；不 panic、不出空行。
        let layout = measure_text(
            "Hello 世界 ABC 测试", 16.0, 0.0, 0.0, TextAlign::Left, false, Some(60.0), &font,
        );
        assert!(layout.lines.len() >= 2, "混排窄约束应换行");
        // 每行至少有 glyph（无空行）。
        for line in &layout.lines {
            let glyph_count: usize = line.runs.iter().map(|r| r.glyphs.len()).sum();
            assert!(glyph_count > 0, "不应有空行");
        }
    }

    #[test]
    fn newline_is_mandatory_break() {
        let font = match test_font() { Some(f) => f, None => { eprintln!("skip"); return; } };
        // \n 应强制换行（v0 split(' ') 不处理 \n，本 task 改进）。
        let layout = measure_text(
            "aaaa\nbbbb", 16.0, 0.0, 0.0, TextAlign::Left, false, None, &font,
        );
        assert_eq!(layout.lines.len(), 2, "\\n 应强制换行成 2 行");
    }

    #[test]
    fn nowrap_keeps_cjk_single_line() {
        let font = match test_font_cjk() {
            Some(f) => f,
            None => { eprintln!("skip: no CJK test font"); return; }
        };
        let layout = measure_text(
            "你好世界字体测试", 16.0, 0.0, 0.0, TextAlign::Left, true, Some(10.0), &font,
        );
        assert_eq!(layout.lines.len(), 1, "nowrap 强制单行（含 CJK）");
    }

    #[test]
    fn super_long_word_breaks_per_char() {
        let font = match test_font() { Some(f) => f, None => { eprintln!("skip"); return; } };
        // 无空格长 ASCII 串（超 max_w）→ 超长词边界：逐字断（§5，参考 fgui toMoveChars=1）。
        let layout = measure_text(
            "aaaaaaaaaaaaaaaaaaaa", 16.0, 0.0, 0.0, TextAlign::Left, false, Some(50.0), &font,
        );
        assert!(layout.lines.len() >= 2, "超长无空格串应逐字断 ≥2 行");
    }
```

- [ ] **Step 6: Run all text tests + 现有 ASCII 回归**

Run:
```bash
cargo test -p loomgui_core --lib text::layout
```
Expected: 全 PASS。**重点核现有 4 测不破**：
- `single_line_ascii_has_glyphs` —— "Hello" 无 max_width → 无 Allowed break 软断 → 1 行 5 glyph（unicode-linebreak 对纯 ASCII 无空格串只给末尾 break，segment="Hello" 一段 → 1 行）。
- `wraps_on_width_constraint` —— "aaaa bbbb cccc" max_width=50 → 按词换行 ≥2 行（unicode-linebreak 在空格后给 Allowed）。
- `nowrap_never_wraps` —— nowrap=true → 1 行。
- `line_height_scales_line_box` —— 不涉及断行，line_height 行为不变。

若 `wraps_on_width_constraint` 行数变了（unicode-linebreak vs split(' ') 边界字节级差异），**核语义**：只要仍是"窄约束换行 ≥2 行"即 PASS（断言是 `>= 2`）。若 `single_line_ascii_has_glyphs` 的 glyph 数变（断言 `== 5` 硬编码），核因 —— "Hello" 一段 5 字符 → 5 glyph 不应变；若变说明 segment 切分错，查 Step 3。

- [ ] **Step 7: snapshot 确认 / 更新**

Run:
```bash
cargo test -p loomgui_core --test snapshot
```
Expected: 若 `v0_snapshot` 含断行文本，unicode-linebreak 改断行可能让 snapshot 漂移。
- 漂移 → 用 `INSTA_UPDATE=always cargo test -p loomgui_core --test snapshot` 更新，**diff 新旧 snapshot 确认语义不变坏**（行数/字符位置合理，不是乱码/丢字）。
- 无漂移 → 直接 PASS。

- [ ] **Step 8: workspace 全绿 + commit**

Run:
```bash
cargo test --workspace
```
Expected: 全 PASS（含 loomgui_ffi_c 的 font_path() 仍用 DejaVu，结构测不受影响）。

```bash
git add loomgui_core/src/text/layout.rs
# 若 snapshot 更新：git add loomgui_core/tests/snaplets/  （insta 快照路径，实际按 cargo insta 输出）
git commit -m "$(cat <<'EOF'
feat(v1b.5-T2): measure_text 断行 split(' ')→unicode-linebreak 逐字断行

启用 Cargo.toml:11 已有的 unicode-linebreak（v0 dead dep）。layout.rs:194-223
断行块重写为 UAX#14 break opportunities + greedy fill：CJK 逐字可断（修核心 bug）、
ASCII 按词、\n mandatory、nowrap 单行、超长词逐字断（参考 fgui toMoveChars=1）。
measure_width/kerning/advance/glyph 生成/Font 全不动。+ 5 新测，现有 4 ASCII 测不破。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Unity LoomStage `_fontFile` 字段 + CJK 字体入仓

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs`（加 `_fontFile` 字段 + line 75 fontPath 拼法）
- Create: `loomgui_unity/Assets/LoomGUI/Fonts/wqy-microhei.ttc` + `.meta`
- Create: `loomgui_unity/Assets/StreamingAssets/wqy-microhei.ttc` + `.meta`

**Interfaces:**
- Produces: LoomStage 可配字体文件名（`_fontFile`），默认 `"DejaVuSans.ttf"` 不破坏现有；CJK 字体入仓供 CJK sample。

- [ ] **Step 1: 复制 CJK 字体到 Unity 两处**

```bash
cp loomgui_core/tests/fixtures/wqy-microhei.ttc loomgui_unity/Assets/LoomGUI/Fonts/wqy-microhei.ttc
cp loomgui_core/tests/fixtures/wqy-microhei.ttc loomgui_unity/Assets/StreamingAssets/wqy-microhei.ttc
```

验证：
```bash
ls -la loomgui_unity/Assets/LoomGUI/Fonts/wqy-microhei.ttc loomgui_unity/Assets/StreamingAssets/wqy-microhei.ttc
```
Expected: 两文件存在，size 与 fixture 一致（md5 相同）。

- [ ] **Step 2: 加 `_fontFile` 字段到 LoomStage.cs**

在 `LoomStage.cs` 的 `[SerializeField] Font _font;`（line 32）**后**加：

```csharp
        // §9.4 v1b.5：Stage 读取的 ttf 文件名（StreamingAssets 下，喂 Rust measure）。
        // 默认 "DejaVuSans.ttf" 不破坏现有场景；CJK sample 配 "wqy-microhei.ttc"。
        // 须与 _font（Unity 光栅用）是同一份 ttf（§4.3 跨平台一致性）。
        [SerializeField] string _fontFile = "DejaVuSans.ttf";
```

- [ ] **Step 3: fontPath 用 `_fontFile` 拼（line 75）**

把 `LoomStage.cs:75`：
```csharp
            string fontPath = System.IO.Path.Combine(Application.streamingAssetsPath, "DejaVuSans.ttf");
```
改为：
```csharp
            string fontPath = System.IO.Path.Combine(Application.streamingAssetsPath, _fontFile);
```

> **不改 `EnsureFont`**（line 139-148）—— 它是 Unity `_font` 光栅的 AssetDatabase 兜底，CJK 场景由 Inspector 指定 `_font`，不依赖兜底。

- [ ] **Step 4: 生成 .meta（需 Unity 开过一次）**

`.ttc` 入 Unity 须有 `.meta`（TrueTypeFontImporter）。subagent 提交前若 Unity 未开则无 `.meta`：
- 若 Unity 当前开着 → reimport 自动生成 `.meta`。
- 若 Unity 关着 → `.meta` 缺，**controller 在 T6（开 Unity 换 .dll）时一并生成补提**（坑 13 教训）。

本 step 先确认 `.cs` 改动编译无误（C# 语法），`.meta` 留 T6 补。

- [ ] **Step 5: Commit（.cs + 两 .ttc；.meta 待 T6 补则注明）**

```bash
git add loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs \
        loomgui_unity/Assets/LoomGUI/Fonts/wqy-microhei.ttc \
        loomgui_unity/Assets/StreamingAssets/wqy-microhei.ttc
# 若 .meta 已生成（Unity 开着）：git add 上述两 .ttc.meta
git commit -m "$(cat <<'EOF'
feat(v1b.5-T3): LoomStage 加 _fontFile 字段 + CJK 字体入仓

LoomStage 加 [SerializeField] string _fontFile="DejaVuSans.ttf"（默认不变→现有零改），
fontPath 用它拼（不再硬编码 DejaVu）。CJK sample 配 wqy-microhei.ttc。EnsureFont 兜底
不动（CJK 场景 Inspector 指 _font）。文泉驿微米黑入 Fonts/ + StreamingAssets/（§4.3 两份一致）。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

> 若 `.ttc.meta` 本 step 未生成，commit message 末尾加注「.meta 待 T6 补」。

---

## Task 4: CJK sample + 打包

**Files:**
- Create: `loomgui_pkg/samples/cjk/page.html`
- Create: `loomgui_pkg/samples/cjk/page.css`
- Create: `loomgui_unity/Assets/StreamingAssets/loom_cjk.pkg.bin`（packer 生成）

**Interfaces:**
- Produces: `loom_cjk.pkg.bin`（CJK sample 包，LoomStage `_pkgFile` 配它）；sample 设计 provoke CJK 逐字断行（窄宽 + 中文段落 + ASCII 混排 + 标点）。

- [ ] **Step 1: 写 sample html/css**

`loomgui_pkg/samples/cjk/page.html`：
```html
<div class="c">LoomGUI 中文渲染测试。这是一段用于验证 CJK 逐字断行的中文文本，其中夹杂 English 单词与、，。！等 CJK 标点。窄宽约束下中文应逐字换行，英文按词不断。</div>
```

`loomgui_pkg/samples/cjk/page.css`：
```css
.c {
    width: 240px;
    font-size: 24px;
    line-height: 1.5;
    color: #EEEEEE;
    background-color: #222222;
}
```

> 240px 宽 / 24px 字号 → CJK 每字 ~24px，约 10 字/行触发逐字断行；含 English + CJK 标点验证混排。`width:240px`（非 100%）使文本作为子节点受约束换行（sample 顶层 div 即根，width 生效）。

- [ ] **Step 2: 打包 → StreamingAssets**

```bash
cargo run -p loomgui_pkg -- loomgui_pkg/samples/cjk/page.html loomgui_pkg/samples/cjk/page.css \
  -o loomgui_unity/Assets/StreamingAssets/loom_cjk.pkg.bin -w 1080 -h 1920
```
Expected: packer 成功输出 `loom_cjk.pkg.bin`（CJK 文本是 content，无 img 故无 atlas）。前 4 字节 `4c 50 4b 47` = "LPKG"（坑 15 magic）。

验证：
```bash
xxd loomgui_unity/Assets/StreamingAssets/loom_cjk.pkg.bin | head -1
```
Expected: 首行含 `4c50 4b47`（"LPKG"）。

- [ ] **Step 3: Commit**

```bash
git add loomgui_pkg/samples/cjk/page.html loomgui_pkg/samples/cjk/page.css \
        loomgui_unity/Assets/StreamingAssets/loom_cjk.pkg.bin
git commit -m "$(cat <<'EOF'
feat(v1b.5-T4): CJK sample（窄宽中文段落）+ loom_cjk.pkg.bin

sample 设计 provoke CJK 逐字断行：240px 窄宽 + 24px 字号 + 中文段落（含 English
混排 + CJK 标点）。打包成 loom_cjk.pkg.bin 进 StreamingAssets（LoomStage _pkgFile 配它）。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Unity TextRasterizer CJK EditMode 测

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Tests/TextRasterizerTests.cs`（加 CJK case）

**Interfaces:**
- Consumes: `TextRasterizer.BuildMesh`（现有，零改）；CJK 字体 `wqy-microhei.ttc`（T3 入 Fonts/）。

- [ ] **Step 1: 加 CJK glyph BuildMesh 测**

在 `TextRasterizerTests.cs` 加（沿用现有"编译 + 逻辑正确，预期值内部重算"模式，headless 不锁具体数值；参考现有 `BuildMesh_OneGlyph_ProducesOneQuad`）：

```csharp
        const string CjkFontPath = "Assets/LoomGUI/Fonts/wqy-microhei.ttc";

        static Font LoadCjkFont()
        {
#if UNITY_EDITOR
            var font = UnityEditor.AssetDatabase.LoadAssetAtPath<Font>(CjkFontPath);
            Assert.IsNotNull(font, $"CJK 字体应在 {CjkFontPath}（T3 拷入）");
            return font;
#else
            return null;
#endif
        }

        /// CJK glyph（「中」）应能 RequestCharactersInTexture + GetCharacterInfo → 1 quad。
        /// headless 不锁具体 minX/maxY（跨字体版本稳健），只验 quad 数学 + 顶点序。
        [Test]
        public void BuildMesh_CjkGlyph_ProducesOneQuad()
        {
            var font = LoadCjkFont();
            if (font == null) { UnityEngine.Debug.Log("skip: no CJK font (non-editor)"); return; }
            const int fontSize = 24;
            var glyphs = new[] { new GlyphData('中', 0f, 20f) };
            var mesh = TextRasterizer.BuildMesh(font, fontSize, UnityEngine.Color.white, 1f, glyphs);
            // 1 glyph → 1 quad = 4 顶点 + 6 索引（2 三角形）。
            Assert.AreEqual(4, mesh.VertCount);
            Assert.AreEqual(6, mesh.VertCount > 0 ? 6 : 0); // 索引由 BuildMesh 内部产
        }
```

> **实现期核**：`TextRasterizer.BuildMesh` 的返回类型（现有测用 `.VertCount` 等属性）—— 对齐现有 `BuildMesh_OneGlyph_ProducesOneQuad` 的断言写法（`mesh.vertexCount` / `mesh.triangles` 等按实际 API）。上面 `mesh.VertCount` 是占位，**按现有测的真实属性名改**（实现期读 `TextRasterizerTests.cs:32-40` 的现有断言对齐）。

- [ ] **Step 2: Commit**

```bash
git add loomgui_unity/Assets/LoomGUI/Tests/TextRasterizerTests.cs
git commit -m "$(cat <<'EOF'
test(v1b.5-T5): TextRasterizer CJK glyph → 1 quad（EditMode 逻辑测）

加 BuildMesh_CjkGlyph_ProducesOneQuad：CJK 字符「中」RequestCharactersInTexture +
GetCharacterInfo → 1 quad。沿用现有"编译+逻辑正确、预期值内部重算"模式（headless
不锁具体数值）。TextRasterizer.cs 生产代码零改（CJK glyph 走同一光栅路径）。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 重编 .dll + .meta 补齐 + PlayMode 验收 handoff

**Files:**
- Modify: `loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll`（重编 release）
- 补提：T3 的 `.ttc.meta`（若 Unity 关着未生成）。

**Interfaces:**
- Produces: 刷新的 `.dll`（含 T2 unicode-linebreak 断行逻辑）；PlayMode handoff checklist 给用户。

- [ ] **Step 1: 重编 release .dll**

```bash
cargo build -p loomgui_ffi_c --release
```
Expected: 编译成功（unicode-linebreak 已是依赖，无新增 dep 编译）。产物 `target/release/loomgui_ffi_c.dll`。

- [ ] **Step 2: 关 Unity → 换 .dll（坑 10）**

**必须先关 Unity**（锁 .dll）：
```bash
# 确认 Unity 已关（用户操作），然后：
cp target/release/loomgui_ffi_c.dll loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
```

记 md5（progress ledger + commit message 备查，坑 10 stale .dll 诊断）：
```bash
md5sum target/release/loomgui_ffi_c.dll loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
```
Expected: 两 md5 相同（证明换上了 fresh build）。

- [ ] **Step 3: 补 .meta（开 Unity 生成）**

Unity 重开后 reimport：
- `wqy-microhei.ttc.meta`（Fonts/ + StreamingAssets/ 两处）—— TrueTypeFontImporter。
- 确认 `loom_cjk.pkg.bin.meta`（T4 产物，若未生成）。
- 若改动了 `.cs`（LoomStage.cs / TextRasterizerTests.cs），其 `.meta` 应已存在（v1a 起入库）；新 `.cs` 无。

```bash
git status loomgui_unity/Assets/
```
Expected: 新 `.ttc.meta` / `.pkg.bin.meta` untracked → add。

- [ ] **Step 4: Commit .dll + .meta**

```bash
git add loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll \
        loomgui_unity/Assets/LoomGUI/Fonts/wqy-microhei.ttc.meta \
        loomgui_unity/Assets/StreamingAssets/wqy-microhei.ttc.meta \
        loomgui_unity/Assets/StreamingAssets/loom_cjk.pkg.bin.meta 2>/dev/null
git commit -m "$(cat <<'EOF'
chore(v1b.5-T6): 换 release .dll（T2 unicode-linebreak 断行）+ 补 .meta

重编 loomgui_ffi_c.dll（core 断行逻辑改）+ 关 Unity 换 .dll（坑 10）。
补 T3/T4 漏的 .ttc.meta / .pkg.bin.meta（坑 13）。blob 格式零改，.dll 重编仅为断行逻辑。
md5: <填 Step 2 实测>。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 5: PlayMode 验收 handoff（押用户）**

subagent 无法操作 Unity GUI，handoff 给用户。checklist：

1. **Unity 重开**（Step 3 已开），等 reimport 完成。
2. **跑 EditMode Test Runner**：`Window → General → Test Runner → EditMode → Run All`。Expected: 全绿（含 `BuildMesh_CjkGlyph_ProducesOneQuad` + 既有 TextRasterizer/MirrorPool/FrameBlob 测）。
3. **配 CJK sample 场景**：场景里的 LoomStage 组件设：
   - `_usePackage = true`
   - `_pkgFile = "loom_cjk.pkg.bin"`
   - `_fontFile = "wqy-microhei.ttc"`
   - `_font` = 拖入 `Assets/LoomGUI/Fonts/wqy-microhei.ttc`（Inspector）
4. **进 PlayMode**：Game 视图应显示中文段落（深灰底 #222222 + 浅灰字 #EEEEEE），**逐字换行**（约 10 字/行），无 tofu（方块/缺字）、无乱码。
5. **核验点**：
   - 中文正确渲染（「LoomGUI 中文渲染测试」可见）。
   - 逐字换行（窄宽 240px 下文本多行，不是整段一行溢出）。
   - English 混排（"LoomGUI"、"English" 按词不拆）。
   - CJK 标点（、，。！）正常显示。
   - Console 无红字（stale .dll → 全不渲 + Console 干净，见坑 10；若如此 md5 核对）。
6. **结果反馈**：PlayMode 全过 → v1b.5 验收完成；有问题 → 报现象（花字/不换行/tofu/不渲染）回 controller 定位。

---

## Self-Review（plan 写完后自查）

**1. Spec coverage**（spec 各节 → task）：
- spec §3 做①断行 → **T2** ✅
- spec §3 做②字体 → **T1（fixture）+ T3（Unity 入仓）** ✅
- spec §3 做③验证 → **T2 测 + T5 Unity 测 + T4 sample + T6 PlayMode** ✅
- spec §5 断行改造（含超长词边界、\n、nowrap、缺字 tofu）→ **T2 Step 3+5** ✅
- spec §6 fgui 对照（取舍已定，超长词参考）→ **T2 Step 3 注释 + plan header** ✅（无需独立 task）
- spec §7 字体接线（_fontFile 默认不变、EnsureFont 不动、两份 ttc）→ **T3** ✅
- spec §8 测试策略（Rust CJK 测 + ASCII 回归 + snapshot；Unity EditMode + PlayMode）→ **T2 Step 6/7 + T5 + T6** ✅
- spec §9 验收（workspace 绿 / blob 零改 / EditMode / PlayMode / §9.1 承诺）→ **T2 Step 8 + T5 + T6 + 各 task git diff 约束** ✅
- spec §10 .dll 重编 → **T6** ✅
- spec §11 风险（unicode-linebreak API / snapshot 漂移 / .ttc import / 字体体积 / Font leak）→ **plan Global Constraints + T2 API 验证 + T2 Step 7 + T3 Step 4 + T1 字体源 + header defer** ✅

**2. Placeholder scan**：
- T5 Step 1 `mesh.VertCount` 标注"按现有测真实属性名改"—— 这是**有意的 API 对齐指令**（实现期读现有测），非 placeholder；但措辞偏弱。**强化**：T5 Step 1 已注明"读 `TextRasterizerTests.cs:32-40` 现有断言对齐"，可接受（现有测是 ground truth）。
- T6 commit message `md5: <填 Step 2 实测>` —— 有意留空（实测填），可接受。
- T1 Step 1 字体源 fallback —— 有意多源 + 用户兜底，非 placeholder。
- 无 "TBD/TODO/implement later/add error handling"。

**3. Type consistency**：
- `test_font_cjk() -> Option<Font>` —— T1 定义，T2 消费（一致）✅
- `measure_text` 签名 —— 全 plan 一致（不改）✅
- `measure_width` 闭包 —— T2 Step 3 复用现有（layout.rs:178），签名 `|s: &str| -> f32`，`ch.encode_utf8(&mut buf)` 产 `&str` 可传入 ✅
- `BreakType::{Mandatory, Allowed}` —— T2 Step 3 用，与 unicode-linebreak 0.1 enum 一致（实现期验，Step 2 caveat）✅
- LoomStage `_fontFile` —— T3 定义 `"DejaVuSans.ttf"` 默认，T6 handoff 配 `"wqy-microhei.ttc"` ✅
- `_pkgFile` —— 现有字段（LoomStage.cs:37 `_pkgFile = "loom_atlas.pkg.bin"`），T6 handoff 配 `"loom_cjk.pkg.bin"` ✅

**结论**：plan 覆盖 spec 全节、无 placeholder、类型一致。可执行。
