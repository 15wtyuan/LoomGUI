# v1b.3 图集打包（shelf，最小可行 batching） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打包器把散图 PNG shelf 打包成一张 atlas.png + AtlasSprite 表；运行时同图集多 sprite 共享 1 Texture2D → MaterialManager 同 Material → Unity 批合条件具备；core 持 UV region（去引擎化）。

**Architecture:** 打包期 `loomgui_pkg`（+`image` crate）解码散图 → shelf 打包 → 编码 atlas.png + AtlasSprite 表写进 `.pkg.bin` v2。运行期 core `load_package` 从表建 `TextureRegistry`（src→{atlas_tex_id,uv_region,dims}），render quad 按 region 烤 UV；blob v3 **不变**（per-vertex UV 已在 mesh_arena）；Unity 载 1 张 atlas → `_texMap[atlas_tex_id]`，MirrorPool 按 tex_id 绑（已应用 blob UV）。

**Tech Stack:** Rust（`image` crate 仅 packer、taffy、serde）、csbindgen FFI、Unity 6.5 URP / C#。

**Spec:** `docs/superpowers/specs/2026-06-21-v1b-atlas-design.md`（单一真相源；本 plan 不重复 why，只给 what/code）。

## Global Constraints

- **.pkg.bin version 2**（`PKG_FORMAT_VERSION=2`，`MIN_VERSION=MAX_VERSION=2`）；旧 v1 包 `TooNew(1)` 拒，无迁移器。AtlasSprite section 在 NodeBlock 之后。
- **blob v3 不变**（14 列、header_len=92、tex_id 列 13、arena 布局全同 v1b.2）。atlas 子区 UV 是 mesh_arena per-vertex UV 的不同值，走既有 `uvs[]` 路径。
- **FFI 串契约（坑16）**：`atlas_info` 返 `String::as_ptr()`（**无尾 NUL**）+ `*out_src_len`；C# 必 `Encoding.UTF8.GetString(ptr, (int)len)`，禁 NUL-scan。
- **`image` crate 仅 `loomgui_pkg`**——core（loomgui_core）不碰像素、不加 `image`。
- **UV 约定**：packer region 用 PNG 像素空间（y-down，`image` 行0=顶）；core `uv_min=[region.x/aw, region.y/ah]`、`uv_max=[(x+w)/aw, (y+h)/ah]`；vert 序 TL,TR,BR,BL ↔ sprite 角 TL,TR,BR,BL（沿用 v1b.2 已 PlayMode 验证 convention，root 一次性 y-flip）。
- **shelf**：`atlas_w = max(512, 最宽 sprite)`；NPOT；无旋转/trim；单图集。
- **atlas_tex_id**：core load 时分配（atlas[0]→tex_id 1）；FFI `atlas_info(i)` 报 `(i+1)`——两侧约定一致。
- **ExecuteAlways 泄漏（坑11/18）**：OnDestroy Dispose 全部 atlas Texture2D + 全限定 `UnityEngine.Object`（`using System;` 引入 `System.Object` 歧义）。
- **stale .dll（坑10）**：blob version 不变（仍3），但 core+FFI 改 → .dll 必重编；v2 .pkg.bin 旧 .dll 拒（TooNew）。PlayMode 前重编换 .dll + 重打 sample + md5 校验。
- **缺图 build-time fail**（packer，§5.5）；**atlas.png 缺失/坏 runtime 降级**白占位（§11）。
- **构建矩阵 R1 门**：`cargo build -p loomgui_core --no-default-features` + `-p loomgui_ffi_c --no-default-features` 皆编（atlas 代码常驻不依赖 parse）。
- 代码注释沿用仓库中文风格。

---

## File Structure

| 文件 | 责任 | 任务 |
|---|---|---|
| `loomgui_core/src/asset/texture.rs` | `TexMeta`（+uv）+ `TextureRegistry`（insert/get/clear）；删 register/next_id | T1 |
| `loomgui_core/src/render/mesh.rs` | `quad(rect, color, uv_min, uv_max)` | T2 |
| `loomgui_core/src/render/mod.rs` | Image 分支按 region 烤 UV；Container/Button 全图 UV | T2 |
| `loomgui_core/src/asset/mod.rs` | `AtlasSection/Info/Sprite` + `build_registry` + write/read_package v2 + version 2 | T3 |
| `loomgui_core/src/stage.rs` | 删 src_cache/image_srcs；加 atlases 字段；load_package build_registry | T3 |
| `loomgui_pkg/{src/lib.rs,src/main.rs,Cargo.toml,tests/pack.rs}` | `image` crate + shelf_pack + atlas.png + pack() 4-arg | T4 |
| `loomgui_ffi_c/src/lib.rs` | 删 image_src/register；加 atlas_count/info；version v1b.3 | T5 |
| `loomgui_unity/.../LoomStage.cs` | LoadTextures→LoadAtlas | T6 |
| `loomgui_unity/.../Tests/AtlasMirrorPoolTests.cs` | 新 EditMode（atlas UV blob→绑 atlas） | T6 |
| `loomgui_unity/.../StreamingAssets/{loom_atlas.pkg.bin,loom_atlas.atlas.png}` | sample 资产 | T7 |

---

## Task 1: core 数据模型 — TexMeta + UV + TextureRegistry 重构

**Files:**
- Modify: `loomgui_core/src/asset/texture.rs`（重写：TexMeta 加 uv、TextureRegistry 改 insert、删 register/next_id）
- Modify（测试 ripple）: `loomgui_core/src/render/mod.rs` 测（register→insert）、`loomgui_core/src/layout/mod.rs` 测（register→insert）、`loomgui_core/src/stage.rs` golden 测（register→insert）

**Interfaces:**
- Produces: `TexMeta { tex_id: u32, uv_min: [f32;2], uv_max: [f32;2], width: u32, height: u32 }`；`TextureRegistry::insert/get/clear/len`（**删 `register`/`next_id`**）。

- [ ] **Step 1: 重写 `loomgui_core/src/asset/texture.rs`**

替换整个文件内容（保留模块 doc 思路，演进为 atlas 模型）：

```rust
//! 纹理注册表（v1b.3 atlas）：src → TexMeta{tex_id, uv_min, uv_max, w, h}。per Stage。
//! atlas 模式：core 在 load_package 时从 AtlasSprite 表建（asset::build_registry），
//! 同图集 sprite 共享 tex_id。tex_id 0 保留 = 未注册哨兵。
//! core 只持整数 id + UV region + 维度；GPU 纹理由后端持有。

use std::collections::HashMap;

#[derive(Debug, Clone, Copy, PartialEq)]
pub struct TexMeta {
    pub tex_id: u32,        // atlas root tex_id（同图集 sprite 共享）；0=未注册哨兵
    pub uv_min: [f32; 2],   // sprite 在 atlas 内 UV 左上（核心 y-down 约定，[0,1]）
    pub uv_max: [f32; 2],   // UV 右下
    pub width: u32,         // sprite 原始像素宽（measure；甲-B = region.w，无 trim）
    pub height: u32,        // sprite 原始像素高
}

#[derive(Debug, Default)]
pub struct TextureRegistry {
    map: HashMap<String, TexMeta>,
}

impl TextureRegistry {
    pub fn get(&self, src: &str) -> Option<TexMeta> { self.map.get(src).copied() }
    /// load_package 从 AtlasSprite 表建时插入（build_registry），或测试手工插。
    pub fn insert(&mut self, src: &str, meta: TexMeta) { self.map.insert(src.into(), meta); }
    pub fn clear(&mut self) { self.map.clear(); }
    pub fn len(&self) -> usize { self.map.len() }
    pub fn is_empty(&self) -> bool { self.map.is_empty() }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn meta(tid: u32, w: u32, h: u32) -> TexMeta {
        TexMeta { tex_id: tid, uv_min: [0.0, 0.0], uv_max: [1.0, 1.0], width: w, height: h }
    }

    #[test]
    fn insert_and_get_round_trips() {
        let mut r = TextureRegistry::default();
        r.insert("a.png", meta(1, 10, 20));
        let m = r.get("a.png").unwrap();
        assert_eq!((m.tex_id, m.width, m.height), (1, 10, 20));
        assert_eq!(m.uv_min, [0.0, 0.0]);
        assert_eq!(m.uv_max, [1.0, 1.0]);
    }

    #[test]
    fn get_miss_returns_none() {
        let r = TextureRegistry::default();
        assert!(r.get("nope.png").is_none());
    }

    #[test]
    fn insert_overwrites_same_src() {
        let mut r = TextureRegistry::default();
        r.insert("a.png", meta(1, 10, 20));
        r.insert("a.png", meta(2, 30, 40));   // 覆盖
        let m = r.get("a.png").unwrap();
        assert_eq!((m.tex_id, m.width), (2, 30));
        assert_eq!(r.len(), 1);
    }

    #[test]
    fn clear_empties_registry() {
        let mut r = TextureRegistry::default();
        r.insert("a.png", meta(1, 1, 1));
        r.clear();
        assert!(r.is_empty());
        assert!(r.get("a.png").is_none());
    }
}
```

- [ ] **Step 2: 更新 `loomgui_core/src/layout/mod.rs` 测——register→insert**

3 个测（`image_measure_prefers_css_length_over_real_dims` / `image_measure_uses_real_dims_when_no_css` / `image_measure_falls_back_to_64_when_unregistered`）里的 `tex.register("x.png", W, H);` 改为：

```rust
tex.insert("x.png", TexMeta {
    tex_id: 1, uv_min: [0.0, 0.0], uv_max: [1.0, 1.0], width: W, height: H,
});
```
（`use crate::asset::texture::TexMeta;` 已在 `use super::*` 链内——确认 `layout/mod.rs` 测 mod 顶部 `use super::*` 拉入；若否加 `use crate::asset::texture::TexMeta;`。未注册测不动——它用 `TextureRegistry::default()` 空。）measure 生产代码**零改**（仍读 `m.width as f32`/`m.height as f32`，字段名不变）。

- [ ] **Step 3: 更新 `loomgui_core/src/render/mod.rs` 测——register→insert**

`build_image_uses_registered_tex_id` 里：
```rust
// 原：let tid = tex.register("logo.png", 200, 100);
let tid = { tex.insert("logo.png", TexMeta {
    tex_id: 1, uv_min: [0.0, 0.0], uv_max: [1.0, 1.0], width: 200, height: 100,
}); 1 };
```
（`build_image_unregistered_is_zero` 不动——用空 registry。`TexMeta` 经 `use crate::asset::texture::*` 或加 import；render/mod.rs 已 `use crate::scene::node::*`，加 `use crate::asset::texture::TexMeta;` 到测 mod。）本 task 只保证编译+原断言（texture==tid）过；UV 四角断言留 T2。

- [ ] **Step 4: 更新 `loomgui_core/src/stage.rs` golden 测——register→insert**

`package_load_renders_identical_to_inline` 里两处 `s.textures.register("logo.png", 64, 32);` 改为：
```rust
s.textures.insert("logo.png", crate::asset::texture::TexMeta {
    tex_id: 1, uv_min: [0.0, 0.0], uv_max: [1.0, 1.0], width: 64, height: 32,
});
```
（inline 与 pkg 两个 Stage 都改。本 task 仍用 2-arg `write_package` + 2-tuple `read_package`——T3 才改签名。）

- [ ] **Step 5: 跑测验证绿**

Run: `cargo test -p loomgui_core`
Expected: PASS（texture 新测 + layout/render/stage 改后的测全绿；blob 测不受影响）。

- [ ] **Step 6: Commit**

```bash
git add loomgui_core/src/asset/texture.rs loomgui_core/src/render/mod.rs loomgui_core/src/layout/mod.rs loomgui_core/src/stage.rs
git commit -m "$(cat <<'EOF'
refactor(v1b.3-T1): TexMeta 加 uv_min/uv_max + TextureRegistry 改 insert

atlas 模型：src→TexMeta{tex_id,uv_min,uv_max,w,h}。删 register/next_id
（atlas 由 load_package 时 build_registry 建，T3）。render/layout/stage
测 register→insert。measure 生产零改（字段名 width/height 不变）。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: render UV 烘焙 — quad 接 uv_rect + Image 按 region 烤

**Files:**
- Modify: `loomgui_core/src/render/mesh.rs`（quad 加 uv_min/uv_max 参数）
- Modify: `loomgui_core/src/render/mod.rs`（Image 分支按 registry uv 烤；Container/Button 全图 UV）

**Interfaces:**
- Consumes: T1 `TexMeta { uv_min, uv_max, .. }`、`TextureRegistry::get`。
- Produces: `mesh::quad(rect, color, uv_min, uv_max)`；Image `RenderNode.Mesh.uvs` = 4 角 region UV。

- [ ] **Step 1: 改 `loomgui_core/src/render/mesh.rs::quad` 签名 + UV**

把 `quad` 改为接 uv_rect（替换模块 doc 里「UV 占位全图」注释为「uv_min/uv_max 指定，全图=0,0,1,1」）：

```rust
/// 生成一个 quad 的 verts/uvs/colors/indices。
///
/// - 4 顶点（左上 → 右上 → 右下 → 左下，CCW）。
/// - UV 由 uv_min/uv_max 指定：TL→(umin,vmin) TR→(umax,vmin) BR→(umax,vmax) BL→(umin,vmax)。
///   纯色块/散图全图 = [0,0],[1,1]；atlas sprite = 子区（v1b.3）。
/// - 4 顶点同色。两三角形 (0,1,2)+(0,2,3)。
#[allow(clippy::type_complexity)]
pub fn quad(
    rect: &Rect,
    color: [f32; 4],
    uv_min: [f32; 2],
    uv_max: [f32; 2],
) -> (Vec<[f32; 2]>, Vec<[f32; 2]>, Vec<[f32; 4]>, Vec<u32>) {
    let verts = vec![
        [rect.x, rect.y],
        [rect.x + rect.w, rect.y],
        [rect.x + rect.w, rect.y + rect.h],
        [rect.x, rect.y + rect.h],
    ];
    let (umin, vmin) = (uv_min[0], uv_min[1]);
    let (umax, vmax) = (uv_max[0], uv_max[1]);
    let uvs = vec![[umin, vmin], [umax, vmin], [umax, vmax], [umin, vmax]];
    let colors = vec![color; 4];
    let indices = vec![0, 1, 2, 0, 2, 3];
    (verts, uvs, colors, indices)
}
```

更新 mesh.rs 测：`quad_uv_full_image` 改名 `quad_uv_uses_min_max`，传 `[0.0,0.0],[1.0,1.0]` 断言全图；新增 `quad_uv_subregion`：
```rust
#[test]
fn quad_uv_subregion() {
    let (_, uvs, _, _) = quad(&Rect::default(), [0.0; 4], [0.25, 0.5], [0.75, 1.0]);
    assert_eq!(uvs[0], [0.25, 0.5]);   // TL
    assert_eq!(uvs[1], [0.75, 0.5]);   // TR
    assert_eq!(uvs[2], [0.75, 1.0]);   // BR
    assert_eq!(uvs[3], [0.25, 1.0]);   // BL
}
```
其余 mesh 测（`quad_four_verts_two_tris`/`quad_verts_at_corners`/`quad_colors_uniform`）的 `quad(...)` 调用补 `[0.0,0.0],[1.0,1.0]` 两参。

- [ ] **Step 2: 改 `loomgui_core/src/render/mod.rs`——Container/Button + Image 分支**

Container/Button 分支（现 `quad(rect, color)`）改为 `quad(rect, color, [0.0, 0.0], [1.0, 1.0])`（纯色块全图 UV）。

Image 分支改为按 registry uv 烤：
```rust
NodeKind::Image { src } => {
    let (tex_id, uv_min, uv_max) = match textures.get(src) {
        Some(m) => (m.tex_id, m.uv_min, m.uv_max),
        None => (0u32, [0.0, 0.0], [1.0, 1.0]),   // 未注册哨兵：tex_id=0 白占位（UV 无关）
    };
    let (v, uvc, col, idx) = crate::render::mesh::quad(rect, [1.0, 1.0, 1.0, 1.0], uv_min, uv_max);
    rn.payload = NodePayload::Mesh {
        verts: v, uvs: uvc, colors: col, indices: idx,
        texture: tex_id, program: 0,
    };
}
```

- [ ] **Step 3: 加 render UV 断言测（render/mod.rs 测）**

`build_image_uses_registered_tex_id` 加 UV 断言（insert 时给非平凡 uv）：
```rust
tex.insert("logo.png", TexMeta {
    tex_id: 1, uv_min: [0.25, 0.0], uv_max: [0.5, 1.0], width: 200, height: 100,
});
// ... build_render_nodes ...
match &rns[0].payload {
    NodePayload::Mesh { texture, uvs, .. } => {
        assert_eq!(*texture, tid);
        assert_eq!(uvs[0], [0.25, 0.0]);   // TL
        assert_eq!(uvs[2], [0.5, 1.0]);    // BR
    }
    _ => panic!("expected Mesh"),
}
```
新增 `build_image_unregistered_uv_is_full`：未注册 Image → `uvs` 仍 (0,0)-(1,1)（哨兵）。

- [ ] **Step 4: 跑测验证绿**

Run: `cargo test -p loomgui_core`
Expected: PASS（mesh subregion 测 + render UV 断言测 + 现有测全绿）。

- [ ] **Step 5: Commit**

```bash
git add loomgui_core/src/render/mesh.rs loomgui_core/src/render/mod.rs
git commit -m "$(cat <<'EOF'
feat(v1b.3-T2): render quad 按 uv_region 烤 — atlas 子区 UV

mesh::quad 接 uv_min/uv_max；Image 分支按 registry.get(src) 的 uv_region
烤 4 角 UV（未注册→全图哨兵）；Container/Button 仍全图 UV。blob 走既有
per-vertex uv 路径（v3 格式不改）。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: .pkg.bin v2 + Stage 集成

**Files:**
- Modify: `loomgui_core/src/asset/mod.rs`（AtlasSection/Info/Sprite + build_registry + write/read_package v2 + version 2）
- Modify: `loomgui_core/src/stage.rs`（删 src_cache/image_srcs/rebuild_src_cache；加 atlases 字段；load_package build_registry；load_inline clear）
- Modify（caller ripple）: `loomgui_pkg/tests/pack.rs`、`loomgui_ffi_c/src/lib.rs` 测（write_package 3-arg + read_package 3-tuple）、`loomgui_core/src/stage.rs` golden（write_package 3-arg）

**Interfaces:**
- Consumes: T1 `TexMeta/TextureRegistry`。
- Produces: `AtlasSection { atlases: Vec<AtlasInfo>, sprites: Vec<AtlasSprite> }`、`AtlasInfo { filename, width, height }`、`AtlasSprite { src, x, y, w, h }`；`build_registry(&AtlasSection) -> TextureRegistry`；`write_package(&Scene, (f32,f32), &AtlasSection) -> Vec<u8>`；`read_package(&[u8]) -> Result<(Scene, (f32,f32), AtlasSection), PkgError>`；`Stage.atlases: Vec<AtlasInfo>`。

- [ ] **Step 1: `asset/mod.rs` 加 AtlasSection 类型 + version 2 + build_registry**

`PKG_FORMAT_VERSION` 改 `2`；`MIN_VERSION=2, MAX_VERSION=2`。在 `pub mod texture;` 后加类型定义：

```rust
/// atlas 元数据（.pkg.bin v2 AtlasSection 的内存形）。
#[derive(Debug, Clone, PartialEq)]
pub struct AtlasSection {
    pub atlases: Vec<AtlasInfo>,   // 甲-B len=1
    pub sprites: Vec<AtlasSprite>, // 全部 sprite（甲-B 都属 atlas 0）
}
impl Default for AtlasSection {
    fn default() -> Self { AtlasSection { atlases: Vec::new(), sprites: Vec::new() } }
}
#[derive(Debug, Clone, PartialEq)]
pub struct AtlasInfo { pub filename: String, pub width: u32, pub height: u32 }
#[derive(Debug, Clone, PartialEq)]
pub struct AtlasSprite { pub src: String, pub x: u32, pub y: u32, pub w: u32, pub h: u32 }

/// 从 AtlasSection 建 TextureView registry。甲-B 单图集：atlas[0] 得 tex_id=1，所有 sprite 属它。
/// （多图集 v1b.4+ 需 sprite 带 atlas_idx 才能分流。）
pub fn build_registry(section: &AtlasSection) -> crate::asset::texture::TextureRegistry {
    let mut reg = crate::asset::texture::TextureRegistry::default();
    if section.atlases.is_empty() { return reg; }   // 无 atlas（inline / 空）→ 空 registry
    let atlas = &section.atlases[0];
    let atlas_tex_id = 1u32;
    let aw = atlas.width as f32; let ah = atlas.height as f32;
    for spr in &section.sprites {
        let uv_min = [spr.x as f32 / aw, spr.y as f32 / ah];
        let uv_max = [(spr.x + spr.w) as f32 / aw, (spr.y + spr.h) as f32 / ah];
        reg.insert(&spr.src, crate::asset::texture::TexMeta {
            tex_id: atlas_tex_id, uv_min, uv_max, width: spr.w, height: spr.h,
        });
    }
    reg
}
```

- [ ] **Step 2: `asset/mod.rs` write_package v2（加 atlas section）**

签名改 `pub fn write_package(scene: &Scene, root_size: (f32, f32), atlas: &AtlasSection) -> Vec<u8>`。在 stringTable 收集阶段，把 atlas filename + sprite src 一并 intern（sprite.src 通常已在 NodeBlock intern；filename 新 intern）。NodeBlock 之后追加 AtlasSection：

```rust
    // —— AtlasSection（v2 新增，NodeBlock 之后）——
    // atlas_count + 每 atlas{filename_idx,u16; w,u32; h,u32}
    //   + sprite_count + 每 sprite{src_idx,u16; x,y,w,h 各 u32}
    let mut atlas_idx_of: std::collections::HashMap<String, u16> = std::collections::HashMap::new();
    let mut atlas_filename_idx = |name: &str| intern(name, &mut strings, &mut atlas_idx_of);
    out.extend_from_slice(&(atlas.atlases.len() as u32).to_le_bytes());
    for a in &atlas.atlases {
        out.extend_from_slice(&atlas_filename_idx(&a.filename).to_le_bytes());
        out.extend_from_slice(&a.width.to_le_bytes());
        out.extend_from_slice(&a.height.to_le_bytes());
    }
    out.extend_from_slice(&(atlas.sprites.len() as u32).to_le_bytes());
    for s in &atlas.sprites {
        out.extend_from_slice(&intern(&s.src, &mut strings, &mut idx_of).to_le_bytes());
        out.extend_from_slice(&s.x.to_le_bytes());
        out.extend_from_slice(&s.y.to_le_bytes());
        out.extend_from_slice(&s.w.to_le_bytes());
        out.extend_from_slice(&s.h.to_le_bytes());
    }
    out
}
```
（`idx_of` 是现有 NodeBlock 的 interning map；filename 用独立 map 避免借用冲突，或复用 `idx_of`——两者皆 `intern`，可共用同一 `strings`/`idx_of`。实现时统一用现有 `idx_of` + `intern` 即可，filename 也进同一 stringTable。简化：删 `atlas_idx_of`，filename 直接 `intern(&a.filename, &mut strings, &mut idx_of)`。）

- [ ] **Step 3: `asset/mod.rs` read_package v2（读 atlas section）**

签名改 `pub fn read_package(bytes: &[u8]) -> Result<(Scene, (f32, f32), AtlasSection), PkgError>`。NodeBlock 读完后追加：

```rust
    // —— AtlasSection ——
    let atlas_count = r.u32("atlas_count")? as usize;
    let mut atlases: Vec<AtlasInfo> = Vec::with_capacity(atlas_count);
    for _ in 0..atlas_count {
        let fidx = r.u16("atlas_filename_idx")?;
        let w = r.u32("atlas_w")?;
        let h = r.u32("atlas_h")?;
        atlases.push(AtlasInfo { filename: string_at(&strings, fidx)?, width: w, height: h });
    }
    let sprite_count = r.u32("sprite_count")? as usize;
    let mut sprites: Vec<AtlasSprite> = Vec::with_capacity(sprite_count);
    for _ in 0..sprite_count {
        let sidx = r.u16("sprite_src_idx")?;
        let x = r.u32("sprite_x")?; let y = r.u32("sprite_y")?;
        let w = r.u32("sprite_w")?; let h = r.u32("sprite_h")?;
        sprites.push(AtlasSprite { src: string_at(&strings, sidx)?, x, y, w, h });
    }
    let scene = Scene::build(&entries);
    Ok((scene, (root_w, root_h), AtlasSection { atlases, sprites }))
}
```

- [ ] **Step 4: `asset/mod.rs` 测——v2 round-trip + version 拒绝**

更新现有测的 `write_package`/`read_package` 调用（3-arg/3-tuple）。新增：
```rust
#[test]
fn atlas_section_round_trips() {
    let entries = vec![(None, NodeKind::Container, ResolvedStyle::default())];
    let scene = Scene::build(&entries);
    let atlas = AtlasSection {
        atlases: vec![AtlasInfo { filename: "a.atlas.png".into(), width: 512, height: 256 }],
        sprites: vec![
            AtlasSprite { src: "x.png".into(), x: 0, y: 0, w: 64, h: 32 },
            AtlasSprite { src: "y.png".into(), x: 64, y: 0, w: 100, h: 200 },
        ],
    };
    let bytes = write_package(&scene, (10.0, 10.0), &atlas);
    let (_s, _rs, a2) = read_package(&bytes).unwrap();
    assert_eq!(a2, atlas);
}
```
现有 `read_rejects_unsupported_version` 测：把 `bytes[4..8]` 改成 `1`（too old）+ `3`（too new）——`1` 现在是 TooOld（MIN=2），`3` 是 TooNew。调整断言：version=1 → TooOld；version=3 → TooNew。

- [ ] **Step 5: `stage.rs`——删 src_cache，加 atlases，load_package build_registry**

`Stage` 结构：
- 删 `src_cache: Vec<String>` 字段。
- 加 `pub atlases: Vec<crate::asset::AtlasInfo>` 字段（FFI T5 读）。
`Stage::new`：删 `src_cache: Vec::new()`，加 `atlases: Vec::new()`。
删 `rebuild_src_cache` 方法 + `image_srcs` 方法。
`load_inline`：`self.textures.clear(); self.atlases.clear();`（删 `rebuild_src_cache()`）。
`load_package`：
```rust
pub fn load_package(&mut self, bytes: &[u8]) -> Result<(), String> {
    let (scene, root_size, atlas_section) = crate::asset::read_package(bytes).map_err(|e| e.to_string())?;
    self.textures = crate::asset::build_registry(&atlas_section);
    self.atlases = atlas_section.atlases;
    self.scene = Some(scene);
    self.root_size = root_size;
    Ok(())
}
```
（删开头的 `self.textures.clear()`——`build_registry` 返新 registry 替换。）
`tick_and_render`：不变（仍 `&self.textures`）。
golden 测 `package_load_renders_identical_to_inline`：`write_package(scene, (200.0, 100.0))` → `write_package(scene, (200.0, 100.0), &crate::asset::AtlasSection::default())`（空 atlas；img 由测手工 insert 覆盖）。`read_package` 已在 load_package 内部（测不直接调 read）。测仍 `insert("logo.png", ...)`（T1 已改）。

- [ ] **Step 6: caller ripple — pkg 测 + ffi 测**

`loomgui_pkg/tests/pack.rs`：`let (scene, rs) = read_package(&bytes)` → `let (scene, rs, _atlas) = read_package(&bytes)`。

`loomgui_ffi_c/src/lib.rs` 测 `load_package_builds_blob_from_package` + `register_texture_and_collect_image_srcs` 里的 `write_package(&Scene::build(&entries), (100.0, 50.0))` → 加 `, &loomgui_core::asset::AtlasSection::default()`（T5 会删 register 测，本 task 只让它编译）。

- [ ] **Step 7: 跑测验证绿（全 workspace）**

Run: `cargo test --workspace`
Expected: PASS（core asset/stage/render/layout + pkg + ffi 全绿；blob 不受影响）。

- [ ] **Step 8: 构建矩阵 R1 门**

Run: `cargo build -p loomgui_core --no-default-features && cargo build -p loomgui_ffi_c --no-default-features`
Expected: 两 crate 皆编（atlas/build_registry 常驻不依赖 parse；注：build_registry 不用 parse，write/read_package 也不用）。

- [ ] **Step 9: Commit**

```bash
git add loomgui_core/src/asset/mod.rs loomgui_core/src/stage.rs loomgui_pkg/tests/pack.rs loomgui_ffi_c/src/lib.rs
git commit -m "$(cat <<'EOF'
feat(v1b.3-T3): .pkg.bin v2 AtlasSection + Stage 集成 build_registry

AtlasSection{atlases,sprites} 序列化进 .pkg.bin 末段；version 1→2（旧 v1
TooNew 拒）。build_registry 从表建 TextureRegistry（atlas[0]→tex_id 1）。
Stage 删 src_cache/image_srcs（loose collect 取代），加 atlases 字段；
load_package 建 registry。pkg/ffi 测 caller 跟改。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 打包器 — image crate + shelf 打包 + atlas.png

**Files:**
- Modify: `loomgui_pkg/Cargo.toml`（+ `image`）
- Rewrite: `loomgui_pkg/src/lib.rs`（pack 4-arg + shelf_pack + atlas.png + PackedPackage）
- Modify: `loomgui_pkg/src/main.rs`（res_dir = html 目录；写两文件）
- Modify: `loomgui_pkg/tests/pack.rs`（新 pack 签名 + atlas 测）

**Interfaces:**
- Consumes: T3 `write_package(&Scene, (f32,f32), &AtlasSection)`、`AtlasSection/Info/Sprite`；core parse/build_scene。
- Produces: `pack(html, css, root_size, res_dir) -> Result<PackedPackage, String>`；`PackedPackage { pkg_bytes, atlas_png, atlas_filename }`；`shelf_pack(sprites) -> (atlas_w, atlas_h, Vec<PlacedSprite>)`。

- [ ] **Step 1: `loomgui_pkg/Cargo.toml` 加 image**

```toml
[dependencies]
loomgui_core = { path = "../loomgui_core", features = ["parse"] }
image = "0.25"
```
（implementer 取当前 stable；若 0.25 API 与下方伪码不符，按 §3 API 适配记 knowledge——核心算法不变。）

- [ ] **Step 2: 重写 `loomgui_pkg/src/lib.rs`**

```rust
//! 打包器库（spec §5）：HTML+CSS+散图 → .pkg.bin + atlas.png。
//! 复用 core parse/style/scene + asset::write_package；新加 image crate 解码/编码 PNG + shelf 打包。

use loomgui_core::asset::{AtlasInfo, AtlasSection, AtlasSprite};
use std::path::Path;

/// 打包产物：.pkg.bin bytes + atlas.png bytes + atlas 相对文件名（写进 .pkg.bin header）。
pub struct PackedPackage {
    pub pkg_bytes: Vec<u8>,
    pub atlas_png: Vec<u8>,
    pub atlas_filename: String,
}

/// 一个 sprite 在 shelf 打包后的位置（atlas 像素坐标，y-down）。
struct PlacedSprite { src: String, x: u32, y: u32, w: u32, h: u32 }

/// 把 HTML+CSS+res_dir 下散图打成 .pkg.bin + atlas.png。
/// res_dir = 解析 <img src> 的基准目录（CLI 传 html_path.parent()）。
/// 缺图 → Err（build-time fail，§5.5）。
pub fn pack(html: &str, css: &str, root_size: (f32, f32), res_dir: &Path) -> Result<PackedPackage, String> {
    let tree = loomgui_core::parse::dom::parse_html(html).map_err(|e| format!("parse_html: {e}"))?;
    let sheet = loomgui_core::parse::css::parse_css(css).map_err(|e| format!("parse_css: {e}"))?;
    let styles = loomgui_core::style::cascade::resolve_styles(&tree, &sheet);
    let scene = loomgui_core::scene::build_scene(&tree, &styles);

    // 1. 收集 Image src（DFS 先序去重）—— scene.nodes 已 DFS 先序。
    let mut srcs: Vec<String> = Vec::new();
    let mut seen = std::collections::HashSet::new();
    for n in &scene.nodes {
        if let loomgui_core::scene::NodeKind::Image { src } = &n.kind {
            if seen.insert(src.as_str()) { srcs.push(src.clone()); }
        }
    }

    // 2. 无图 → 空 atlas（atlas_count=0）。pkg.bin 仍 v2，Unity load 空 atlas 跳过。
    if srcs.is_empty() {
        let pkg = loomgui_core::asset::write_package(&scene, root_size, &AtlasSection::default());
        return Ok(PackedPackage {
            pkg_bytes: pkg,
            atlas_png: Vec::new(),
            atlas_filename: String::new(),
        });
    }

    // 3. 解码每张 PNG（image crate）→ (src, w, h, RgbaImage)。
    let mut decoded: Vec<(String, u32, u32, image::RgbaImage)> = Vec::with_capacity(srcs.len());
    for src in &srcs {
        let path = res_dir.join(src);
        let img = image::open(&path).map_err(|e| format!("image not found: {src} ({e})"))?;
        let rgba = img.to_rgba8();
        let w = rgba.width(); let h = rgba.height();
        decoded.push((src.clone(), w, h, rgba));
    }

    // 4. shelf 打包。
    let mut dims: Vec<(String, u32, u32)> = decoded.iter().map(|(s, w, h, _)| (s.clone(), *w, *h)).collect();
    let (atlas_w, atlas_h, placed) = shelf_pack(&mut dims);

    // 5. blit 进 atlas buffer（RGBA8）。
    let mut buf = vec![0u8; (atlas_w * atlas_h * 4) as usize];
    for (src, _w, _h, rgba) in &decoded {
        let p = placed.iter().find(|p| &p.src == src).unwrap();
        for row in 0..p.h {
            for col in 0..p.w {
                let px = rgba.get_pixel(col, row).0;
                let di = (((p.y + row) * atlas_w + (p.x + col)) * 4) as usize;
                buf[di..di + 4].copy_from_slice(&px);
            }
        }
    }

    // 6. 编码 atlas.png 到内存。
    let atlas_img = image::RgbaImage::from_raw(atlas_w, atlas_h, buf)
        .ok_or_else(|| String::from("atlas buffer size mismatch"))?;
    let atlas_png = image::save_buffer_to_memory(&atlas_img, image::ImageFormat::Png)
        .map_err(|e| format!("encode atlas png: {e}"))?;

    // 7. AtlasSection + write_package v2。
    let atlas_section = AtlasSection {
        atlases: vec![AtlasInfo { filename: "loom.atlas.png".into(), width: atlas_w, height: atlas_h }],
        sprites: placed.iter().map(|p| AtlasSprite {
            src: p.src.clone(), x: p.x, y: p.y, w: p.w, h: p.h,
        }).collect(),
    };
    let pkg = loomgui_core::asset::write_package(&scene, root_size, &atlas_section);

    Ok(PackedPackage {
        pkg_bytes: pkg,
        atlas_png,
        atlas_filename: "loom.atlas.png".into(),
    })
}

/// shelf 打包：按高降序、atlas_w=max(512,最宽)、逐行摆、超宽换行。NPOT。无旋转/trim。
fn shelf_pack(sprites: &mut Vec<(String, u32, u32)>) -> (u32, u32, Vec<PlacedSprite>) {
    const DEFAULT_ATLAS_W: u32 = 512;
    sprites.sort_by(|a, b| b.2.cmp(&a.2));   // 按高降序
    let atlas_w = sprites.iter().map(|(_, _, w)| *w).max().unwrap_or(0).max(DEFAULT_ATLAS_W);
    let mut placed = Vec::with_capacity(sprites.len());
    let mut x = 0u32; let mut y = 0u32; let mut shelf_h = 0u32;
    for (src, w, h) in sprites.iter() {
        if x + w > atlas_w { y += shelf_h; x = 0; shelf_h = 0; }
        placed.push(PlacedSprite { src: src.clone(), x, y, w: *w, h: *h });
        x += w;
        shelf_h = shelf_h.max(*h);
    }
    (atlas_w, y + shelf_h, placed)
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn shelf_pack_no_overlap_within_bounds() {
        let mut s = vec![
            ("a".into(), 100, 50), ("b".into(), 200, 80), ("c".into(), 300, 30),
        ];
        let (aw, ah, placed) = shelf_pack(&mut s);
        // 不出界
        for p in &placed { assert!(p.x + p.w <= aw && p.y + p.h <= ah); }
        // 两两不重叠
        for i in 0..placed.len() {
            for j in (i + 1)..placed.len() {
                let (a, b) = (&placed[i], &placed[j]);
                let overlap = a.x < b.x + b.w && b.x < a.x + a.w && a.y < b.y + b.h && b.y < a.y + a.h;
                assert!(!overlap, "sprites {a:?} {b:?} overlap");
            }
        }
        // atlas_w 至少 = 最宽 sprite（300）
        assert!(aw >= 300);
    }
}
```

> **§3 API 适配**：`image::save_buffer_to_memory` 在某些版本叫 `ImageBuffer::save_to_memory` 或需 `PngEncoder`；`from_raw` 签名稳定。implementer 验 `image` 0.25 实际 API，调测过即可——算法（decode→shelf→blit→encode）不变。

- [ ] **Step 3: 改 `loomgui_pkg/src/main.rs`（res_dir + 写两文件）**

`main()` 把 `pack(&html, &css, (w, h))` 改为：
```rust
let html_path = std::path::PathBuf::from(html_path);
let res_dir = html_path.parent().unwrap_or_else(|| std::path::Path::new("."));
let html = match fs::read_to_string(&html_path) { /* ... */ };
// ... css ...
match loomgui_pkg::pack(&html, &css, (w, h), res_dir) {
    Ok(p) => {
        fs::write(&out_path, &p.pkg_bytes).map_err(|e| eprintln!("write {out_path}: {e}"))?;
        let atlas_path = std::path::PathBuf::from(&out_path).with_extension("atlas.png");
        if !p.atlas_png.is_empty() {
            fs::write(&atlas_path, &p.atlas_png).map_err(|e| eprintln!("write {atlas_path:?}: {e}"))?;
        }
        eprintln!("wrote {out_path} ({} bytes) + atlas ({} bytes)", p.pkg_bytes.len(), p.atlas_png.len());
        ExitCode::SUCCESS
    }
    Err(e) => { eprintln!("pack: {e}"); ExitCode::FAILURE }
}
```
（html_path 由 `args[1]` 改 `PathBuf`；保持 `-o/-w/-h` 参数解析。）

- [ ] **Step 4: 改 `loomgui_pkg/tests/pack.rs`（新签名 + atlas 测）**

```rust
use loomgui_core::asset::{read_package, AtlasSection, PKG_MAGIC};
use loomgui_pkg::pack;

#[test]
fn pack_produces_valid_package_roundtrips() {
    // 无图 scene：pack 仍产 v2 pkg（空 atlas），不需 res_dir 有图。
    let html = r#"<div class="c"><span>hi</span></div>"#;
    let css = ".c{width:200px;height:100px;background-color:#ff0000;}";
    let res_dir = std::path::Path::new(".");
    let p = pack(html, css, (200.0, 100.0), res_dir).expect("pack ok");
    assert_eq!(u32::from_le_bytes(p.pkg_bytes[0..4].try_into().unwrap()), PKG_MAGIC);
    let (scene, rs, atlas) = read_package(&p.pkg_bytes).expect("read ok");
    assert_eq!(rs, (200.0, 100.0));
    assert!(atlas.atlases.is_empty(), "无图 → 空 atlas");
    assert!(scene.roots.len() >= 1);
}

#[test]
fn pack_with_images_builds_atlas_and_section() {
    // fixtures：两张测试 PNG（implementer 在 loomgui_pkg/tests/fixtures/ 放 a.png(4x4 红)、b.png(2x2 绿)）。
    let html = r#"<div><img src="a.png"><img src="b.png"></div>"#;
    let css = "";
    let res_dir = std::path::Path::new(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures");
    let p = pack(html, css, (100.0, 100.0), &res_dir).expect("pack ok");
    assert!(!p.atlas_png.is_empty(), "有图 → atlas 非空");
    let (_scene, _rs, atlas) = read_package(&p.pkg_bytes).unwrap();
    assert_eq!(atlas.atlases.len(), 1);
    assert_eq!(atlas.sprites.len(), 2);
    // PNG round-trip：decode atlas，验 a.png 的 region 像素 == 红。
    let atlas_img = image::load_from_memory(&p.atlas_png).unwrap().to_rgba8();
    let a = &atlas.sprites[0];
    let px = atlas_img.get_pixel(a.x, a.y).0;
    assert_eq!(px, [255, 0, 0, 255], "a.png region 左上像素应红");
}

#[test]
fn pack_missing_image_fails() {
    let html = r#"<div><img src="nope.png"></div>"#;
    let res_dir = std::path::Path::new(".");
    let r = pack(html, "", (10.0, 10.0), res_dir);
    assert!(r.is_err(), "缺图 build-time fail");
}
```

implementer 额外建 `loomgui_pkg/tests/fixtures/a.png`（4×4 红）+ `b.png`（2×2 绿）——用 `image` crate 在一个一次性 `#[test] fn make_fixtures()` 里生成（或手工 commit 小 PNG）。fixtures 生成测示例：
```rust
#[test]
fn write_test_fixtures_once() {   // helper：手写无 fixture 时跑一次生成
    use image::{ImageBuffer, Rgba};
    let a: ImageBuffer<Rgba<u8>, Vec<u8>> = ImageBuffer::from_pixel(4, 4, Rgba([255, 0, 0, 255]));
    a.save("tests/fixtures/a.png").unwrap();
    let b: ImageBuffer<Rgba<u8>, Vec<u8>> = ImageBuffer::from_pixel(2, 2, Rgba([0, 255, 0, 255]));
    b.save("tests/fixtures/b.png").unwrap();
}
```
（跑此测生成 fixtures 后 commit PNGs，再删此 helper 测——或保留作 regen。implementer 定。）

- [ ] **Step 5: 跑测验证绿**

Run: `cargo test -p loomgui_pkg`
Expected: PASS（shelf no-overlap + pack e2e + atlas round-trip + missing fail）。

- [ ] **Step 6: Commit**

```bash
git add loomgui_pkg/Cargo.toml loomgui_pkg/src/lib.rs loomgui_pkg/src/main.rs loomgui_pkg/tests/pack.rs loomgui_pkg/tests/fixtures
git commit -m "$(cat <<'EOF'
feat(v1b.3-T4): 打包器 image crate + shelf 打包 atlas.png

pack(html,css,root_size,res_dir) → PackedPackage{pkg_bytes, atlas_png,
atlas_filename}。image crate 解码散图 → shelf 打包（NPOT/无旋转/trim）
→ blit 进 atlas buffer → 编码 PNG。AtlasSprite 表写进 .pkg.bin v2。
缺图 build-time fail。fixtures a.png/b.png + round-trip 测。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: FFI — 删 loose collect/register，加 atlas_count/info

**Files:**
- Modify: `loomgui_ffi_c/src/lib.rs`（删 `image_src_count`/`image_src_at`/`register_texture`；加 `atlas_count`/`atlas_info`；version 串 v1b.2→v1b.3；删 register 测、加 atlas 测）

**Interfaces:**
- Consumes: T3 `Stage.atlases: Vec<AtlasInfo>`。
- Produces: `loomgui_stage_atlas_count(h) -> usize`；`loomgui_stage_atlas_info(h, i, out_tex_id, out_w, out_h, out_src_len) -> *const u8`。

- [ ] **Step 1: version 串 v1b.2→v1b.3 + 改测**

`loomgui_version()` 内 `CString::new("v1b.2")` → `"v1b.3"`。测 `version_returns_c_string_v1b`：断言 `"v1b.3"`（测名保留 cosmetic，或改 `_v1b3`）。

- [ ] **Step 2: 删 loose FFI**

删 `loomgui_stage_register_texture`（行 121-136）+ `loomgui_stage_image_src_count`（139-144）+ `loomgui_stage_image_src_at`（148-161）。删测 `register_texture_and_collect_image_srcs`（322-370）。

- [ ] **Step 3: 加 atlas_count + atlas_info FFI**

在 `loomgui_stage_load_package` 之后加：

```rust
/// atlas 数量（甲-B 恒 1，无图 scene = 0）。
#[no_mangle]
pub extern "C" fn loomgui_stage_atlas_count(h: *const StageHandle) -> usize {
    if h.is_null() { return 0; }
    let sh = unsafe { &*h };
    sh.stage.atlases.len()
}

/// 第 i 个 atlas 信息。返 atlas filename UTF-8 串指针（**无尾 NUL** + *out_src_len=字节长）；
/// *out_tex_id = core 分配的 atlas tex_id（= i+1）；*out_w/*out_h = atlas 像素尺寸。
/// OOB / null → null。串归 Stage 拥有，下次 load 前有效（坑16 len-based 读契约）。
#[no_mangle]
pub extern "C" fn loomgui_stage_atlas_info(
    h: *const StageHandle,
    index: usize,
    out_tex_id: *mut u32,
    out_w: *mut u32,
    out_h: *mut u32,
    out_src_len: *mut usize,
) -> *const u8 {
    if h.is_null() { return std::ptr::null(); }
    let sh = unsafe { &*h };
    if index >= sh.stage.atlases.len() { return std::ptr::null(); }
    let a = &sh.stage.atlases[index];
    unsafe {
        if !out_tex_id.is_null() { *out_tex_id = (index as u32) + 1; }   // atlas[0]→tex_id 1
        if !out_w.is_null() { *out_w = a.width; }
        if !out_h.is_null() { *out_h = a.height; }
        if !out_src_len.is_null() { *out_src_len = a.filename.len(); }
    }
    a.filename.as_ptr()
}
```

- [ ] **Step 4: 加 atlas FFI 测（替代删掉的 register 测）**

在 `abi_tests` mod 加（手搓含 atlas 的包——直接构 AtlasSection + write_package）：

```rust
#[test]
fn atlas_count_and_info_round_trip() {
    use loomgui_core::asset::{write_package, AtlasInfo, AtlasSection, AtlasSprite};
    use loomgui_core::scene::{NodeKind, Scene};
    use loomgui_core::style::resolved::ResolvedStyle;
    let (fp, fplen) = font_path();
    let entries = vec![
        (None, NodeKind::Container, ResolvedStyle::default()),
        (Some(0), NodeKind::Image { src: "a.png".into() }, ResolvedStyle::default()),
        (Some(0), NodeKind::Image { src: "b.png".into() }, ResolvedStyle::default()),
    ];
    let atlas = AtlasSection {
        atlases: vec![AtlasInfo { filename: "loom.atlas.png".into(), width: 512, height: 256 }],
        sprites: vec![
            AtlasSprite { src: "a.png".into(), x: 0, y: 0, w: 64, h: 32 },
            AtlasSprite { src: "b.png".into(), x: 64, y: 0, w: 100, h: 200 },
        ],
    };
    let pkg = write_package(&Scene::build(&entries), (100.0, 50.0), &atlas);

    let h = loomgui_stage_new(fp.as_ptr() as *const u8, fplen, 100.0, 50.0);
    assert!(!h.is_null());
    assert_eq!(loomgui_stage_load_package(h, pkg.as_ptr(), pkg.len()), 0);

    assert_eq!(loomgui_stage_atlas_count(h), 1);
    let mut tid = 0u32; let mut w = 0u32; let mut hh = 0u32; let mut slen = 0usize;
    let p = loomgui_stage_atlas_info(h, 0, &mut tid, &mut w, &mut hh, &mut slen);
    assert!(!p.is_null());
    assert_eq!(tid, 1, "atlas[0] → tex_id 1");
    assert_eq!((w, hh), (512, 256));
    let fname = unsafe { std::str::from_utf8(std::slice::from_raw_parts(p, slen)).unwrap() };
    assert_eq!(fname, "loom.atlas.png");
    // OOB → null
    assert!(loomgui_stage_atlas_info(h, 99, &mut tid, &mut w, &mut hh, &mut slen).is_null());

    loomgui_stage_free(h);
}
```

- [ ] **Step 5: 跑测验证绿**

Run: `cargo test -p loomgui_ffi_c`
Expected: PASS（version v1b.3 + atlas_count/info round-trip + 现有 load_package/borrow_frame 测；register 测已删）。

- [ ] **Step 6: 构建矩阵 + 重新生成 csbindgen 绑定**

Run: `cargo build -p loomgui_ffi_c --no-default-features && cargo build -p loomgui_ffi_c`
Expected: 两配置皆编。csbindgen 绑定（`Native` 类，gitignored）会在 Unity 侧重新生成——T6 确认 C# `Native` 有 `atlas_count`/`atlas_info`、无 `image_src*`/`register_texture`。

- [ ] **Step 7: Commit**

```bash
git add loomgui_ffi_c/src/lib.rs
git commit -m "$(cat <<'EOF'
feat(v1b.3-T5): FFI atlas_count/atlas_info 取代 loose collect/register

删 image_src_count/at + register_texture（loose 散图模型被 atlas 取代）；
加 atlas_count + atlas_info（返 filename 无尾 NUL 串 + tex_id=i+1 + dims，
坑16 len-based 契约）。version 串 v1b.2→v1b.3。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Unity — LoomStage.LoadAtlas + EditMode 测

**Files:**
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs`（`LoadTextures`→`LoadAtlas`）
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/Bindings/Native.cs`（csbindgen 重新生成——确认 `Native` 类更新；此文件惯例 gitignored 或入库，按仓库实际）
- Create: `loomgui_unity/Assets/LoomGUI/Tests/AtlasMirrorPoolTests.cs`

**Interfaces:**
- Consumes: T5 `atlas_count`/`atlas_info` FFI（C# `Native`）；v3 blob（atlas UV 在 mesh_arena）。
- Produces: `_texMap[atlas_tex_id]` 填 atlas Texture2D；MirrorPool 不改（已应用 blob UV）。

- [ ] **Step 1: 重新生成 csbindgen C# 绑定 + 确认 Native**

按仓库既有流程重新生成 `Native` 绑定（通常编辑期 csbindgen 跑或手动同步）。确认：
- 有 `extern fn loomgui_stage_atlas_count(...) -> nuint` + `loomgui_stage_atlas_info(...)`（签名为 `byte*` 返 + `uint*`/`nuint*` out）。
- 删 `image_src_count`/`image_src_at`/`register_texture`（T5 已删 FFI）。
- 若 `Native` 是 gitignored（运行时生成），本步在 Unity 编译时自动；若入库，手工同步。

- [ ] **Step 2: 改 `LoomStage.cs`——LoadTextures→LoadAtlas**

`LoadTextures()` 整体替换为 `LoadAtlas()`（保留方法签名 `void LoadAtlas()`，Awake 的 `if (_usePackage) LoadAtlas()` 调用点名改）：

```csharp
/// v1b.3：collect atlas（atlas_count/info）→ 读 atlas.png → _texMap[atlas_tex_id]。
/// 同 atlas 所有 sprite 共享 1 Texture2D。缺图/坏图 → LogError 跳过（下游 tex_id 缺 → 白占位）。
/// len-based 串读（坑16：image_info 返无尾 NUL 串 + out_len，禁 NUL-scan）。
void LoadAtlas()
{
    _texMap.Clear();
    if (_stage == null) return;

    nuint count;
    unsafe { count = Native.loomgui_stage_atlas_count(_stage); }

    for (nuint i = 0; i < count; i++)
    {
        byte* p = null; nuint srcLen = 0; uint tid = 0; uint aw = 0; uint ah = 0;
        unsafe { p = Native.loomgui_stage_atlas_info(_stage, i, &tid, &aw, &ah, &srcLen); }
        if (p == null || srcLen == 0) continue;

        string src = Encoding.UTF8.GetString(p, (int)srcLen);
        string path = System.IO.Path.Combine(Application.streamingAssetsPath, src);

        byte[] bytes;
        try { bytes = System.IO.File.ReadAllBytes(path); }
        catch (System.Exception e)
        {
            Debug.LogError($"[LoomStage] atlas not found: {src} ({e.Message})");
            continue;
        }

        var tex = new Texture2D((int)aw, (int)ah);
        if (!tex.LoadImage(bytes))
        {
            Debug.LogError($"[LoomStage] bad atlas png: {src}");
            if (Application.isPlaying) UnityEngine.Object.Destroy(tex);
            else UnityEngine.Object.DestroyImmediate(tex);
            continue;
        }

        _texMap[tid] = tex;   // tid = i+1（core 分配）；同 atlas 所有 sprite 共享
    }
}
```

Awake 里 `if (_usePackage) LoadTextures();` → `if (_usePackage) LoadAtlas();`。`OnDestroy` Dispose `_texMap`（v1b.2 已有，全限定 `UnityEngine.Object`——坑18）零改。

- [ ] **Step 3: 新 EditMode 测 `AtlasMirrorPoolTests.cs`**

验 atlas UV blob → MirrorPool 绑 atlas Texture2D + mesh.uv == 子区 4 角。手搓 v3 blob（复用现有 blob builder 模式，参考 `MirrorPoolTexIdTests.cs`）：2 个 Mesh 节点（同 tex_id=1，不同 UV 子区）+ mock `_texMap[1]`。断言两节点 `sharedMaterial.mainTexture == atlasTex` 且 `Mesh.uv` 各自 == 子区。

```csharp
using System.Collections.Generic;
using LoomGUI;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

namespace LoomGUI.Tests
{
    public class AtlasMirrorPoolTests
    {
        // 复用 MirrorPoolTexIdTests 的 v3 blob builder 模式（见该文件），加 uv 子区写入。
        // 本测：2 Mesh 节点 tex_id=1（同 atlas），uv 分别 [0,0]-[0.5,0.5] 与 [0.5,0]-[1,0.5]。
        // mock _texMap[1] = atlasTex。Sync 后断言两 GO 的 sharedMaterial.mainTexture == atlasTex
        // 且各自 Mesh.uv 匹配其子区。
        [Test]
        public void AtlasSprites_ShareMaterial_AndBakeRegionUV()
        {
            // implementer：照 MirrorPoolTexIdTests 构 FrameBlob（v3）：
            //   - 2 节点 payload_kind=1，tex_id 列 = 1/1，mesh arena 各含 4 顶点 + 子区 uv。
            //   - mm = new MaterialManager(shader); pool = new MirrorPool();
            //   - var atlasTex = new Texture2D(64,64); var texMap = new Dictionary<uint,Texture2D>{{1, atlasTex}};
            //   - pool.Sync(blob, root, mm, texMap, Texture2D.whiteTexture, font);
            //   - 取两 GO 的 MeshRenderer.sharedMaterial，断言 .mainTexture == atlasTex（同实例）。
            //   - 断言 Mesh.uv[0]/[2] 匹配各自子区左上/右下。
            Assert.Pass("implementer 照 MirrorPoolTexIdTests 模式填；见 spec §12 Unity EditMode");
        }
    }
}
```

> implementer：把上面占位 `Assert.Pass` 替换为完整 blob 构造 + 断言（`MirrorPoolTexIdTests.cs` 是同形模板——复制其 v3 blob builder，加 uv 子区列写 + 多节点）。这是本 plan 唯一一处需 implementer 照模板填的代码——`MirrorPoolTexIdTests.cs` 提供完整 v3 blob 字节构造模式（header 92 / 14 列 / mesh arena），本测加：tex_id 列写 1 + mesh arena uv 写子区值。

- [ ] **Step 4: Unity EditMode 跑测**

Run（Unity Test Runner）: `AtlasMirrorPoolTests` + 现有 `MirrorPoolTexIdTests`/`FrameBlobTests`/`MirrorPoolTests` 全绿。
Expected: PASS（blob v3 格式未改，现有测不受影响；新 atlas UV 测过）。

- [ ] **Step 5: Commit**

```bash
git add loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs loomgui_unity/Assets/LoomGUI/Tests/AtlasMirrorPoolTests.cs
# Native 绑定按仓库惯例（gitignored 则跳过；入库则 add）
git commit -m "$(cat <<'EOF'
feat(v1b.3-T6): Unity LoadAtlas + atlas UV EditMode 测

LoomStage.LoadTextures→LoadAtlas（atlas_count/info→load atlas.png→
_texMap[atlas_tex_id]，len-based 串读）。MirrorPool/FrameBlob 零改
（已应用 blob per-vertex UV）。新 AtlasMirrorPoolTests 验同 atlas 多
sprite 共享 Material + 子区 UV 烘焙。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: sample 资产 + 重编 .dll + PlayMode 验收（押用户）

**Files:**
- Create: `loomgui_pkg/samples/atlas/`（源 html/css/PNGs——可复现）
- Create: `loomgui_unity/Assets/StreamingAssets/loom_atlas.pkg.bin` + `loom_atlas.atlas.png`（committed）
- Delete/replace: `loomgui_unity/Assets/StreamingAssets/loom_texture.pkg.bin` + `loom_sample.png`（v1b.2 sample，被 v1b.3 取代）
- Modify: `loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs`（`_pkgFile` 默认改 `loom_atlas.pkg.bin`）

**Interfaces:**
- Consumes: T1-T6 全链。

- [ ] **Step 1: 建 sample 源（3 张不同色 PNG + html）**

在 `loomgui_pkg/samples/atlas/` 放：`page.html`（含 3 个 `<img>` 不同 src）、`page.css`、`red.png`（64×64 红）、`green.png`（64×64 绿）、`blue.png`（48×48 蓝）。PNG 用一次性脚本生成（`image` crate 或手工）。`page.html`：
```html
<div class="c">
  <img src="red.png"><img src="green.png"><img src="blue.png">
</div>
```
`page.css`：
```css
.c{display:flex;flex-direction:row;width:600px;height:200px;gap:8px;background-color:#222222;}
```

- [ ] **Step 2: 跑 packer 产 .pkg.bin + atlas.png**

Run: `cargo run -p loomgui_pkg -- loomgui_pkg/samples/atlas/page.html loomgui_pkg/samples/atlas/page.css -o loomgui_unity/Assets/StreamingAssets/loom_atlas.pkg.bin`
（packer 自动写 `loom_atlas.atlas.png` 同目录旁挂——main.rs `with_extension("atlas.png")`。）
Expected: `loom_atlas.pkg.bin`（v2 magic LPKG + version 2）+ `loom_atlas.atlas.png`（3 色块横排）。

- [ ] **Step 3: 删 v1b.2 sample + 改 _pkgFile**

删 `loomgui_unity/Assets/StreamingAssets/loom_texture.pkg.bin` + `loom_sample.png`（含 .meta）。`LoomStage.cs` 的 `_pkgFile = "loom_default.pkg.bin"` / `"loom_texture.pkg.bin"` → `"loom_atlas.pkg.bin"`。

- [ ] **Step 4: 重编 release .dll（关 Unity，坑10）**

确认 Unity 关闭（用户）。Run:
```bash
cargo build -p loomgui_ffi_c --release
# 拷 target/release/loomgui_ffi_c.dll → loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
```
md5 校验：`md5sum target/release/loomgui_ffi_c.dll loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll` 两值相等（坑10 stale-.dll 诊断）。

- [ ] **Step 5: 押用户 PlayMode 验收**

Unity 开 → 场景 LoomStage `_usePackage=true` → PlayMode：
- 3 张图全显（红/绿/蓝），**方向正**（不倒/不镜像——R1 UV 方向约定）。
- **FrameDebugger**：3 个 Image MeshRenderer 共享同 1 Material（同 atlas texture）→ SRP Batcher 批合（同 material 批）。
- 开 URP RenderPipelineAsset Dynamic Batching → draw call < 3（可能合并，best-effort）。
- 缺 atlas.png → 白占位 + Console 红字（§11 降级）。

- [ ] **Step 6: Commit**

```bash
git add loomgui_pkg/samples/atlas loomgui_unity/Assets/StreamingAssets/loom_atlas.pkg.bin loomgui_unity/Assets/StreamingAssets/loom_atlas.atlas.png loomgui_unity/Assets/LoomGUI/Runtime/LoomStage.cs loomgui_unity/Assets/Plugins/LoomGUI/loomgui_ffi_c.dll
# 删除的 v1b.2 sample：git rm loomgui_unity/Assets/StreamingAssets/loom_texture.pkg.bin loomgui_unity/Assets/StreamingAssets/loom_sample.png
git commit -m "$(cat <<'EOF'
feat(v1b.3-T7): atlas sample + release .dll + PlayMode 验收 G7+

3 张不同色散图 → packer 产 loom_atlas.pkg.bin(v2) + loom_atlas.atlas.png。
LoomStage _pkgFile 改 loom_atlas.pkg.bin。release .dll 重编换（坑10）。
删 v1b.2 loose sample（loom_texture.pkg.bin + loom_sample.png）。
PlayMode 验收：3 图共显方向正 + FrameDebugger 同 Material 批合（G7+）。

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review（写后自查）

**1. Spec 覆盖**：
- §5 打包器（image+shelf+atlas.png+pack 4-arg+缺图 fail）→ T4 ✓
- §6 .pkg.bin v2（AtlasSection+version 2）→ T3 ✓
- §7 core（TexMeta+uv/build_registry/quad uv_rect/Image 烤/measure 零改/UV 方向）→ T1/T2/T3 ✓
- §8 FFI（删 loose/加 atlas_count-info/len-based）→ T5 ✓
- §9 blob v3 不变 → 全 plan 不动 blob，T6 仅验 ✓
- §10 Unity（LoadAtlas/MirrorPool 不改/内容管线）→ T6/T7 ✓
- §11 错误处理（缺图 build fail/atlas 坏降级）→ T4/T6 ✓
- §12 测试（packer shelf/PNG round-trip/core registry+UV/measure/blob v3 绿/FFI atlas/Unity EditMode/PlayMode）→ T1-T7 ✓
- §13 风险（R1 UV 方向 PlayMode 验/R2 stale v2.dll/R3 shelf 测/R4 image API adapt/R5 收益认知/R6 泄漏）→ T4/T5/T6/T7 + Global Constraints ✓

**2. Placeholder 扫描**：T6 Step 3 的 `Assert.Pass` 占位是唯一一处——明确指向 `MirrorPoolTexIdTests.cs` 模板填（非敷衍，是该测需照 v3 blob 构造模板，plan 已指出模板位置 + 要加的列写）。其余步骤皆含完整代码。T4 Step 2 的 image API 标 §3 adapt（算法完整，仅 crate 方法名待验）。无 TBD/TODO。

**3. 类型一致性**：`TexMeta { tex_id, uv_min, uv_max, width, height }`（T1）→ T2 render 读 `m.uv_min/uv_max`、T3 build_registry 填同字段 ✓。`AtlasSection/Info/Sprite`（T3）→ T4 packer 构、T5 FFI 读 `Stage.atlases: Vec<AtlasInfo>` ✓。`write_package(scene, root_size, &AtlasSection)` / `read_package -> (Scene, (f32,f32), AtlasSection)`（T3）→ T4 packer 调、T5 测调 ✓。FFI `atlas_info` tex_id = `(i+1)`（T5）↔ core `build_registry` atlas[0]→1（T3）↔ Unity `_texMap[tid]`（T6）✓。blob v3 14 列全 plan 不动 ✓。

**4. 无遗漏 spec 项**：rotation/trim/multi-atlas/refcount 明确 defer（§15），plan 不含——符合 scope。measure 三档 T1 仅改测试 fixture（生产零改）——符合 §7.6。
