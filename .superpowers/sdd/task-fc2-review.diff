=== commits 990d3e6..e9bf909 ===
e9bf909 docs(fence): 围栏文档同步去 l-container（fence.md + editor 副本 + rules 模板）

=== diff stat ===
 docs/design/fence.md                            | 1 -
 editor/rules/claude/CLAUDE.md.tmpl              | 2 +-
 editor/rules/codex/AGENTS.md.tmpl               | 2 +-
 editor/rules/opencode/AGENTS.md.tmpl            | 2 +-
 editor/skill/loomgui-editor/references/fence.md | 1 -
 5 files changed, 3 insertions(+), 5 deletions(-)

=== full diff ===
diff --git a/docs/design/fence.md b/docs/design/fence.md
index 4873376..a7f3b0f 100644
--- a/docs/design/fence.md
+++ b/docs/design/fence.md
@@ -25,17 +25,16 @@
 **白名单**（`FENCE_TAGS`，parse/dom.rs:29）：
 
 | 标签 | 映射 NodeKind | 出处 |
 |---|---|---|
 | `div` | Container | scene/node.rs:278 |
 | `span` | Text（内容取 `el.text`） | scene/node.rs:283 |
 | `img` | Image（src 取 `el.attrs["src"]`） | scene/node.rs:280 |
 | `button` | Button | scene/node.rs:279 |
-| `l-container` | Container（与 div 同） | scene/node.rs:278 |
 
 **围栏外标签**：一律**报错**（不降级、不静默忽略）。parse/dom.rs:63-68。
 - 【实证】`rejects_fence_out_element`（dom.rs:203）：`<video>`/`<input>`/`<b>` 等被拒。
 - 【实证】`fence_tags_all_accepted`（dom.rs:216）：白名单标签全接受。
 
 **裸文本规则**：Container/Button 内裸文本自动生成 Text 子节点（scene/node.rs:304-316）。行内混排（元素内"文本+元素+文本"）报错（dom.rs:31 注释）。
 
 ---
diff --git a/editor/rules/claude/CLAUDE.md.tmpl b/editor/rules/claude/CLAUDE.md.tmpl
index 9ba7dba..f50e8b6 100644
--- a/editor/rules/claude/CLAUDE.md.tmpl
+++ b/editor/rules/claude/CLAUDE.md.tmpl
@@ -1,15 +1,15 @@
 <!-- loomgui-editor-begin -->
 # LoomGUI 围栏规则（硬约束）
 
 生成 HTML+CSS 时严守以下围栏。围栏外写法写了不报错但**不生效**（静默忽略），会导致预览与 Unity 渲染不一致 = 不可预测。完整规则见 skill references/fence.md。
 
 ## 元素白名单
-只用 `div` / `span`（+裸文本）/ `img` / `button` / `l-container`。其他标签（video/input/p/ul/...）会报错。
+只用 `div` / `span`（+裸文本）/ `img` / `button`。其他标签（video/input/p/ul/...）会报错。
 
 ## CSS 布局
 - `display:flex/none`（**禁 grid**，写了落 Flex 预览会骗你）
 - `flex-direction` / `flex-wrap` / `gap` / `row-gap` / `column-gap` / `justify-content` / `align-items` / `align-self` / `flex`(grow/shrink/basis) / `order` / `aspect-ratio`
 - `width/height/min/max`(px/%/auto) / `padding` / `margin` / `border-width`
 - **子项间距用 `gap`，别用 margin**（Chrome 折叠 margin、LoomGUI 求和不折叠）
 - 禁：`position:absolute/fixed/sticky`（写了不脱离流）、`float`、`align-content`
 
diff --git a/editor/rules/codex/AGENTS.md.tmpl b/editor/rules/codex/AGENTS.md.tmpl
index 9ba7dba..f50e8b6 100644
--- a/editor/rules/codex/AGENTS.md.tmpl
+++ b/editor/rules/codex/AGENTS.md.tmpl
@@ -1,15 +1,15 @@
 <!-- loomgui-editor-begin -->
 # LoomGUI 围栏规则（硬约束）
 
 生成 HTML+CSS 时严守以下围栏。围栏外写法写了不报错但**不生效**（静默忽略），会导致预览与 Unity 渲染不一致 = 不可预测。完整规则见 skill references/fence.md。
 
 ## 元素白名单
-只用 `div` / `span`（+裸文本）/ `img` / `button` / `l-container`。其他标签（video/input/p/ul/...）会报错。
+只用 `div` / `span`（+裸文本）/ `img` / `button`。其他标签（video/input/p/ul/...）会报错。
 
 ## CSS 布局
 - `display:flex/none`（**禁 grid**，写了落 Flex 预览会骗你）
 - `flex-direction` / `flex-wrap` / `gap` / `row-gap` / `column-gap` / `justify-content` / `align-items` / `align-self` / `flex`(grow/shrink/basis) / `order` / `aspect-ratio`
 - `width/height/min/max`(px/%/auto) / `padding` / `margin` / `border-width`
 - **子项间距用 `gap`，别用 margin**（Chrome 折叠 margin、LoomGUI 求和不折叠）
 - 禁：`position:absolute/fixed/sticky`（写了不脱离流）、`float`、`align-content`
 
diff --git a/editor/rules/opencode/AGENTS.md.tmpl b/editor/rules/opencode/AGENTS.md.tmpl
index 9ba7dba..f50e8b6 100644
--- a/editor/rules/opencode/AGENTS.md.tmpl
+++ b/editor/rules/opencode/AGENTS.md.tmpl
@@ -1,15 +1,15 @@
 <!-- loomgui-editor-begin -->
 # LoomGUI 围栏规则（硬约束）
 
 生成 HTML+CSS 时严守以下围栏。围栏外写法写了不报错但**不生效**（静默忽略），会导致预览与 Unity 渲染不一致 = 不可预测。完整规则见 skill references/fence.md。
 
 ## 元素白名单
-只用 `div` / `span`（+裸文本）/ `img` / `button` / `l-container`。其他标签（video/input/p/ul/...）会报错。
+只用 `div` / `span`（+裸文本）/ `img` / `button`。其他标签（video/input/p/ul/...）会报错。
 
 ## CSS 布局
 - `display:flex/none`（**禁 grid**，写了落 Flex 预览会骗你）
 - `flex-direction` / `flex-wrap` / `gap` / `row-gap` / `column-gap` / `justify-content` / `align-items` / `align-self` / `flex`(grow/shrink/basis) / `order` / `aspect-ratio`
 - `width/height/min/max`(px/%/auto) / `padding` / `margin` / `border-width`
 - **子项间距用 `gap`，别用 margin**（Chrome 折叠 margin、LoomGUI 求和不折叠）
 - 禁：`position:absolute/fixed/sticky`（写了不脱离流）、`float`、`align-content`
 
diff --git a/editor/skill/loomgui-editor/references/fence.md b/editor/skill/loomgui-editor/references/fence.md
index 4873376..a7f3b0f 100644
--- a/editor/skill/loomgui-editor/references/fence.md
+++ b/editor/skill/loomgui-editor/references/fence.md
@@ -25,17 +25,16 @@
 **白名单**（`FENCE_TAGS`，parse/dom.rs:29）：
 
 | 标签 | 映射 NodeKind | 出处 |
 |---|---|---|
 | `div` | Container | scene/node.rs:278 |
 | `span` | Text（内容取 `el.text`） | scene/node.rs:283 |
 | `img` | Image（src 取 `el.attrs["src"]`） | scene/node.rs:280 |
 | `button` | Button | scene/node.rs:279 |
-| `l-container` | Container（与 div 同） | scene/node.rs:278 |
 
 **围栏外标签**：一律**报错**（不降级、不静默忽略）。parse/dom.rs:63-68。
 - 【实证】`rejects_fence_out_element`（dom.rs:203）：`<video>`/`<input>`/`<b>` 等被拒。
 - 【实证】`fence_tags_all_accepted`（dom.rs:216）：白名单标签全接受。
 
 **裸文本规则**：Container/Button 内裸文本自动生成 Text 子节点（scene/node.rs:304-316）。行内混排（元素内"文本+元素+文本"）报错（dom.rs:31 注释）。
 
 ---
